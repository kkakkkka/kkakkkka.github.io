<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2022/02/26/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo new <span class="token string">"My New Post"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo server<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo generate<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo deploy<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>杂七杂八</category>
      </categories>
      <tags>
        <tag>杂七杂八</tag>
      </tags>
  </entry>
  <entry>
    <title>C++程序设计辅导-知识点总结</title>
    <url>/2022/04/19/help-cpp/</url>
    <content><![CDATA[<h2 id="第一周"><a href="#第一周" class="headerlink" title="第一周"></a>第一周</h2><ul>
<li>I/O（cin、cout）</li>
<li>namespace（多个同名变量）（必考，给一段代码问最后哪些变量的值是多少）</li>
<li>引用（写个函数，可以直接交换两个数的值）</li>
<li>指针和引用的区别（01：22）<ul>
<li>相同：可以使一个函数向调用者返回多个数值。 </li>
<li>不同：原理不同。引用传递中，形参、实参实质为同一变 量，或者说是为某个变量起多了一个名字。而使用指针作 函数参数，则是使被调用函数获得某变量的地址，从而使 用这个地址访问这个变量。 </li>
<li>从返回值的角度看，引用形参比利用指针方便。</li>
</ul>
</li>
<li>const和constexpr（01：26、27）</li>
<li>auto（01：28）</li>
</ul>
<h2 id="第二周"><a href="#第二周" class="headerlink" title="第二周"></a>第二周</h2><ul>
<li>什么是抽象（04：4）<ul>
<li>Data abstraction：只关心该数据“是什么”及“如何使用”，而 不关心它是如何运作的。 </li>
<li>Control abstraction：只关心这个行为能够为我们带来什么，而 不关心这个行为的具体实现方法。</li>
</ul>
</li>
<li>抽象数据类型（ADT）的定义、组成（02：5）<ul>
<li>在程序设计中，对于被抽象的数据，称为抽象数据类型  （Abstract Data Type, ADT）。 </li>
<li>一种ADT应具有 </li>
<li>说明部分（说明该该ADT是什么及如何使用）：说明部分描述 数据值的特性和作用于这些数据之上的操作。ADT的用户仅须 明白这些说明，而无须知晓其内部实现。 </li>
<li>实现部分。</li>
</ul>
</li>
<li>类的成员应该定义为私有还是公有？（02：12）</li>
<li>结构体和类的区别（02：13、14）<ul>
<li>C中的结构体只能包含变量，而C++中的类除了包含变量，还能包含函数</li>
<li>结构体定义出来的变量还是叫变量，而类定义出来的变量叫对象</li>
</ul>
</li>
<li>类的成员函数的定义（在类定义内部进行定义和在类定义外部进行定义的区别）（02：16）</li>
<li>类的私有成员变量的访问和修改（通过get和set函数）</li>
<li>类的静态成员变量（初始化，值的修改）（02：28、30、32）</li>
</ul>
<h2 id="第三周"><a href="#第三周" class="headerlink" title="第三周"></a>第三周</h2><ul>
<li>新类型：bool<ul>
<li>用cout输出的结果是什么？</li>
</ul>
</li>
<li>函数重载（03：3、4）（必考）</li>
<li>函数的默认参数（03：5）（必考）<ul>
<li>主要是判断默认参数的位置是否正确，放位置不符合规范会报错</li>
</ul>
</li>
<li>string字符串<ul>
<li>本质是类</li>
<li>几种构造方法：默认构造、cstring构造、同类对象构造、int，char作为参数构造</li>
</ul>
</li>
<li>public、private与访问控制（必考）<ul>
<li>公有成员在程序中类的外部是可访问的。您可以不使用任何成员函数来设置和 获取公有变量的值 </li>
<li>私有成员变量或函数在类的外 部是不可访问的，甚至是不可 查看的。只有类和友元函数可 以访问私有成员。 默认情况下，类的所有成员都 是私有的。例如在右面的类中， width 是一个私有成员，这意 味着，如果您没有使用任何访 问修饰符，类的成员将被假定 为私有成员。</li>
</ul>
</li>
<li>构造函数（默认、带参数）（03：代码全部敲一遍）<ul>
<li>如果设计的类没有构造函数，C++编译器会自动为该类型建立一个缺省构造函数。该构造 函数没有任何形参，且函数体为空。应该养成编写构造函数的习惯。</li>
</ul>
</li>
<li>析构函数（ 每次删除所创建的对象时执行）</li>
<li>PS：理论和实验都很喜欢考构造函数、析构函数，一定要搞明白</li>
<li>this指针<ul>
<li>在 C++ 中，每一个对象都能通过 this 指针来访问自己的地址。this 指针是所有成 员函数的隐含参数。因此，在成员函数内部，它可以用来指向调用对象。 </li>
<li>友元函数没有 this 指针，因为友元不是类的成员。 只有动态成员函数才有 this 指针。 （TODO) </li>
<li>当成员函数参数与成员数据重名时， 必须用 this 访问成员数据</li>
</ul>
</li>
</ul>
<h2 id="第四周"><a href="#第四周" class="headerlink" title="第四周"></a>第四周</h2><ul>
<li><p>程序内存布局与对象（变量）存放位置（记住结论即可，不是这门课程的重点，原理会在计算机组成原理课程中深入学习）(04：2、3)</p>
<ul>
<li><p>正文（ 即代码，函数实现，库实现，字符串等资源。 ）</p>
</li>
<li><p>静态变量（对象） </p>
</li>
<li><p>编译阶段初始化数据  </p>
</li>
<li><p>用零初始化  </p>
</li>
<li><p>栈（Stack）  </p>
</li>
<li><p>函数参数，自动变量（对象） 存放在栈里面</p>
</li>
<li><p>堆（Heap） </p>
</li>
<li><p>动态变量（对象）存放在堆里面，由 stdlib.h 管理 </p>
</li>
<li><p>这个布局图可以记一下，考的概率不大</p>
<img src="1649142143298.png" alt="1649142143298" style="zoom: 80%;">
</li>
<li><p>动态变量与对象指针</p>
<ul>
<li>为变量申请内存（new）</li>
<li>使用变量（解引用）</li>
<li>释放内存（delete，matrix上机考试会检查内存泄漏）</li>
</ul>
</li>
<li><p>什么是野指针</p>
<ul>
<li>指针指向的位置是不可知的（随机的、不正确的、没有明确限制的）指针变量在定义时如果未初始化，其值是随机的 </li>
<li>指针变量在定义时如果未初始化，其值是随机的， 意味着指针指向了一个地址是不确定的变量 ， 此时去解引用就是去访问了一个不确定的地址，所以结果是不可知的。 </li>
<li>课件（04：16）给出的是另一种例子，已经释放内存后尝试解引用）</li>
</ul>
</li>
<li><p>c++ 11 中显式默认设置的函数和已删除的函数（关键字=default和=delete）</p>
<ul>
<li>在C++的类中，有四类特殊的成员函数：① 默认构造函数；② 拷贝构造函数；③ 拷贝赋值函数（operator=）；④ 析构函数；它们控制着类的实例的创建、初始化、拷贝以及销毁。 </li>
<li>如果显式声明了任何构造函数，则不会自动生成默认构造函数。</li>
<li>如果显式声明了虚拟析构函数，则不会自动生成默认析构函数。</li>
<li>如果显式声明了移动构造函数或移动赋值运算符，不自动生成复制构造函数/复制赋值运算符。</li>
<li>如果显式声明了复制构造函数、复制赋值运算符、移动构造函数、移动赋值运算符或析构函数，不自动生成移动构造函数/移动赋值运算符。</li>
<li>通过关键字=default，可以编译器会隐式生成一个版本，在代码更加简洁的同时，编译器隐式生成的版本的执行效率更高。</li>
<li>通过关键字=delete，可以标记不想被类实例调用的函数（例如拷贝构造和拷贝赋值），相当于删除了它们。</li>
<li>放一个讲的很详细的链接 <a href="https://www.bilibili.com/read/cv13912639">https://www.bilibili.com/read/cv13912639</a></li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="第五周"><a href="#第五周" class="headerlink" title="第五周"></a>第五周</h2><ul>
<li><p>构造函数的列表初始化</p>
<ul>
<li>使用的好处： 使用初始化列表少了一次调用默认构造函数的过程，这对于数据密集型的类来说，是非常 高效的。  </li>
<li>必须使用初始化列表<ul>
<li>常量成员：只能初始化不能赋值</li>
<li>引用类型：必须在定义时进行初始化，且不能重新赋值</li>
<li>没有默认构造函数的类类型： 使用初始化列表可以不必调用默认构造函数来初始 化，而是直接调用拷贝构造函数初始化</li>
</ul>
</li>
<li>初始化顺序<ul>
<li>按照在类中出现的顺序，而不是在初始化列表中出现的顺序（PPT有例题）</li>
</ul>
</li>
</ul>
</li>
<li><p>对象的内存布局</p>
<ul>
<li>全局数据区、代码区、栈区、堆区（自由存储区）<ul>
<li>全局数据区存放全局变量，静态数据和常量 </li>
<li>所有类成员函数和非成员函数代码存放在代码区 </li>
<li>为运行函数而分配的局部变量、函数参数、返回数据、返回地址等存放在栈区 </li>
<li>余下的空间都被称为堆区 </li>
<li>在类的定义时，类的成员函数被放在代码区。 </li>
<li>类的静态成员变量在全局数据区。</li>
<li>非静态成员变量在类的实例内，实例在栈区或者堆区。</li>
<li>虚函数指针、虚基类指针在类的实例内，实例在栈区或者堆区。</li>
<li>类的实例如果是定义的类变量，则在栈内存区，</li>
<li>如果是new出来的类指针，则在堆内存区，同时引用会保存在栈里。</li>
</ul>
</li>
<li><img src="/2022/04/19/help-cpp/1649513082560.png" alt="1649513082560"></li>
<li>对象方法静态联编：指在编译阶段，就能直接使 用代码段函数地址调用动态对象的方法。该方法仅需要向非静态成员函数传送this指针，即可用实现静态调用。   </li>
<li>优势： <ul>
<li>对象布局与C结构内存布局一致，使得内存 中对象便于与其他语言程序库兼容 </li>
<li>高性能</li>
</ul>
</li>
</ul>
</li>
<li><p>拷贝构造函数</p>
<ul>
<li><p>形参类型为该类类型本身，参数按引用传递（避免在函数调用过程中生成形参副本，一般声明为const）</p>
<pre class="line-numbers language-none"><code class="language-none">C::C(const C&amp; obj);<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
</li>
<li><p>缺省拷贝构造函数：逐位复制</p>
</li>
<li><p>几种应用场景（05：14、15、16）</p>
</li>
<li><p>浅拷贝与深拷贝的概念、区别及实现（考点）</p>
</li>
</ul>
</li>
</ul>
<h2 id="第六周"><a href="#第六周" class="headerlink" title="第六周"></a>第六周</h2><h2 id="第七周"><a href="#第七周" class="headerlink" title="第七周"></a>第七周</h2><h2 id="第八周"><a href="#第八周" class="headerlink" title="第八周"></a>第八周</h2><h2 id="第九周"><a href="#第九周" class="headerlink" title="第九周"></a>第九周</h2>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>如何复现论文</title>
    <url>/2022/06/11/howtoreproduce/</url>
    <content><![CDATA[<h2 id="新领域无从下手"><a href="#新领域无从下手" class="headerlink" title="新领域无从下手"></a>新领域无从下手</h2><p>阅读已有新工作的必经之路，所以努力要站在巨人的肩膀上。</p>
<ol>
<li>阅读目标领域内近3-5年顶会论文，特别是综述论文。</li>
<li>收集公开赛事或榜单上学习Top名次的解决方案，包含思路和代码。</li>
</ol>
<p>无他唯孰能尔，多收集多整理，理解领域思路和套路。</p>
<h2 id="复现别人的论文"><a href="#复现别人的论文" class="headerlink" title="复现别人的论文"></a>复现别人的论文</h2><p>科研并不是从0到1，一定要多去了解已有的工作，以及已有的论文代码。在阅读完论文代码后，可以按照如下步骤逐步复现： </p>
<h3 id="步骤1：找到有开源代码的论文"><a href="#步骤1：找到有开源代码的论文" class="headerlink" title="步骤1：找到有开源代码的论文"></a>步骤1：找到有开源代码的论文</h3><p>在Github上找到历史的有代码的论文，这些论文项目虽然比较旧，但参考价值很大。</p>
<h3 id="步骤2：整理好数据集的加载"><a href="#步骤2：整理好数据集的加载" class="headerlink" title="步骤2：整理好数据集的加载"></a>步骤2：整理好数据集的加载</h3><p>搞清楚数据集怎么制作怎么加载怎么输入怎么计算怎么输出，数据集如何处理，如何编码。</p>
<h3 id="步骤3：搭建论文模型"><a href="#步骤3：搭建论文模型" class="headerlink" title="步骤3：搭建论文模型"></a>步骤3：搭建论文模型</h3><p>基于论文思路整理好模型结构，有多少层，每层的细节，每层的维度，一步一步搭建。保证模型可以正常训练和预测。</p>
<h3 id="步骤4：确定训练细节"><a href="#步骤4：确定训练细节" class="headerlink" title="步骤4：确定训练细节"></a>步骤4：确定训练细节</h3><p>根据论文实验部分的细节，确定具体的batch、epoch、学习率和优化器，确定训练的流程没有问题。</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>方法论</tag>
      </tags>
  </entry>
  <entry>
    <title>打造你自己的集群</title>
    <url>/2022/03/01/build-small-cluster/</url>
    <content><![CDATA[<p>SYSU-SCC的选拔赛搭建系统题，因为升学计划改变，没有接着做优化题，最后也没有加入超算队，但从这些题目中学习了很多知识。记录一下这些非常好的集群入门题。</p>
<h2 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h2><p>超算集群比赛，首先要做的事情当然是组集群。机器硬件配好了，系统软件也要跟上来！在这部分的任务里，我们希望你使用虚拟机构建集群，并准备一些系统软件（例如编译器和库）。最后，你需要在集群上编译一个真实的应用程序。</p>
<p>这些任务要求你需要熟悉 Linux 的相关操作（不懂也没关系，按着我们材料的学习路径自行学习即可）。你不必完成所有任务。我们在下表中列出了建议，“R”为“建议完成”和“O”为“可选”。</p>
<table>
<thead>
<tr>
<th>task</th>
<th>大一/大二</th>
<th>大三/大四</th>
<th>想要成为集群的维护者</th>
</tr>
</thead>
<tbody><tr>
<td>task 1</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 2</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 3</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 4</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 5</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 6</td>
<td>r</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 7</td>
<td>o</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 8</td>
<td>o</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 9</td>
<td>o</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>task 10</td>
<td>o</td>
<td>r</td>
<td>r</td>
</tr>
<tr>
<td>final task</td>
<td>o</td>
<td>o</td>
<td>r</td>
</tr>
</tbody></table>
<h3 id="对该集群，我们期望的架构"><a href="#对该集群，我们期望的架构" class="headerlink" title="对该集群，我们期望的架构"></a>对该集群，我们期望的架构</h3><p>一个集群一般包括高速网络，大容量存储设备，多核处理器，以及相关的系统软件，如分布式文件系统，编译器和库等软件。</p>
<p>在这个项目中，你的目标是构建一个小但完整的双节点集群，如下图所示：</p>
<p><img src="/2022/03/01/build-small-cluster/arch.jpeg"></p>
<p>Node 1 包含两个物理盘（实际上是两个虚拟盘），一个物理盘（挂载在<code>/data/</code> 路径下）。一个物理盘（在<code>/</code>目录下）用于安装操作系统，另一个物理盘（在<code>/data/</code>目录下）作为数据盘存储其他的数据（如第三方库、计算数据等），Node 2 仅有一个包含操作系统的物理盘， Node 2 的 <code>/data/</code>路径会通过 NFS 把 Node 1 的 <code>/data/</code> 挂载过来。这样的设计能让两个节点共享 <code>/data/</code> 目录下的数据。</p>
<h3 id="提示"><a href="#提示" class="headerlink" title="提示"></a>提示</h3><p>我们假设你可能没有足够的硬件来部署真实的集群。因此，你可以使用虚拟机进行下面的任务。当然，我们也欢迎你使用真正的硬件来完成以下任务。</p>
<p>在下面的所有任务中，我们将为你提供目标和相应的TODO列表。此外，我们还提供一些提示，并希望你顺利完成这些任务。如果你遇到一些困难，可以随意Google。如果觉得自己遇到了难于解决的问题，可以随时向我们提问。<strong>不要闭门造车！</strong></p>
<h3 id="如何提交"><a href="#如何提交" class="headerlink" title="如何提交"></a>如何提交</h3><p>你不需要提交一个额外的报告，你只需要完成下面的 <strong>TODO</strong> ，并直接相关信息填在该 README.md 上即可。</p>
<p>对于Todo列表中的每个项目，你可以放置包含命令的代码块。如果需要，可以附加相关的屏幕截图以证明你成功完成了该任务。此外，你还可以撰写笔记以解释详细信息。</p>
<p>TIPS: 一般来说，把命令和log直接贴出来能够让我们对你所做的事情有更清晰的认识，”talk is cheap, show me your code”，大多数无关紧要的步骤直接用代码块贴log（或者只贴命令）即可，可以仅在你觉得需要的部分放截图。</p>
<h2 id="Task-1-：设置一个计算节点，并安装操作系统"><a href="#Task-1-：设置一个计算节点，并安装操作系统" class="headerlink" title="Task 1 ：设置一个计算节点，并安装操作系统"></a>Task 1 ：设置一个计算节点，并安装操作系统</h2><p>在本任务中，你需要在集群中设置一个标准的计算节点。</p>
<p>我们建议使用 <strong>VMware Workstation player</strong> 创建虚拟机。**<a href="https://www.vmware.com/products/workstation-player/workstation-player-evaluation.html">VMware workstation player</a>**可以免费下载使用，没有必要花时间寻找Pro版本。当然， 也可以用<a href="https://www.virtualbox.org/">VirtualBox</a> 或 <a href="https://virt-manager.org/">virt-manager</a> 。</p>
<p>为了安装和设置一个标准的计算节点，你需要：</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 在虚拟机上安装一个操作系统<ul>
<li>你可以选择 <strong>CentOS 7</strong> , <strong>CentOS 8</strong> 或者 <strong>Debian 10</strong>, <strong>Debian 11</strong> 作为操作系统。</li>
<li>TODO：解释你选择的理由？不存在标准答案，说你自己的理解即可。</li>
<li>把系统盘的大小设置在大概 25 GB 就够了。</li>
<li>设置用户名时，我们推荐把用户名设置为 <code>scc</code> 。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 通过 OS 自带的包管理器（如 <code>yum</code> 或 <code>apt</code> ）安装一些必要的软件<ul>
<li>安装一些必要的软件： gcc/g++/gfortran/nfs-utils/tmux/htop/vim/git</li>
<li>你还可以安装一些其他你喜欢的软件。<ul>
<li>TODO：你可以简单说说你为啥喜欢这些软件</li>
</ul>
</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 把该计算节点的 hostname 设置为 <code>node1</code><ul>
<li>如何保证该设置的效果重启后还能生效？</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 关闭一些无用的服务<ul>
<li>例如： firewall/SELinux</li>
<li>TODO：为什么我们说这些服务是没用的，关闭他们有什么好处？</li>
<li>TODO：如何关闭服务？如何保证已关闭的服务不会开机自启？</li>
<li>TODO：你觉得还有什么服务是没用的吗？</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> （可选）编写一个脚本把上面的这些琐碎事情自动化，并用这个脚本一键初始化 node2<ul>
<li>如何编写 bash 脚本？一些参考资料 <a href="https://www.bilibili.com/video/BV1bU4y1a73h">link1</a> <a href="https://www.bilibili.com/video/BV1y44y1v7c3">link2</a> <a href="https://missing.csail.mit.edu/2020/shell-tools/">link3</a></li>
</ul>
</li>
</ul>
<h3 id="提交"><a href="#提交" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>我选择的是centos7，因为centos是从红帽企业版中抽离出的源码再次编译而成的，具有使用简单，兼容性好等优点。相较于debian，其使用了企业版源码，不会像debian一样使用一些不严谨的驱动程序代码造成驱动安装问题。而使用centos7而非centos8的原因是8已经停止了维护，而7还在维护。</p>
</li>
<li><p>我喜欢这些软件的原因是我经常使用他们，他们能够帮助我完成很多工作。</p>
</li>
<li><p>修改/etc/hostname文件内容即可保证其在重启后依然生效。</p>
</li>
<li><p>关闭服务</p>
<ul>
<li><p>因为我们只是在内网使用，无需加强安全防护，如果不关闭的话在进行操作的时候会造成一些麻烦。</p>
</li>
<li><p>通过下列指令即可关闭服务，同时将其从开机自启项中移除</p>
<pre class="line-numbers language-none"><code class="language-none">systemctl stop firewalld.service
systemctl disable firewalld.service
sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>iptables也可以关掉</p>
</li>
</ul>
</li>
<li><p>脚本内容如下</p>
<pre class="line-numbers language-none"><code class="language-none">sed -i 's/ONBOOT=no/ONBOOT=yes/g' /etc/sysconfig/network-scripts/ifcfg-ens33
service network restart

rm -f /etc/hostname
echo Node2 &gt; /etc/hostname

yum install wget gcc gcc-c++ gdb make git net-tools traceroute -y 

systemctl stop firewalld.service
systemctl disable firewalld.service
sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config

reboot<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
</ul>
<h2 id="Task-2-设置node2，测试网络，测试-ssh-链接"><a href="#Task-2-设置node2，测试网络，测试-ssh-链接" class="headerlink" title="Task 2: 设置node2，测试网络，测试 ssh 链接"></a>Task 2: 设置node2，测试网络，测试 ssh 链接</h2><p>在本任务中，你需要设置第二个计算节点 <code>node2</code>，配置与 <code>node1</code> 相同。此外，你还需要测试 <code>node1</code> 和 <code>node2</code> 之间的网络。</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 设置 <code>node2</code>，<code>node2</code> 和 <code>node1</code> 配置一样<ul>
<li>你可以直接复制 <code>node1</code> 的系统镜像，也可以重复 <code>node1</code> 上的操作。</li>
<li>注意: <code>node1</code> 和 <code>node2</code> 需要有相同的用户名，设置 hostname 的时候需要设置成 <code>node2</code></li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 找到并查看 <code>node1</code> 和 <code>node2</code> 上的ip地址和路由表。<ul>
<li>Tips: 学习使用命令 <code>ifconfig</code>/<code>ip</code>/…</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 测试 <code>node1</code> 和 <code>node2</code> 之间的网络是否连通。<ul>
<li>Tips: 学习使用命令 <code>ping</code>/<code>traceroute</code></li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 学习使用 <code>ssh</code> <ul>
<li><input checked="" disabled="" type="checkbox"> TODO: 从物理机，使用 <code>ssh</code> 直接连接 <code>node1</code> 和 <code>node2</code> 操作这两个节点 ，而不使用 GUI 界面</li>
<li><input checked="" disabled="" type="checkbox"> TODO: 在 <code>node1</code> 上 使用 <code>ssh</code> 连接到 <code>node2</code></li>
<li><input checked="" disabled="" type="checkbox"> TODO: 在 <code>node2</code> 上 使用 <code>ssh</code> 连接到 <code>node1</code></li>
<li><input checked="" disabled="" type="checkbox"> TODO: 配置免密登陆，让物理机能够不需要输入密码就能登陆 <code>node1</code> 和 <code>node2</code></li>
<li><input checked="" disabled="" type="checkbox"> TODO: 配置免密登陆，让 <code>node1</code> 和 <code>node2</code> 之间可以免密登陆<ul>
<li>Tips: 学习使用私钥登陆，<a href="https://www.ruanyifeng.com/blog/2011/12/ssh_remote_login.html">ssh 相关教程</a></li>
</ul>
</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 学习一个配置文件 <code>/etc/hosts</code><ul>
<li><input checked="" disabled="" type="checkbox"> 在前面的操作中，需要经常输入虚拟机的ip地址，有没有办法可以偷懒不去记？<ul>
<li>配好后，你应该可以在 <code>node1</code> 上直接输入 <code>ssh node2</code> 就可以连接 <code>node2</code>.</li>
<li>或者在<code>node1</code> 上直接 <code>ping node2</code> 就可以测试 <code>node1</code> 和 <code>node2</code> 之间的网络</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="提交-1"><a href="#提交-1" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>按照脚本设置即可。</p>
</li>
<li><p>输入ifconfig指令，查看node1和node2的ip地址，分别为192.168.8.130和192.168.8.131</p>
</li>
<li><p>通过ping和traceroute指令进行网络连通性的测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643723367816.png" alt="1643723367816"></p>
<p><img src="/2022/03/01/build-small-cluster/1643723381690.png" alt="1643723381690"></p>
</li>
<li><p>使用ssh</p>
<ul>
<li><p>通过vscode的ssh进行两个节点的连接，从而实现远程终端控制+文件传输。如果只进行远程终端控制，通过如下指令即可完成。</p>
<pre class="line-numbers language-none"><code class="language-none">ssh scc@192.168.8.130
ssh scc@192.168.8.131<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
</li>
<li><p>对于node1和node2的相互连接，通过上面的指令即可完成。</p>
</li>
<li><p>免密登录需要先添加公钥，以node1免密登录node2为例：首先在终端输入ssh-keygen，然后ssh-copy-id <a href="mailto:scc@192.168.8.131">scc@192.168.8.131</a>，即可实现node1到node2的免密登录。对于node2，将第二步的ip地址换为node1的ip地址即可。而对于物理机，第二步需要将指令分别应用于node1和node2的ip地址。</p>
<p><img src="/2022/03/01/build-small-cluster/1643723497117.png" alt="1643723497117"></p>
<p>对于windows系统，可能会提示ssh-copy-id这一函数未定义，在终端执行如下指令。</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token keyword">function</span> ssh-copy-id<span class="token punctuation">(</span><span class="token punctuation">[</span>string<span class="token punctuation">]</span><span class="token variable">$userAtMachine</span>, <span class="token variable">$args</span><span class="token punctuation">)</span><span class="token punctuation">{</span>   
    <span class="token variable">$publicKey</span> <span class="token operator">=</span> <span class="token string">"<span class="token variable">$ENV</span>:USERPROFILE"</span> + <span class="token string">"/.ssh/id_rsa.pub"</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token punctuation">(</span>Test-Path <span class="token string">"<span class="token variable">$publicKey</span>"</span><span class="token punctuation">))</span><span class="token punctuation">{</span>
        Write-Error <span class="token string">"ERROR: failed to open ID file '<span class="token variable">$publicKey</span>': No such file"</span>            
    <span class="token punctuation">}</span>
    <span class="token keyword">else</span> <span class="token punctuation">{</span>
        <span class="token operator">&amp;</span> <span class="token function">cat</span> <span class="token string">"<span class="token variable">$publicKey</span>"</span> <span class="token operator">|</span> <span class="token function">ssh</span> <span class="token variable">$args</span> <span class="token variable">$userAtMachine</span> <span class="token string">"umask 077; test -d .ssh || mkdir .ssh ; cat &gt;&gt; .ssh/authorized_keys || exit 1"</span>      
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
</ul>
</li>
<li><p>配置文件 <code>/etc/hosts</code></p>
<ul>
<li><p>使用管理员权限在/etc/hosts这一文件尾加上下面的语句</p>
<pre class="line-numbers language-none"><code class="language-none">192.168.8.131 node2
192.168.8.130 node1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
</li>
<li><p>实现效果如下：</p>
<p><img src="/2022/03/01/build-small-cluster/1643723934446.png" alt="1643723934446"></p>
<p>试试ping指令：</p>
<p><img src="/2022/03/01/build-small-cluster/1643723988175.png" alt="1643723988175"></p>
</li>
</ul>
</li>
</ul>
<h2 id="Task-3-在-node1-上增加一个大容量存储设备"><a href="#Task-3-在-node1-上增加一个大容量存储设备" class="headerlink" title="Task 3: 在 node1 上增加一个大容量存储设备"></a>Task 3: 在 <code>node1</code> 上增加一个大容量存储设备</h2><p>目前 <code>node1</code> 和 <code>node2</code> 的系统盘都挺小的，在这个任务中，你需要在 <code>node1</code> 上增加一个比较大的盘（80GB），后面我们会把这一块盘通过 NFS 分享给 <code>node2</code> 使用。</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node1</code> 上增加一块盘<ul>
<li>在虚拟机管理软件上操作一下就可以了。</li>
<li>在 <code>node1</code> 上，可以使用命令 <code>lsblk</code> / <code>fdisk -l</code> 去检查有没有新的盘加进来。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 在新的盘上进行分区<ul>
<li>学习命令 <code>fdisk</code></li>
<li>一般情况下，只需要把整个盘都划为一个分区。</li>
<li>同样可以使用命令 <code>lsblk</code> / <code>fdisk -l</code> 去检查由于分区而增加的设备。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 把刚刚分好的区进行格式化，格式化成 ext4 文件系统。<ul>
<li>学习命令 <code>mkfs.ext4</code></li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 把格式化好的分区挂载到 <code>node1</code> 上的 <code>/data/</code> 路径下 。<ul>
<li>学习命令 <code>mount</code> / <code>umount</code></li>
<li>使用命令 <code>df -h</code> 检查挂载结果</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 使用非特权用户（如 <code>scc</code>）访问 <code>/data</code> 路径<ul>
<li>学习使用命令 <code>chown</code> / <code>chmod</code></li>
<li>路径 <code>/data</code> 需要能够被普通用户（如 <code>scc</code> 用户）可读可写。</li>
<li>学习 Linux 中的权限模型</li>
<li>在后面的任务中，你要尽量避免直接使用 root 用户 和 <code>sudo</code> 命令。<ul>
<li>除了一些必须使用root用户的操作</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>在后面的任务中，我们将只在 <code>/data</code> 下操作、存储数据。</p>
<h3 id="提交-2"><a href="#提交-2" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>扩容</p>
<ul>
<li>首先将虚拟机关闭，进行node1的虚拟机设置，将其最大磁盘大小增加80GB。</li>
</ul>
<img src="1643724023899.png" alt="1643724023899" style="zoom: 80%;">

<ul>
<li>通过命令查看增加的磁盘空间。</li>
</ul>
<img src="1643724046875.png" alt="1643724046875" style="zoom:80%;">
</li>
<li><p>通过fdisk进行新增的80GB的磁盘的设置。</p>
<img src="1643724089977.png" alt="1643724089977" style="zoom:80%;">
</li>
<li><p>对磁盘进行挂载并进行格式化，把格式化好的分区挂载到 <code>node1</code> 上的 <code>/data/</code> 路径下，指令如下。</p>
<pre class="line-numbers language-none"><code class="language-none">mkfs.ext4 /dev/sda3
cd /
mkdir /data
mount /dev/sda3 /data<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<ul>
<li><p>挂载后的效果如下：</p>
<p><img src="/2022/03/01/build-small-cluster/1643724163008.png" alt="1643724163008"></p>
</li>
</ul>
</li>
<li><p>对所有用户赋予对/data读写执行的权限，并将修改/data的权限交给scc：</p>
<pre class="line-numbers language-none"><code class="language-none">chmod a+rwx /data
chown -R scc:scc /data<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<ul>
<li><p>通过ls -l进行验证，可以看到任意用户都有了对/data进行rwx的权限，同时scc成为了这一文件夹的用户。</p>
<p><img src="/2022/03/01/build-small-cluster/1643724602960.png" alt="1643724602960"></p>
</li>
</ul>
</li>
</ul>
<h2 id="Task-4-学习-Linux-中的软件环境"><a href="#Task-4-学习-Linux-中的软件环境" class="headerlink" title="Task 4: 学习 Linux 中的软件环境"></a>Task 4: 学习 Linux 中的软件环境</h2><p>我们将尝试在没有root和sudo的情况下配置、安装和运行一些软件。本任务将尝试让您详细了解Linux中某些环境变量的影响。在下一个任务中，我们将使用Environment Module来管理环境。</p>
<p>在这个任务中，你需要在没有root的情况下安装最新版本的CMake。</p>
<ul>
<li><p><input checked="" disabled="" type="checkbox"> 
安装最新版本的 CMake</p>
<ul>
<li><p><input checked="" disabled="" type="checkbox"> 
下载CMake的源码到 <code>/data/software/source/</code> 并且解压</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 阅读 <code>README</code> 去看看怎么从源码编译安装</li>
</ul>
</li>
<li><p><input checked="" disabled="" type="checkbox"> 
编译源代码并安装</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 你可以使用系统包管理器安装一些缺失的cmake依赖</li>
<li><input checked="" disabled="" type="checkbox"> 您必须将CMake安装到 <code>/data/software/install/cmake/${version}</code>，<code>${version}</code> 是 cmake 的版本号</li>
<li><input checked="" disabled="" type="checkbox"> 编译是一个计算密集型的过程，另外它可能需要较大的内存。因此，你可以临时扩展虚拟机的CPU /内存。</li>
</ul>
</li>
<li><p><input checked="" disabled="" type="checkbox"> 
看看命令 <code>cmake --version</code> 的结果来检查cmake的版本</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 如何不需要绝对路径，直接只用 <code>cmake</code> 命令？</li>
<li><input checked="" disabled="" type="checkbox"> 看看命令 <code>which cmake</code> 的结果</li>
<li><input checked="" disabled="" type="checkbox"> TIPS：看看环境变量 <code>PATH</code></li>
</ul>
</li>
</ul>
</li>
<li><p><input checked="" disabled="" type="checkbox"> 
了解其他环境变量</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 仔细阅读该部分学习材料：<a href="./linux-software-environment.md">link</a></li>
<li><input checked="" disabled="" type="checkbox"> 这些环境变量，在后面配置 modulefile 的时候非常重要</li>
<li><input checked="" disabled="" type="checkbox"> 回答问题：类似于 <code>PATH</code> 的环境变量，把自己的路径加载最前面和最后面，有什么区别吗？为什么？<ul>
<li><code>export PATH=/path/to/..:${PATH}</code> 和 <code>export PATH=${PATH}:/path/to/...</code></li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> （optional）还有没有什么重要的环境变量？可以介绍一下<ul>
<li>例子：pkg-config 是什么？有什么作用？有命令行参数或环境变量去控制搜索路径吗？</li>
<li>例子：cmake 如何 find package ? 有命令行参数或环境变量去控制搜索路径吗？</li>
<li>例子：python 中的 import package，是从什么路径寻找的？如果自己手动操作安装到非默认路径，如何让 python 找到你的package？（tips： <code>PYTHONHOME</code> 和 <code>PYTHONPATH</code> 环境变量）</li>
<li>…</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="提交-3"><a href="#提交-3" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>通过wget下载CMake <a href="https://cmake.org/download/">Download | CMake</a> 的源码到 <code>/data/software/source/</code> 并且解压</p>
<pre class="line-numbers language-none"><code class="language-none">tar -zxvf cmake-3.22.2.tar.gz<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
</li>
<li><p>编译源码并安装</p>
<pre class="line-numbers language-none"><code class="language-none">./bootstrap --prefix=/data/software/install/cmake/3.22.2
make -j2
make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>查看cmake版本</p>
<ul>
<li>首先用绝对路径查看</li>
</ul>
<p><img src="/2022/03/01/build-small-cluster/1643725778268.png" alt="1643725778268"></p>
<ul>
<li><p>通过源码安装Environment Modules软件包，后面也用得着。</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://jaist.dl.sourceforge.net/project/modules/Modules/modules-5.0.1/modules-5.0.1.tar.gz --no-check-certificate
tar -zxvf modules-5.0.1.tar.gz 
cd modules-5.0.1
./configure --prefix=/data/software/install/modules/5.0.1
make &amp;&amp; make install
source /data/software/install/modules/5.0.1/profile.sh<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>然后我们进行modulefile的配置，将其添加到环境变量中。</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed cmake"
}
 
module-whatis   "Loads the environment for my installed cmake"
 
set     CMAKE_HOME    /data/software/install/cmake/3.22.2
setenv     CMAKE_HOME    /data/software/install/cmake/3.22.2
prepend-path    PATH            $CMAKE_HOME/bin
prepend-path    LIBRARY_PATH    $CMAKE_HOME/lib
prepend-path    LD_LIBRARY_PATH $CMAKE_HOME/lib
prepend-path    LD_INCLUDE_PATH $CMAKE_HOME/include
prepend-path    MANPATH         $CMAKE_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>然后我们module load cmake/3.22.2，就可以直接查看cmake版本了。</p>
<p><img src="/2022/03/01/build-small-cluster/1643725976982.png" alt="1643725976982"></p>
</li>
</ul>
</li>
<li><p>加在前面的话会优先使用这一环境变量，先在这一环境变量中找到后就不会去找后面的了。区别就是优先级的不同。</p>
</li>
</ul>
<h2 id="Task-5-安装HPC集群必备软件"><a href="#Task-5-安装HPC集群必备软件" class="headerlink" title="Task 5: 安装HPC集群必备软件"></a>Task 5: 安装HPC集群必备软件</h2><p>我们将会配置、安装一些HPC集群的必备软件（尽量避免使用root权限）。在本部分任务中，我们需要自己编写脚本管理软件环境，或使用 Environment Module 来管理我们的软件，并初步学习HPC集群常见的软件环境管理方法。</p>
<p>Environment Module 是控制 Shell 当前会话环境变量的应用程序。例如，使用 Environment Module 加载最新的 gcc，gcc 的路径会加到 <code>PATH</code> 环境变量最前面了，由此，旧的 gcc 命令就能被新的所覆盖。在其他情况下，某些运行时所需的库需要将其路径加到 <code>LD_LIBRARY_PATH</code> 中，这些使用 Environment Module 都能轻松处理。</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 首先安装Environment Module 模块！<ul>
<li><input checked="" disabled="" type="checkbox"> Environment Module 有好几个以不同语言实现的变体。我们推荐安装系统包管理器提供的版本（通常用TCL编写）。</li>
</ul>
</li>
</ul>
<p><strong>你的目标</strong>: 创建一些 modulefiles，让非特权用户可以加载常用的编译器、第三方库、应用等。为了让集群存储环境更为规范，我们要求所有的软件都需要安装在 <code>/data/software/install</code> 下，并且遵循一定的规则（如gcc 7.5.0 安装在 <code>/data/software/install/gcc/7.5.0/</code>下) ，不同版本的软件需要有独立的目录。</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> CMake 3.x</li>
<li><input checked="" disabled="" type="checkbox"> GNU Compiler Collection 7.x + 10.x<ul>
<li>使用包管理器默认提供的编译器(通常也是gcc)从源代码编译gcc。不要忘记启用对C、c++和Fortran的语言支持。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> Intel Parallel Studio XE 2018 +  Intel oneAPI toolkit (Intel oneAPI Base Toolkit + Intel oneAPI HPC Toolkit)<ul>
<li>Intel Parallel Studio 2018.4 <a href="http://registrationcenter-download.intel.com/akdlm/irc_nas/tec/13717/parallel_studio_xe_2018_update4_cluster_edition.tgz">download link</a></li>
<li>Ask us for the license</li>
<li>Intel oneAPI Toolkit <a href="https://software.intel.com/content/www/us/en/develop/tools/oneapi/all-toolkits.html#analytics-kit">download link</a></li>
<li>TIPS: use <a href="https://software.intel.com/content/www/us/en/develop/articles/using-environment-modules-with-the-intel-development-tools.html">env2</a> to convert the environment script of Intel PSXE.</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> OpenMPI 1.x + 3.x</li>
<li><input checked="" disabled="" type="checkbox"> CUDA Toolkit 11.x (不需要安装驱动)</li>
</ul>
<p>你可以使用类似下面的命令检查环境，这里是一个 <code>gcc</code> 的例子，你应该用类似的命令检查所有你安装的软件，同时最好附带部分例子表明你安装的软件能够正常使用，如编译一个<code>hello_world.c</code> 。</p>
<pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># use gcc provided by the system package manager</span>
<span class="token function">which</span> gcc
gcc --version
<span class="token comment"># use gcc 7.5.0</span>
module load gcc/7.5.0
<span class="token function">which</span> gcc
gcc --version
<span class="token comment"># compile a hello_world.c and run to check the gcc compiler</span>
module unload gcc/7.5.0
<span class="token comment"># use gcc 10.3.0</span>
module load gcc/10.3.0
<span class="token function">which</span> gcc
gcc --version
<span class="token comment"># compile a hello_world.c and run to check the gcc compiler</span>
module unload gcc/10.3.0<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>


<p>其他材料：</p>
<ol>
<li><a href="https://modules.readthedocs.io/en/latest/cookbook/compiler-etc-dependencies.html">Environment Module</a><ol>
<li>提供一个参考链接： <a href="https://blog.csdn.net/kongxx/article/details/52253593">link</a></li>
<li><a href="https://software.intel.com/content/www/us/en/develop/articles/using-environment-modules-with-the-intel-development-tools.html">env2</a> 也提供一个参考链接： <a href="https://www.intel.com/content/www/us/en/developer/articles/technical/using-environment-modules-with-the-intel-development-tools.html">link</a></li>
</ol>
</li>
<li><a href="https://github.com/spack/spack">Spack</a> 是下一代的 HPC 集群软件管理工具，有空可以了解一下！</li>
</ol>
<h3 id="提交-4"><a href="#提交-4" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>Environment Module以及cmake的安装在task4中介绍过了，这里就不重复阐述了。</p>
</li>
<li><p>GNU Compiler Collection</p>
<ul>
<li><p>首先进行编译依赖的安装</p>
<pre class="line-numbers language-none"><code class="language-none">mkdir /data/software/install/gmp
mkdir /data/software/install/gmp/6.1.0
tar jxvf gmp-6.1.0.tar.bz2
cd gmp-6.1.0
./configure --prefix=/data/software/install/gmp/6.1.0
make -j4 &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">mkdir /data/software/install/mpfr
mkdir /data/software/install/mpfr/3.1.4
tar jxvf tar jxvf mpfr-3.1.4.tar.bz2
cd mpfr-3.1.4
./configure --prefix=/data/software/install/mpfr/3.1.4
make -j4 &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">mkdir /data/software/install/mpc
mkdir /data/software/install/mpc/1.0.3
tar zxvf mpc-1.0.3.tar.gz
cd mpc-1.0.3
./configure --prefix=/data/software/install/mpc/1.0.3
make -j4 &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">mkdir /data/software/install/isl
mkdir /data/software/install/isl/0.16.1
tar jxvf isl-0.16.1.tar.bz2
cd isl-0.16.1
./configure --prefix=/data/software/install/isl/0.16.1
make -j4 &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>然后加入环境变量</p>
<pre class="line-numbers language-none"><code class="language-none">export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/data/software/install/isl/0.16.1/lib:/data/software/install/mpc/1.0.3/lib:/data/software/install/mpfr/3.1.4/lib:/data/software/install/gmp/6.1.0/lib<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/zlib-1.2.11/lib:/data/software/install/isl/0.16.1/lib:/data/software/install/mpc/1.0.3/lib:/data/software/install/mpfr/3.1.4/lib:/data/software/install/gmp/6.1.0/lib<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
</li>
<li><p>7.3.0</p>
<pre class="line-numbers language-none"><code class="language-none">wget http://mirrors.aliyun.com/gnu/gcc/gcc-7.3.0/gcc-7.3.0.tar.gz
mkdir /data/software/install/gcc/7.3.0
tar zxvf gcc-7.3.0.tar.gz
cd gcc-7.3.0
./configure --prefix=/data/software/install/gcc/7.3.0 --enable-multilib --enable-languages=c,c++,fortran --enable-threads=posix --enable--long-long
make&amp;make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>进行配置</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed gcc"
}
 
module-whatis   "Loads the environment for my installed gcc"
 
set     GCC_HOME    /data/software/install/gcc/7.3.0
setenv     GCC_HOME    /data/software/install/gcc/7.3.0
prepend-path    PATH            $GCC_HOME/bin
prepend-path    LIBRARY_PATH    $GCC_HOME/lib
prepend-path    LD_LIBRARY_PATH $GCC_HOME/lib
prepend-path    LD_INCLUDE_PATH $GCC_HOME/include
prepend-path    MANPATH         $GCC_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>进行测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643745046770.png" alt="1643745046770"></p>
</li>
<li><p>10.3.0</p>
<pre class="line-numbers language-none"><code class="language-none">wget http://mirrors.aliyun.com/gnu/gcc/gcc-10.3.0/gcc-10.3.0.tar.gz
tar zxvf gcc-10.3.0.tar.gz 
cd gcc-10.3.0
./configure --prefix=/data/software/install/gcc/10.3.0 --enable-multilib --enable-languages=c,c++,fortran --enable-threads=posix --enable--long-long
make &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>然后进行配置</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed gcc"
}
 
module-whatis   "Loads the environment for my installed gcc"
 
set     GCC_HOME    /data/software/install/gcc/10.3.0
setenv     GCC_HOME    /data/software/install/gcc/10.3.0
prepend-path    PATH            $GCC_HOME/bin
prepend-path    LIBRARY_PATH    $GCC_HOME/lib
prepend-path    LD_LIBRARY_PATH $GCC_HOME/lib
prepend-path    LD_INCLUDE_PATH $GCC_HOME/include
prepend-path    MANPATH         $GCC_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643740977441.png" alt="1643740977441"></p>
</li>
</ul>
</li>
<li><p>Intel</p>
<ul>
<li><p>Intel Parallel Studio 2018.4(获取license)</p>
<pre class="line-numbers language-none"><code class="language-none">tar zxvf parallel_studio_xe_2018_update4_cluster_edition.tgz
cd parallel_studio_xe_2018_update4_cluster_edition
bash install.sh<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">source compilervars.sh -arch intel64 -platform linux<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>配置</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module4.6
proc ModulesHelp { } {
global version prefix

puts stderr "\t Loads the environment for my installed parallel studio"
}

module-whatis   "Loads the environment for my installed parallel studio"
 
set     STUDIO_HOME    /data/software/install/parallel_studio_xe/2018.4
setenv     STUDIO_HOME    /data/software/install/parallel_studio_xe/2018.4
source-sh source /data/software/install/parallel_studio_xe/2018.4/bin/compilervars.sh -arch intel64 -platform linux<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>验证</p>
<p><img src="/2022/03/01/build-small-cluster/1643775945940.png" alt="1643775945940"></p>
</li>
<li><p>Intel oneAPI Base Toolkit</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://registrationcenter-download.intel.com/akdlm/irc_nas/18487/l_BaseKit_p_2022.1.2.146_offline.sh

bash ./l_BaseKit_p_2022.1.2.146_offline.sh -a --silent --install-dir /data/software/install/oneapi/base --eula accept<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>配置</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module4.6
proc ModulesHelp { } {
global version prefix

puts stderr "\t Loads the environment for my installed oneapi"
}

module-whatis   "Loads the environment for my installed oneapi"
 
set     ONEAPI_HOME    /data/software/install/oneapi/base
setenv     ONEAPI_HOME    /data/software/install/oneapi/base
source-sh bash /data/software/install/oneapi/base/setvars.sh --force<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>Intel oneAPI HPC Toolkit</p>
<pre class="line-numbers language-none"><code class="language-none">bash install.sh --silent --eula accept<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li>
</ul>
</li>
<li><p>对于openmpi，我装了1.6.5和3.1.0两个版本。</p>
<ul>
<li><p>安装openmpi-3.1.0</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://download.open-mpi.org/release/open-mpi/v3.1/openmpi-3.1.0.tar.gz
tar zxvf openmpi-3.1.0.tar.gz 
cd openmpi-3.1.0
./configure --prefix=/data/software/install/openmpi/3.1.0/ 
mkdir /data/software/install/openmpi/3.1.0
make &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>配置modulefile文件3.1.0</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix

puts stderr "\t Loads the environment for my installed openmpi"
}
 
module-whatis   "Loads the environment for my installed openmpi"
 
set     MPI_HOME    /data/software/install/openmpi/3.1.0
setenv     MPI_HOME    /data/software/install/openmpi/3.1.0
prepend-path    PATH            $MPI_HOME/bin
prepend-path    LIBRARY_PATH    $MPI_HOME/lib
prepend-path    LD_LIBRARY_PATH $MPI_HOME/lib
prepend-path    LD_INCLUDE_PATH $MPI_HOME/include
prepend-path    MANPATH         $MPI_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>安装openmpi-1.6.5</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://download.open-mpi.org/release/open-mpi/v1.6/openmpi-1.6.5.tar.gz
tar zxvf openmpi-1.6.5.tar.gz 
cd openmpi-1.6.5
mkdir /data/software/install/openmpi/1.6.5
./configure --prefix=/data/software/install/openmpi/1.6.5
make &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>配置modulefile文件1.6.5</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed openmpi"
}
 
module-whatis   "Loads the environment for my installed openmpi"
 
set     MPI_HOME    /data/software/install/openmpi/1.6.5
setenv     MPI_HOME    /data/software/install/openmpi/1.6.5
prepend-path    PATH            $MPI_HOME/bin
prepend-path    LIBRARY_PATH    $MPI_HOME/lib
prepend-path    LD_LIBRARY_PATH $MPI_HOME/lib
prepend-path    LD_INCLUDE_PATH $MPI_HOME/include
prepend-path    MANPATH         $MPI_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
</ul>
</li>
<li><p>CUDA Toolkit 11.0</p>
<pre class="line-numbers language-none"><code class="language-none">wget http://developer.download.nvidia.com/compute/cuda/11.0.2/local_installers/cuda_11.0.2_450.51.05_linux.run
sh cuda_11.0.2_450.51.05_linux.run<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>要进行路径的修改，装在/data/software/install/cuda/11.0，由于不需要安装驱动，可以取消掉部分选项。安装完成后的界面如下</p>
<p><img src="/2022/03/01/build-small-cluster/1644030953565.png" alt="1644030953565"></p>
<p>配置</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix

puts stderr "\t Loads the environment for my installed cuda"
}
 
module-whatis   "Loads the environment for my installed cuda"
 
set     CUDA_HOME    /data/software/install/cuda/11.0
setenv     CUDA_HOME    /data/software/install/cuda/11.0
prepend-path    PATH            $CUDA_HOME/bin
prepend-path    LIBRARY_PATH    $CUDA_HOME/lib64
prepend-path    LD_LIBRARY_PATH $CUDA_HOME/lib64
prepend-path    LD_INCLUDE_PATH $CUDA_HOME/include
prepend-path    MANPATH         $CUDA_HOME/doc/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>验证安装是否成功</p>
<p><img src="/2022/03/01/build-small-cluster/1644031197907.png" alt="1644031197907"></p>
</li>
</ul>
<h2 id="Task-6-Network-File-System"><a href="#Task-6-Network-File-System" class="headerlink" title="Task 6: Network File System"></a>Task 6: Network File System</h2><p>现在所有软件都安装在 <code>node1</code> 的 <code>/data/software/install</code> 目录下，我们现在将通过 Network File System 将目录 <code>/data</code> 分享到 <code>node2</code> 上，这样 <code>node2</code> 就不用重复安装这些软件。（类似的做法可以扩展到更多规模的集群）。</p>
<p>你的任务：</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node1</code> 上将目录 <code>/data</code> 导出成 Network File System。<ul>
<li>你可以在 <code>node2</code> 上使用命令 <code>showmount -e node1</code> 检查。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node2</code> 上把<code>node1</code> 的 <code>/data</code> 挂载到本地的 <code>/data</code> 目录</li>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node2</code> 上检查软件环境是否正常。<ul>
<li>在 <code>node2</code> 上使用脚本导入 <code>/data</code> 中的软件，并且做一些事情使用这些软件确保正常。</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> TODO: 对于很大很大规模的集群，NFS 还会好使吗？<ul>
<li>如果不，可以推荐一些其他的分布式文件系统，并且简单介绍一下其优劣。</li>
<li>可以自行了解现在超算的存储架构和性能 <a href="https://io500.org/">io500</a></li>
</ul>
</li>
</ul>
<h3 id="提交-5"><a href="#提交-5" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>在node1上，修改/etc/exports中的内容如下</p>
<pre class="line-numbers language-none"><code class="language-none">/data 192.168.8.131(insecure,rw,async,no_root_squash)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>使设置生效</p>
<pre class="line-numbers language-none"><code class="language-none">exportfs -rv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>在两个节点上，都将nfs设置为开机自启动</p>
<pre class="line-numbers language-none"><code class="language-none">systemctl start nfs
systemctl start rpcbind
systemctl enable nfs
systemctl enable rpcbind
service nfs start
service rpcbind start<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>然后在node2上查看</p>
<pre class="line-numbers language-none"><code class="language-none">showmount -e node1<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>可以看到nfs共享目录：</p>
<p><img src="/2022/03/01/build-small-cluster/1643729335975.png" alt="1643729335975"></p>
</li>
<li><p>在node2上进行网络挂载并通过df -h进行验证，这一步需要root权限。</p>
<pre class="line-numbers language-none"><code class="language-none">sudo mount -t nfs -o nolock node1:/data /data<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>可以看到挂载成功，目录中的文件可用。</p>
<p><img src="/2022/03/01/build-small-cluster/1643729382993.png" alt="1643729382993"></p>
</li>
<li><p>尝试使用node1配置好的modules，在~/.bashrc文件尾加入</p>
<pre class="line-numbers language-none"><code class="language-none">export MODULEPATH=/data/software/modules<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>然后source ~/.bashrc，尝试使用openmpi，可以看到此时node2已经可以使用node1安装好的软件了。</p>
<p><img src="/2022/03/01/build-small-cluster/1643729421062.png" alt="1643729421062"></p>
</li>
</ul>
<h2 id="Task-7-【测试网络性能】在集群上跑-OSU-benchmark"><a href="#Task-7-【测试网络性能】在集群上跑-OSU-benchmark" class="headerlink" title="Task 7: 【测试网络性能】在集群上跑 OSU benchmark"></a>Task 7: 【测试网络性能】在集群上跑 OSU benchmark</h2><p>在这部分，你需要运行 <a href="https://mvapich.cse.ohio-state.edu/benchmarks/">osu-micro-benchmark</a> 去测量<code>node1</code> 和 <code>node2</code> 之间的网络带宽和延迟。</p>
<p>这部分任务只是让你熟悉MPI的使用和测试，不需调参获得更好的结果。</p>
<p>你的任务：</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 下载源代码 <a href="https://mvapich.cse.ohio-state.edu/download/mvapich/osu-micro-benchmarks-5.7.1.tgz">osu-micro-benchmark</a> 并安装 </li>
<li><input checked="" disabled="" type="checkbox"> 使用 Open MPI 在两个进程上测试 <code>ose_bw</code> and <code>osu_latency</code><ul>
<li>确保一个进程在 <code>node1</code> 另一个进程在 <code>node2</code>.，可以通过 <code>mpirun ..... hostname</code> 来测试</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 使用 Intel MPI (包含在 Intel Parallel Studio 中) 在两个进程上测试 <code>ose_bw</code> and <code>osu_latency</code><ul>
<li>确保一个进程在 <code>node1</code> 另一个进程在 <code>node2</code>.</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 回答问题：现在超算一般用的是什么网络？在不同的网络上跑这些benchmark，你猜测结果分别是多少呢？</li>
<li><input checked="" disabled="" type="checkbox"> （可选）思考：如果软件环境和osu benchmark没有装在 <code>/data</code> 下，而是在node1的系统盘里（即这些内容只能在 node1 上访问，node2 上不能直接访问），执行这些多节点的程序可能会遇到什么问题？（可以自己试试看）</li>
</ul>
<h3 id="提交-6"><a href="#提交-6" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>下载</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://mvapich.cse.ohio-state.edu/download/mvapich/osu-micro-benchmarks-5.7.1.tgz
tar zxvf osu-micro-benchmarks-5.7.1.tgz<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
</li>
<li><p>测一下open mpi，测试指令</p>
<pre class="line-numbers language-none"><code class="language-none">cd /data/software/source/osu-micro-benchmarks-5.7.1
./configure CC=/data/software/install/openmpi/3.1.0/bin/mpicc CXX=/data/software/install/openmpi/3.1.0/bin/mpicxx --prefix=/data/software/install/osu/5.7.1
make &amp;&amp; make install

cd /data/software/source/osu-micro-benchmarks-5.7.1/mpi/pt2pt
$(which mpirun) -np 2 -machinefile /data/hosts hostname
$(which mpirun) -np 2 -machinefile /data/hosts ./osu_bw
$(which mpirun) -np 2 -machinefile /data/hosts ./osu_latency<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<ul>
<li><p>机器名测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991305079.png" alt="1643991305079"></p>
</li>
<li><p>MPI带宽测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991315434.png" alt="1643991315434"></p>
</li>
<li><p>延迟测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991525835.png" alt="1643991525835"></p>
</li>
</ul>
</li>
<li><p>测一下intel mpi，测试指令</p>
<pre class="line-numbers language-none"><code class="language-none">cd /data/software/source/osu-micro-benchmarks-5.7.1
./configure CC=/data/software/install/oneapi/base/mpi/2021.5.1/bin/mpicc CXX=/data/software/install/oneapi/base/mpi/2021.5.1/bin/mpicxx --prefix=/data/software/install/osu/5.7.1
make uninstall
make &amp;&amp; make install

cd /data/software/source/osu-micro-benchmarks-5.7.1/mpi/pt2pt
$(which mpirun) -ppn 1 --hosts node1,node2 hostname
$(which mpirun) -ppn 1 --hosts node1,node2 ./osu_bw
$(which mpirun) -ppn 1 --hosts node1,node2 ./osu_latency<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<ul>
<li><p>机器名测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991982658.png" alt="1643991982658"></p>
</li>
<li><p>MPI带宽测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991719234.png" alt="1643991719234"></p>
</li>
<li><p>延迟测试</p>
<p><img src="/2022/03/01/build-small-cluster/1643991959385.png" alt="1643991959385"></p>
</li>
</ul>
</li>
<li><p>一般使用的是 InfiniBand 网络。 主流网络互连技术包括以太网、FC和InfiniBand。FC是为实现存储互联而设计的存储专属网络，由于其技术局限性，迄今只在存储互联领域得到广泛应用。以太网与InfiniBand同为开放网络互连技术，以太网更侧重网络协议的通用性，而InfiniBand拥有大幅领先于以太网和FC性能的优势，同时是具备软件定义网络(soft defined network,SDN)属性的智能网络。 因此我猜测在InfiniBand上跑benchmark带宽是最大的，延迟是最小的，而另外两种网络由于其局限性，性能表现会差一些。</p>
</li>
<li><p>会发生如下错误</p>
<pre class="line-numbers language-none"><code class="language-none">bash: orted: command not found
--------------------------------------------------------------------------
A daemon (pid 62171) died unexpectedly with status 127 while attempting
to launch so we are aborting.

There may be more information reported by the environment (see above).

This may be because the daemon was unable to find all the needed shared
libraries on the remote node. You may set your LD_LIBRARY_PATH to have the
location of the shared libraries on the remote nodes and this will
automatically be forwarded to the remote nodes.
--------------------------------------------------------------------------
--------------------------------------------------------------------------
mpirun noticed that the job aborted, but has no info as to the process
that caused that situation.
--------------------------------------------------------------------------<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这是由于远程主机node2无法访问软件环境和osu benchmark导致的。</p>
</li>
</ul>
<h2 id="Task-8-【测试磁盘性能】在集群上跑磁盘性能测试"><a href="#Task-8-【测试磁盘性能】在集群上跑磁盘性能测试" class="headerlink" title="Task 8: 【测试磁盘性能】在集群上跑磁盘性能测试"></a>Task 8: 【测试磁盘性能】在集群上跑磁盘性能测试</h2><p>在这部分，你需要运行 <a href="https://github.com/axboe/fio">fio</a> 对集群的存储设备的性能进行测试。</p>
<ol>
<li>三个需要注意的选项：<ol>
<li><code>--ioengine=libaio</code>，选取该异步IO库可以尽可能压榨盘的性能。</li>
<li><code>--direct=1</code>，该选项可以跳过操作系统的cache，以测得真实性能。</li>
<li><code>--rw=XXXX</code>，这里选择read/write/randread/randwrite，分别对应顺序读、顺序写、随机读、随机写。</li>
</ol>
</li>
<li>测试项一：测试写入、读取最大带宽（顺序，随机）<ol>
<li>一般使用较大的块大小（如1MB）和较大的队列深度进行多线程测试。</li>
</ol>
</li>
<li>测试项二：测试4K写入、读取最大IOPS （顺序，随机）<ol>
<li>一般使用较大的队列深度进行多线程测试。</li>
</ol>
</li>
<li>测试项三：测试4K写入、读取延时 （顺序，随机）<ol>
<li>一般使用队列深度为1进行单线程测试。</li>
</ol>
</li>
</ol>
<p>你需要做的是：</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node1</code> 上测试 <code>/data/</code> 下的顺序/随机 写入、读取带宽，写入、读取 IOPS，以及写入读取的延时</li>
<li><input checked="" disabled="" type="checkbox"> 在 <code>node2</code> 上测试 <code>/data/</code> 下的顺序/随机 写入、读取带宽，写入、读取 IOPS，以及写入读取的延时</li>
<li><input checked="" disabled="" type="checkbox"> 尝试理解 fio 测试的日志含义，从而回答下面问题：<ul>
<li><input checked="" disabled="" type="checkbox"> 回答问题：在 <code>node2</code> 上测试的结果和 <code>node1</code> 上的结果有哪些区别，原因可能是什么？</li>
<li><input checked="" disabled="" type="checkbox"> 回答问题：现在比较常见的存储设备有哪几种？这几种存储设备的性能大致如何描述？</li>
</ul>
</li>
<li><input disabled="" type="checkbox"> （optional）写个脚本自动搞定这个任务？</li>
</ul>
<p>参考资料：<a href="https://cloud.google.com/compute/docs/disks/benchmarking-pd-performance">link</a></p>
<h3 id="提交-7"><a href="#提交-7" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>首先装一下fio</p>
<pre class="line-numbers language-none"><code class="language-none">./configure --prefix=/data/software/install/fio
make &amp;&amp; make install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
</li>
<li><p>对磁盘性能进行测试，首先创建文件夹</p>
<pre class="line-numbers language-none"><code class="language-none">TEST_DIR=/data/fiotest
mkdir -p $TEST_DIR<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<ul>
<li><p>使用 1 MB 的 I/O 块大小和至少 64 的 I/O 深度，通过执行具有多个（8 个或更多）并行数据流的顺序写入来测试写入吞吐量： </p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=write_throughput --directory=$TEST_DIR --numjobs=8 \
--size=10G --time_based --runtime=60s --ramp_time=2s --ioengine=libaio \
--direct=1 --verify=0 --bs=1M --iodepth=64 --rw=write \
--group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<p>随机写入吞吐量</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=write_throughput --directory=$TEST_DIR --numjobs=8 \
--size=10G --time_based --runtime=60s --ramp_time=2s --ioengine=libaio \
--direct=1 --verify=0 --bs=1M --iodepth=64 --rw=randwrite \
--group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>使用 4 KB 的 I/O 块大小和至少 64 的 I/O 深度，通过执行随机写入来测试写入 IOPS：</p>
</li>
</ul>
  <pre class="line-numbers language-none"><code class="language-none">fio --name=write_iops --directory=$TEST_DIR --size=10G \
--time_based --runtime=60s --ramp_time=2s --ioengine=libaio --direct=1 \
--verify=0 --bs=4K --iodepth=64 --rw=randwrite --group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>  顺序写入IOPS</p>
  <pre class="line-numbers language-none"><code class="language-none">fio --name=write_iops --directory=$TEST_DIR --size=10G \
--time_based --runtime=60s --ramp_time=2s --ioengine=libaio --direct=1 \
--verify=0 --bs=4K --iodepth=64 --rw=write --group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<ul>
<li><p>使用 1 MB 的 I/O 块大小和至少 64 的 I/O 深度，通过执行具有多个（8 个或更多）并行数据流的顺序读取来测试读取吞吐量： </p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_throughput --directory=$TEST_DIR --numjobs=8 \
--size=10G --time_based --runtime=60s --ramp_time=2s --ioengine=libaio \
--direct=1 --verify=0 --bs=1M --iodepth=64 --rw=read \
--group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<p>随机读取吞吐量</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_throughput --directory=$TEST_DIR --numjobs=8 \
--size=10G --time_based --runtime=60s --ramp_time=2s --ioengine=libaio \
--direct=1 --verify=0 --bs=1M --iodepth=64 --rw=randread \
--group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>使用 4 KB 的 I/O 块大小和至少 64 的 I/O 深度，通过执行随机读取来测试读取 IOPS： </p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_iops --directory=$TEST_DIR --size=10G \
--time_based --runtime=60s --ramp_time=2s --ioengine=libaio --direct=1 \
--verify=0 --bs=4K --iodepth=64 --rw=randread --group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>顺序读取IOPS：</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_iops --directory=$TEST_DIR --size=10G \
--time_based --runtime=60s --ramp_time=2s --ioengine=libaio --direct=1 \
--verify=0 --bs=4K --iodepth=64 --rw=read --group_reporting=1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>测试随机写入延迟时间。</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=write_latency_test \
  --directory=$TEST_DIR --filesize=10G \
  --time_based --ramp_time=2s --runtime=1m \
  --ioengine=libaio --direct=1 --verify=0 --randrepeat=0 \
  --bs=4K --iodepth=4 --rw=randwrite<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>顺序写入延迟</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=write_latency_test \
  --directory=$TEST_DIR --filesize=10G \
  --time_based --ramp_time=2s --runtime=1m \
  --ioengine=libaio --direct=1 --verify=0 --randrepeat=0 \
  --bs=4K --iodepth=4 --rw=write<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>测试读取延迟时间</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_latency_test \
  --directory=$TEST_DIR --filesize=10G \
  --time_based --ramp_time=2s --runtime=1m \
  --ioengine=libaio --direct=1 --verify=0 --randrepeat=0 \
  --bs=4K --iodepth=4 --rw=randread<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>顺序读取延迟</p>
<pre class="line-numbers language-none"><code class="language-none">fio --name=read_latency_test \
  --directory=$TEST_DIR --filesize=10G \
  --time_based --ramp_time=2s --runtime=1m \
  --ioengine=libaio --direct=1 --verify=0 --randrepeat=0 \
  --bs=4K --iodepth=4 --rw=read<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>清理： </p>
<pre class="line-numbers language-none"><code class="language-none">rm $TEST_DIR/write* $TEST_DIR/read*<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li>
</ul>
</li>
<li><p>node1结果</p>
<ul>
<li><p>顺序读取最大带宽为238661KiB/s，写入最大带宽为228462KiB/s。</p>
</li>
<li><p>随机读取最大带宽为94208KiB/s，写入最大带宽为99753KiB/s。</p>
</li>
<li><p>顺序读取最大IOPS：2985，顺序写入最大IOPS：3004。</p>
</li>
<li><p>随机读取最大IOPS：898，随机写入最大IOPS：902。</p>
</li>
<li><p>顺序读取延时：180.17，顺序写入延时：317.77。</p>
</li>
<li><p>随机读取延时：1938.17，随机写入延时：3629.74。</p>
</li>
</ul>
</li>
<li><p>node2结果</p>
<ul>
<li><p>顺序读取最大带宽为87888KiB/s，写入最大带宽为88064KiB/s。</p>
</li>
<li><p>随机读取最大带宽为79872KiB/s，写入最大带宽为81756KiB/s。</p>
</li>
<li><p>顺序读取最大IOPS：608，顺序写入最大IOPS：626。</p>
</li>
<li><p>随机读取最大IOPS：606，随机写入最大IOPS：624。</p>
</li>
<li><p>顺序读取延时：963.66，顺序写入延时：937.75。</p>
</li>
<li><p>随机读取延时：998.34，随机写入延时：1234.65。</p>
</li>
</ul>
</li>
<li><p>尝试理解 fio 测试的日志含义，从而回答下面问题：</p>
<ul>
<li><p>在 <code>node2</code> 上测试的结果和 <code>node1</code> 上的结果有哪些区别，原因可能是什么？</p>
<p>（1）node1上读写最大带宽要大于node2。</p>
<p>（2）node1上读写最大IOPS要大于node2</p>
<p>（3）node1上读写延时要小于node2。</p>
<p>可能原因是/data/是挂载在node1节点的磁盘上的，node2通过nfs进行磁盘的访问，存在一定的延时。另外虚拟机运行性能受到物理机运行状态的影响，在测试时具有不确定性。</p>
</li>
<li><p>现在比较常见的存储设备有哪几种？这几种存储设备的性能大致如何描述？</p>
<p>主要是两种，机械硬盘和固态硬盘。固态硬盘的优势在于读写速度都非常非常快，特别是零散的小文件的读取，性能要明显高于机械硬盘，而现在最新的PCIe 4.0 NVMe SSD性能已经达到了机械硬盘的几十倍，最明显的好处就是，应用启动快了，游戏加载快了，文件拷贝也快了。机械硬盘的优点在于价格便宜，数据可靠性更好一些，易于进行数据恢复。</p>
</li>
</ul>
</li>
</ul>
<h2 id="Task-9-【测试软件环境】在集群上编译一个真实应用"><a href="#Task-9-【测试软件环境】在集群上编译一个真实应用" class="headerlink" title="Task 9: 【测试软件环境】在集群上编译一个真实应用"></a>Task 9: 【测试软件环境】在集群上编译一个真实应用</h2><p>天气研究和预报模型（WRF，发音为”worf”）是一个主要用于数值天气预报（NWP）的高度并行，基于CPU的Fortran软件包。此应用程序是 ISC21 中的基准测试应用程序之一。(细节在<a href="https://hpcadvisorycouncil.atlassian.net/wiki/spaces/HPCWORKS/pages/1827438600/WRF+with+Single+Domain+-+Practice+case+for+ISC21+SCC">这里</a>)</p>
<p>在这个任务中，你只需要编译该应用即可。</p>
<p>这里有一些tips：</p>
<ol>
<li>认真阅读 <a href="https://hpcadvisorycouncil.atlassian.net/wiki/pages/viewpage.action?pageId=1827438600&amp;pageVersion=27">link</a> </li>
<li>安装依赖并且使用 Environment Module 进行管理。<ol>
<li>这些依赖包括 MPI, hdf5, netcdf (netcdf-c 和 netcdf-fortran)</li>
</ol>
</li>
<li>编译时你需要选择 “dm+sm” </li>
<li>编译时间可能较久，从十几分钟到一小时不等。</li>
<li>你可以选择 gcc 编译器，也可以选择 intel 编译器。</li>
</ol>
<p>你的目标是看到这样的log：</p>
<pre class="line-numbers language-none"><code class="language-none">==========================================================================
build started:   Fri Apr 9 18:44:39 EDT 2021
build completed: Fri Apr 9 19:53:47 EDT 2021
 
---&gt;                  Executables successfully built                  &lt;---
 
-rwxr-x--- 1 lcl_davidg iscscc-scinet 52523904 Apr  9 19:53 main/ndown.exe
-rwxr-x--- 1 lcl_davidg iscscc-scinet 52486664 Apr  9 19:53 main/real.exe
-rwxr-x--- 1 lcl_davidg iscscc-scinet 51479608 Apr  9 19:53 main/tc.exe
-rwxr-x--- 1 lcl_davidg iscscc-scinet 60223864 Apr  9 19:52 main/wrf.exe
 
==========================================================================<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<h3 id="提交-8"><a href="#提交-8" class="headerlink" title="提交"></a>提交</h3><p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>zlib</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed zlib"
}
 
module-whatis   "Loads the environment for my installed zlib"
 
set     ZLIB_HOME    /data/software/install/zlib/1.2.11
setenv     ZLIB_HOME    /data/software/install/zlib/1.2.11
prepend-path    PATH            $ZLIB_HOME/bin
prepend-path    LIBRARY_PATH    $ZLIB_HOME/lib
prepend-path    LD_LIBRARY_PATH $ZLIB_HOME/lib
prepend-path    LD_INCLUDE_PATH $ZLIB_HOME/include
prepend-path    MANPATH         $ZLIB_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>hdf55</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed hdf5"
}
 
module-whatis   "Loads the environment for my installed hdf5"
 
set     HDF5_HOME    /data/software/install/hdf5/1.8.22
setenv     HDF5_HOME    /data/software/install/hdf5/1.8.22
prepend-path    PATH            $HDF5_HOME/bin
prepend-path    LIBRARY_PATH    $HDF5_HOME/lib
prepend-path    LD_LIBRARY_PATH $HDF5_HOME/lib
prepend-path    LD_INCLUDE_PATH $HDF5_HOME/include
prepend-path    MANPATH         $HDF5_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>netcdf</p>
<pre class="line-numbers language-none"><code class="language-none">wget https://downloads.unidata.ucar.edu/netcdf-fortran/4.5.4/netcdf-fortran-4.5.4.tar.gz
wget https://downloads.unidata.ucar.edu/netcdf-c/4.8.1/src/netcdf-c-4.8.1.tar.gz<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>安装的时候注意两个安一个目录下面</p>
<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed netcdf-c"
}
 
module-whatis   "Loads the environment for my installed netcdf-c"
 
set     CDFC_HOME    /data/software/install/netcdf-c/4.8.1
setenv     CDFC_HOME    /data/software/install/netcdf-c/4.8.1
prepend-path    PATH            $CDFC_HOME/bin
prepend-path    LIBRARY_PATH    $CDFC_HOME/lib
prepend-path    LD_LIBRARY_PATH $CDFC_HOME/lib
prepend-path    LD_INCLUDE_PATH $CDFC_HOME/include
prepend-path    MANPATH         $CDFC_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-none"><code class="language-none">#%Module1.0
proc ModulesHelp { } {
global version prefix
 
puts stderr "\t Loads the environment for my installed netcdf-fortran"
}
 
module-whatis   "Loads the environment for my installed netcdf-fortran"
 
set     CDFF_HOME    /data/software/install/netcdf-fortran/4.5.4
setenv     CDFF_HOME    /data/software/install/netcdf-fortran/4.5.4
prepend-path    PATH            $CDFF_HOME/bin
prepend-path    LIBRARY_PATH    $CDFF_HOME/lib
prepend-path    LD_LIBRARY_PATH $CDFF_HOME/lib
prepend-path    LD_INCLUDE_PATH $CDFF_HOME/include
prepend-path    MANPATH         $CDFF_HOME/share/man<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>WRF_ISC21</p>
<pre class="line-numbers language-none"><code class="language-none">git clone https://github.com/wrf-model/WRF WRF_ISC21
cd WRF_ISC21
git checkout tags/ISC21 -b ISC21-branch
export NETCDF=$CDFC_HOME
export WRFIO_NCD_LARGE_FILE_SUPPORT=1
export NETCDF_classic=1
./configure
sed -i '/^CFLAGS_LOCAL/s/.*/CFLAGS_LOCAL    =       -w -O3 -ip -DRSL0_ONLY/' configure.wrf
./compile -j 6 em_real 2&gt;&amp;1 &gt; build_wrf.log
tail -15 build_wrf.log<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>根据题意，执行configure的时候选择67。</p>
<p>结果如下：</p>
<p><img src="/2022/03/01/build-small-cluster/1643889668649.png" alt="1643889668649"></p>
</li>
</ul>
<h2 id="Task-10-集群监控系统"><a href="#Task-10-集群监控系统" class="headerlink" title="Task 10: 集群监控系统"></a>Task 10: 集群监控系统</h2><p>在这个任务中，你需要搭建一个集群监控系统，你需要了解以下工具的使用：</p>
<ol>
<li><p><a href="https://github.com/prometheus/node_exporter">node_exporter</a></p>
</li>
<li><p><a href="https://github.com/prometheus/prometheus">prometheus</a></p>
</li>
<li><p><a href="https://github.com/grafana/grafana">grafana</a></p>
</li>
</ol>
<ul>
<li><input checked="" disabled="" type="checkbox"> 使用 <code>node_exporter</code> 导出 <code>node1</code> 和 <code>node2</code> 的监控指标</li>
<li><input checked="" disabled="" type="checkbox"> 使用 <code>prometheus</code> 记录 <code>node_exporter</code> 导出的指标</li>
<li><input checked="" disabled="" type="checkbox"> 使用  <code>Grafana</code> 导出存储在 <code>prometheus</code> 的指标<ul>
<li>你可以按你自己的想法安排 <code>Grafana</code> 面板，网上也有很多现成的面板可以学习</li>
</ul>
</li>
<li><input checked="" disabled="" type="checkbox"> 在集群上生成一些负载，并使用<code>Grafana</code> 上的面板展示这些负载<ul>
<li><code>stress</code> 可以帮助你生成一些特定的负载 (CPU/Memory/..).<h3 id="提交-9"><a href="#提交-9" class="headerlink" title="提交"></a>提交</h3></li>
</ul>
</li>
</ul>
<p>TODO: 你需要添加一些详细的信息，说明你如何完成上面的任务。</p>
<ul>
<li><p>node_exporter</p>
<pre class="line-numbers language-none"><code class="language-none">./node_exporter
curl http://localhost:9100/metrics &gt; node1.metrics
curl http://localhost:9100/metrics | grep "node_" &gt; pnode1.metrics<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>得到的输出</p>
<ul>
<li>HELP： 解释当前指标的含义</li>
<li>TYPE： 说明当前指标的数据类型</li>
</ul>
<p>监控指标含义</p>
<ul>
<li>node_boot_time： 系统启动时间</li>
<li>node_cpu： 系统CUP使用情况</li>
<li>node_disk_*： 磁盘io</li>
<li>node_filesystem*： 文件系统使用量</li>
<li>node_load1： 系统负载</li>
<li>node_memeory_*： 系统内存使用量</li>
<li>node_network_*： 网络宽带</li>
<li>node_time： 当前系统时间</li>
<li>go_*： node exporter中go相关指标</li>
<li>*process_ ：**node exporter自身进程相关指标</li>
</ul>
<p>效果如下</p>
<p><img src="/2022/03/01/build-small-cluster/1643895506480.png" alt="1643895506480"></p>
</li>
<li><p>prometheus</p>
<p>首先启动</p>
<pre class="line-numbers language-none"><code class="language-none">./prometheus --config.file="prometheus.yml" &amp;
nohup ./node_exporter &amp;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>检查端口占用确定是否启动</p>
<pre class="line-numbers language-none"><code class="language-none">ss -naltp | grep 9090
ss -naltp | grep 9100<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p><img src="/2022/03/01/build-small-cluster/1643949022952.png" alt="1643949022952"></p>
<p><img src="/2022/03/01/build-small-cluster/1643949002455.png" alt="1643949002455"></p>
<p>设置转发端口，进入浏览器界面</p>
<pre class="line-numbers language-none"><code class="language-none">http://localhost:9090/targets<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p><img src="/2022/03/01/build-small-cluster/1643949743317.png" alt="1643949743317"></p>
<pre class="line-numbers language-none"><code class="language-none">http://localhost:9090/graph<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>进行go_gc_duration_seconds这一指标的信息查询</p>
<p><img src="/2022/03/01/build-small-cluster/1643949890531.png" alt="1643949890531"></p>
<p><img src="/2022/03/01/build-small-cluster/1643949941722.png" alt="1643949941722"></p>
</li>
<li><p>grafana</p>
<p>执行如下指令</p>
<pre class="line-numbers language-none"><code class="language-none">sudo rpm -Uvh grafana-8.3.4-1.x86_64.rpm
sudo systemctl daemon-reload
sudo systemctl start grafana-server
sudo systemctl status grafana-server
sudo systemctl enable grafana-server<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>在<code>/etc/sysconfig/grafana-server</code>文件尾加入下列语句</p>
<pre class="line-numbers language-none"><code class="language-none">http_proxy=http://proxy.domain:3128/
https_proxy=http://proxy.domain:3128/
no_proxy=internal.domain,127.0.0.1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>然后执行如下指令</p>
<pre class="line-numbers language-none"><code class="language-none">sudo service grafana-server start
sudo service grafana-server status
sudo /sbin/chkconfig --add grafana-server<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>设置转发端口，从浏览器访问 <a href="http://localhost:3000/%EF%BC%8C%E5%8F%AF%E4%BB%A5%E7%9C%8B%E5%88%B0Grafana%E7%95%8C%E9%9D%A2%E5%A6%82%E4%B8%8B%EF%BC%88%E7%AC%AC%E4%B8%80%E6%AC%A1%E8%AE%BF%E9%97%AE%E9%9C%80%E8%BF%9B%E8%A1%8C%E6%96%B0%E5%AF%86%E7%A0%81%E7%9A%84%E8%AE%BE%E7%BD%AE%EF%BC%8C%E9%BB%98%E8%AE%A4%E8%B4%A6%E5%8F%B7%E5%AF%86%E7%A0%81%E9%83%BD%E6%98%AFadmin%E3%80%82">http://localhost:3000/，可以看到Grafana界面如下（第一次访问需进行新密码的设置，默认账号密码都是admin。</a></p>
<p><img src="/2022/03/01/build-small-cluster/1643983202310.png" alt="1643983202310"></p>
<p>在Configuration中选择添加数据源后，即可配置面板进行相关指标的可视化查看。</p>
<p><img src="/2022/03/01/build-small-cluster/1643983742771.png" alt="1643983742771"></p>
<p>添加面板对指标进行实时监视</p>
<p><img src="/2022/03/01/build-small-cluster/1643984064282.png" alt="1643984064282"></p>
</li>
<li><p>通过stress生成一些特定的负载，下面跑一个IO压测</p>
<pre class="line-numbers language-none"><code class="language-none">stress-ng -i 4 --hdd 1 --timeout 240<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>可以看到面板中node_disk_io_time_seconds_total这一指标有明显变化<img src="/2022/03/01/build-small-cluster/1643984838655.png" alt="1643984838655"></p>
</li>
</ul>
<h2 id="Final-Task"><a href="#Final-Task" class="headerlink" title="Final Task :"></a>Final Task :</h2><p>可以看看这里面的小任务，发挥你自己的想法随意发挥</p>
<p><a href="final_task.md">final task</a></p>
]]></content>
      <categories>
        <category>集群</category>
      </categories>
      <tags>
        <tag>集群</tag>
      </tags>
  </entry>
  <entry>
    <title>Learning Transferable Visual Models From Natural Language Supervision</title>
    <url>/2022/03/10/learn-tvm-from-nlp/</url>
    <content><![CDATA[<p>SOTA训练系统以预测一组固定的预定对象类别，这限制了CV系统的通用性，对于任何其他视觉概念，需要额外的标记数据来进行指定。</p>
<p>从有关图像的原始文本中进行学习能够利用更加广泛的监督来源。本文证明了预测哪个标题与哪个图像搭配的简单预训练任务是一种有效且可扩展的方式，可以在从互联网收集的 4 亿（图像，文本）对的数据集上从头开始学习 SOTA  图像表示。</p>
<p>在预训练之后，使用自然语言来文本表示学习的视觉概念（或描述新的概念），使模型能够零样本转移（zero-shot transfer）到下游任务。在大多数任务上，与完全监督学习的baseline相比具有竞争力，且无需任何特定的数据集。</p>
<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>现有工作缺乏动态输出机制，削弱了灵活性，限制了零样本迁移能力</p>
<p>对比语言图像预训练</p>
<p>CLIP与GPT相似，优于最好的Imagenet模型，且计算效率更高</p>
<h2 id="Approach"><a href="#Approach" class="headerlink" title="Approach"></a>Approach</h2><p><img src="/2022/03/10/learn-tvm-from-nlp/1647189274363.png" alt="1647189274363"></p>
<p>方法的核心是从自然语言的监督中学习感知。</p>
<p>从自然语言中学习的优势</p>
<ul>
<li>因为不需要采用经典的机器学习兼容格式（从N个选出1个），相较于用于图像分类的标准crowd-sourced labelling更加容易拓展</li>
<li>不仅是学习一种表示，还能将表示与语言联系起来，实现灵活的零样本迁移</li>
</ul>
<h3 id="Pre-Training-Method"><a href="#Pre-Training-Method" class="headerlink" title="Pre-Training Method"></a>Pre-Training Method</h3><p>训练效率是最重要的，本文根据这一指标选择预训练方法</p>
<p>仅预测整个文本与哪个图像配对，而不是该文本的确切单词</p>
<p><img src="/2022/03/10/learn-tvm-from-nlp/1647224538268.png" alt="1647224538268"></p>
<p>橙线和蓝线表示的方法试图预测每张图像所附文本的确切单词。这是一项艰巨的任务，因为与图像同时出现的描述、评论和相关文本种类繁多。可以看到本文提出的方法在效率上有很大的提升。</p>
<p>对于给定的N个（图像，文本）对，CLIP用于预测NxN个对中哪个是实际发生的。</p>
<p>CLIP学习得到一个多模态嵌入空间，通过图片编码器和文本编码器来最大化batch中的N个真实对的余弦相似度，同时最小化另外$N^2-N$个非正确对的余弦相似度。</p>
<p>在相似度分数上，本文使用对称交叉熵损失进行优化</p>
<p><img src="/2022/03/10/learn-tvm-from-nlp/1647225316655.png" alt="1647225316655"></p>
<p>从头开始训练，没有使用ImageNet的权重或是文本编码器的预训练权重进行初始化。</p>
<p>不使用表示和对比嵌入空间之间的非线性投影，只使用线性投影将每个编码器的表示映射到多模态嵌入空间。作者没有发现两个版本之间训练效率的差异，并推测非线性投影可能仅在自监督表示学习方法中与当前图像的细节共同适应。</p>
<p>删除了Zhang et al.(2020)提出的文本转换函数 $t_u$（从文本中统一采样单个句子），因为 CLIP 预训练数据集中的许多（图像，文本）对中文本只是一个句子。</p>
<p>简化了图像变化函数$t_v$，从resized的图片中随机裁剪正方形是训练阶段唯一使用的数据增强。</p>
<p>用于控制softmax中logits的范围的温度参数$\tau$，在训练阶段直接优化为log-parameterized乘法标量，以避免将其作为超参数。</p>
<h3 id="Choosing-and-Scaling-a-Model"><a href="#Choosing-and-Scaling-a-Model" class="headerlink" title="Choosing and Scaling a Model"></a>Choosing and Scaling a Model</h3><p>图像编码器</p>
<ul>
<li>ResNet-50<ul>
<li>使用ResNet-D和抗锯齿 rect-2 模糊池进行了优化</li>
<li>用注意力池机制替换了全局平均池层，实现形式为单层的多头QKV注意力，查询以图像的全局平均池表示为条件</li>
</ul>
</li>
<li>ViT<ul>
<li>在Transformer前面的combined patch和 position embeddings添加了一个额外的层归一化</li>
<li>使用了略有不同的初始化方案</li>
</ul>
</li>
</ul>
<p>文本编码器</p>
<ul>
<li>Transformer，修改细节见Radford et al. (2019) <ul>
<li>8 个注意力头，参数量63M，12 层，512-wide</li>
<li>Transformer对文本的小写字节对编码 (BPE) 表示进行操作，词汇大小为 49,152 。</li>
<li>为了计算效率，最大序列长度为76</li>
<li>文本序列用 [SOS] 和 [EOS] 标记括起来，并且在 [EOS] 标记处转换器的最高层的激活被视为文本的特征表示，经过层归一化，然后线性投影到多-模态嵌入空间</li>
<li>Masked self-attention 在文本编码器中使用，以保留使用预训练语言模型进行初始化或添加语言建模作为辅助目标的能力（这一步需要在未来工作进一步探索）</li>
</ul>
</li>
</ul>
<p>模型的缩放</p>
<ul>
<li>图像编码器<ul>
<li>平均分配额外的计算以增加模型的宽度、深度和分辨率</li>
</ul>
</li>
<li>文本编码器<ul>
<li>只缩放模型的宽度，使其与计算的 ResNet 宽度增加成正比</li>
<li>不缩放深度，CLIP 的性能对文本编码器的容量不太敏感</li>
</ul>
</li>
</ul>
<h3 id="Training"><a href="#Training" class="headerlink" title="Training"></a>Training</h3><p>图像编码器（训练了32个epochs）</p>
<ul>
<li>ResNet-50</li>
<li>ResNet-101</li>
<li>RN50x4、RN50x16 和 RN50x64这三个EfficientNet-style model，分别使用 ResNet-50 的大约 4 倍、16 倍和 64 倍的计算。</li>
<li>ViT-B/32</li>
<li>ViT-B/16</li>
<li>ViT-L/14</li>
</ul>
<p>优化器：Adam，在所有非增益或偏差的权重使用解耦权重衰减正则化，使用余弦调度调整学习率</p>
<p>在训练 1 个 epoch 时，使用网格搜索、随机搜索和手动调整基线 ResNet50 模型的组合来设置初始超参数。</p>
<p>可学习参数$\tau$初始化为0.07，并进行裁剪以防止将 logits 缩放超过 100</p>
<p>一个非常大的minibatch size 32768，使用混合精度来加速训练、节省内存。</p>
<p>为了节省额外的内存，梯度检测点使用了半精度 Adam 统计（Dhariwal 等人，2020）和半精度随机舍入文本编码器权重。</p>
<h2 id="Experiment"><a href="#Experiment" class="headerlink" title="Experiment"></a>Experiment</h2><p>在CV领域，零样本学习是指在图像分类中泛化到看不见的对象类别的研究</p>
<p>本文将零样本学习视为对看不见的数据集的泛化（这个看不见的数据集里面有没有未知的类别？）</p>
<p>现有无监督学习领域的研究主要关注机器学习系统的表示学习能力，本文鼓励将零样本迁移作为衡量机器学习任务学习能力的一种方式。</p>
<p>从这样的角度看，数据集评估了特定分布上任务的性能。但当前学界创建许多流行的CV数据集，主要是作为基准来指导通用图像分类方法的开发，而不是衡量特定任务的性能。</p>
<p>在这些类型的数据集上，零样本迁移更多的是对 CLIP 对分布转移和域泛化的鲁棒性的评估，而不是任务泛化。</p>
<p>Visual N-grams：</p>
<ul>
<li>首先将每个数据集的类名转换为其n-gram表示</li>
<li>根据模型计算其概率，将对应的类别预测为最高的得分</li>
</ul>
<p>本文</p>
<ul>
<li>对每个数据集，使用数据集中所有类的名称作为潜在文本对的集合</li>
<li>根据CLIP预测最可能的（图像，文本）对</li>
</ul>
<p>具体做法</p>
<ul>
<li>根据不同的编码器计算图像的特征嵌入和文本集的特征嵌入</li>
<li>计算嵌入的余弦相似度，通过温度参数$\tau$进行缩放，通过softmax归一化为概率分布</li>
<li>预测层是一个多项式逻辑回归分类器（具有L2归一化输入、L2归一化权重、无偏差和温度缩放）</li>
</ul>
<p>图像编码器是计算图像特征表示的计算机视觉主干，而文本编码器是一个超网络 (Ha et al.,  2016)，它根据指定文本的文本生成线性分类器的权重类所代表的视觉概念。</p>
<p>CLIP 的零样本分类器是通过自然语言生成的，它允许直接指定的视觉概念。相比之下，常规的监督学习必须从训练样本中间接推断出概念。</p>
<h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>NLP预训练模型的成功在CV领域同样适用。CLIP模型在预训练阶段执行各种各样的预训练任务，以优化训练目标。然后可以通过自然语言提示利用此任务学习，以实现对许多现有数据集的零样本迁移。在规模足够大时，这种方法的性能与特定任务的监督学习相比具有竞争力，且仍有很大的改进空间。</p>
<p>存在的问题</p>
<ul>
<li>CLIP的任务学习和迁移能力仍待提升</li>
<li>需要提高计算效率，计算开销过大，性能未饱和</li>
<li>在细粒度分类上的性能较差</li>
<li>对解决深度学习模型泛化脆弱的潜在问题几乎没有作用，需要在庞大而多样的数据集上进行训练，使所有数据都能有效地分布</li>
</ul>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>一行Python指令实现文件共享</title>
    <url>/2022/05/09/pyshare/</url>
    <content><![CDATA[<h2 id="一行Python指令实现文件共享"><a href="#一行Python指令实现文件共享" class="headerlink" title="一行Python指令实现文件共享"></a>一行Python指令实现文件共享</h2><p>首先在终端切换到需要共享的文件目录</p>
<p><img src="/2022/05/09/pyshare/1652073769778.png" alt="1652073769778"></p>
<h3 id="输入指令"><a href="#输入指令" class="headerlink" title="输入指令"></a>输入指令</h3><pre class="line-numbers language-python" data-language="python"><code class="language-python">python <span class="token operator">-</span>m http<span class="token punctuation">.</span>server <span class="token number">31142</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p> 把电脑的文件通过 http 协议共享出去，<code>31142</code>是端口，这里可以任意指定没有被占用的端口。 </p>
<p> 出现如下界面，即为共享成功。</p>
<p><img src="/2022/05/09/pyshare/1652073798053.png" alt="1652073951054"></p>
<h3 id="访问文件"><a href="#访问文件" class="headerlink" title="访问文件"></a>访问文件</h3><p>对于本机，在浏览器访问网址</p>
<pre class="line-numbers language-none"><code class="language-none">http://localhost:31142<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>界面如下，可以看到文件夹下的目录</p>
<p><img src="/2022/05/09/pyshare/1652073951054.png" alt="1652073951054"></p>
<p>也可以找到本机的 ip地址，将 <code>localhost</code> 替换成 ip地址即可访问。这一方法对于连接上同一网络的局域网内其他机器来说也同样适用：</p>
<pre class="line-numbers language-none"><code class="language-none">http://172.xx.xxx.xxx:31142<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p><img src="/2022/05/09/pyshare/1652074234803.png" alt="1652074234803"></p>
]]></content>
      <categories>
        <category>杂七杂八</category>
      </categories>
      <tags>
        <tag>杂七杂八</tag>
      </tags>
  </entry>
  <entry>
    <title>MDETR - Modulated Detection for End-to-End Multi-Modal Understanding</title>
    <url>/2022/04/20/mdetr/</url>
    <content><![CDATA[<h2 id="Abstract"><a href="#Abstract" class="headerlink" title="Abstract"></a>Abstract</h2><p>多模态推理系统依靠预先训练的目标检测器从图像中提取感兴趣的区域，但目标检测器这个关键模块通常被当作黑盒使用，其采用对象和属性的固定词汇进行训练，这一过程独立于下游任务。这使得这样的系统难以捕捉自由形式文本所表达的视觉概念的长尾分布（long tail）。</p>
<p>本文提出了一种端到端的调制检测器，用于检测以原始文本为条件的图像中的对象，如标题或问题。 不同于传统的目标检测，<strong>modulated detection（调制检测）不是对每一个检测到的物体都感兴趣，而是只对原始文本中出现的object感兴趣。</strong>  </p>
<p>MDETR使用transformer-based的架构，通过在模型的早期阶段融合这两种模式，对文本和图像进行联合推理。本文对网络进行预训练。从预先存在的多模态数据集中挖掘了1.3M个文本-图像对，文本中的短语和图像中的对象之间具有明确的对齐方式。 </p>
<p>然后，本文对几个下游任务进行微调，例如短语基础（phrase grounding）、引用表达式理解和分割，在流行基准上获得了SOTA。本文还研究了当在few-shot setting中进行微调时，MDETR作为给定标签集上的目标检测器的实用性。实验结果表明本文提出的预训练方法提供了一种能够处理具有很少标记实例的对象类别的长尾的方案。本文的方法可以很容易地扩展为视觉问答，在GQA和CLEVR上表现出了有竞争力的性能。 </p>
<p>注：长尾问题：在实际的视觉相关问题中，数据都存在长尾分布：少量类别占据绝大多数样本，大量的类别仅有少量的样本，比如open-images，ImageNet等。 <a href="https://blog.csdn.net/qq_36523203/article/details/107019526">https://blog.csdn.net/qq_36523203/article/details/107019526</a></p>
<h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>本文提出了一种完全可微分的调制检测器MDETR，这一模型在各种数据集上的多模式理解任务上表现出强大的性能，并具备在其他下游应用中的潜力，例如小样本检测（few-shot detection）和视觉问答。</p>
<p>本文的贡献主要是提供了开发完全集成的多模式架构的思路，而不像过去那样依赖于黑盒目标检测器。</p>
<p>优点</p>
<ul>
<li>结合多模态工作和DETR，得到一个具有良好泛化性的目标检测器，方案中检测目标的查询不再受到单模态数据集的标注限制</li>
<li>MDETR并没有使用一些语言-视觉预训练工作的自监督策略(例如mask region classification)，而是设计了一套有监督的、将text-discription和object region对齐的预训练方案</li>
<li>模型能够从自由格式的文本中检测细微的概念，并推广到未知的类别和属性组合。使用者不用纠结于”bicycle’和’’motorcycle”要不要整成一类来训练检测器这种问题。</li>
<li>在使用更少数据的前提下，在一些任务上取得了SOTA的结果</li>
</ul>
<p>缺点</p>
<ul>
<li>面对长尾检测的零样本迁移时，无法避免极端的类不平衡造成的问题（性能从 10% 数据的 20.9 AP  显着下降到 100% 的 7.5 AP）。</li>
<li>作者给出的一种可能的改进方案：重复因子采样（Repeat Factor Sampling）。</li>
</ul>
<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>当前的多模态理解系统依赖于目标检测，将其当作一个黑盒来检测图像中固定的概念词汇。这一方法不考虑下游多模态理解任务，导致了性能的瓶颈。目标检测器被冻结阻止了模型感知能力的进一步细化，而且将其限制为只能访问检测到的对象而非整个图像。这限制了pipeline与其他模式作为上下文的协同训练。在视觉语言设置中，这意味着将结果系统的词汇限制为检测器的类别和属性。因此这样的系统无法识别可以用自由格式文本表达的概念的新颖组合。</p>
<p>最近的一些工作[67、45、13]考虑text-conditioned的目标检测任务，这些方法拓展了主流的单阶段和两阶段检测架构来实现这一目标。但这样的检测器尚未被证明能够提高下游任务的性能，这些任务需要对检测到的对象进行推理（如VQA），本文作者认为这些检测器不是端到端可区分的，因此无法与下游任务协同训练。</p>
<p>MDETR是基于DETR[2]检测框架的端到端调制检测器，结合自然语言理解进行目标检测，实现了真正的端到端多模态推理。MDETR仅依靠文本和对齐的检测框作为对图像中概念的监督形式。因此，与当前的检测方法不同，MDETR 从自由格式的文本中检测细微的概念，并推广到未知的类别和属性组合。MDETR的预测以文本为基础，这是视觉推理的关键要求[66]。</p>
<p>通过使用包含 200,000 张图像的数据集和带有框注释的对齐文本进行预训练时，MDETR在用于phrase grounding的Flickr30k 数据集，用于REC的RefCOCO/+/g 数据集和用于RES的PhraseCut数据集上取得了SOTA，同时在视觉问答的 GQA 和 CLEVR 基准测试上达到了有竞争力的性能。</p>
<p>如下图， 查询“粉红大象”的MDETR输出。颜色不是分割掩码，而是像素的真实颜色。模型在训练中从未见过粉红或蓝色的大象。 </p>
<p><img src="/2022/04/20/mdetr/1650375394734.png" alt="1650375394734"></p>
<p><strong>本文的贡献</strong></p>
<ul>
<li>提出一种源自 DETR 检测器的端到端文本调制检测系统。</li>
<li>证明了调制检测方法可以无缝地应用于解决诸如phrase grounding和REC等任务，通过使用合成图像和真实图像的数据集进行训练，在这两个任务上达到了SOTA。</li>
<li>表明良好的调制检测性能会自然地转化为下游任务性能，例如在视觉问答、引用表达分割和few-shot长尾目标检测方面实现竞争性能。</li>
</ul>
<h2 id="Related-work"><a href="#Related-work" class="headerlink" title="Related work"></a>Related work</h2><p>多模态理解的最新进展主要是通过预训练大型 Transformer 模型来从大量对齐的图像-文本数据中学习通用多模态表示 [53]，然后在下游任务上对其进行微调。</p>
<p>这些方法可以分为单流 [6, 28, 73, 26] 和双流 [55, 34, 35, 54]  架构，具体取决于文本和图像是由单个组合转换器处理还是由两个单独的转换器处理通过一些交叉注意力层。对于这两种类型，流行的方法是独立提取视觉和文本特征，然后使用转换器的注意力机制来学习两者之间的对齐。</p>
<p>当前最先进模型使用的视觉特征是使用外部预训练检测器 [1] 提取的，该检测器输出有噪声、通常过采样和模棱两可的区域。  [28]试图通过使用标签作为文本和图像之间的锚来缓解噪声图像特征的问题。与 MDETR 相比，这仍然是一种较弱的监督形式，<strong>MDETR  在文本中的单词或短语与图像中的对象之间进行了明确的对齐</strong>。</p>
<p>为了减轻概念的固定词汇所隐含的约束，[73] 训练了很多更大的对象检测数据集以追求更好的覆盖范围。即拓展训练样本以增加泛化能力。[9] 在现有高性能模型的基础上进行对抗性训练，将性能推得更高。其他方法 [69]  尝试将场景图预测作为其预训练的一部分来学习更稳健的表示。最近的一些工作还尝试构建多用途多模态架构，这些架构能够在单个架构中处理各种视觉语言 [7]  以及纯语言任务 [16]。</p>
<p>解决与本文类似的问题但具有更多任务的专用模型架构的另一项工作是对于这个任务，专门设计的单个阶段 [67、3、27] 和两个阶段 [45、13]  的引用表达分割和短语检测模型。</p>
<h2 id="Method"><a href="#Method" class="headerlink" title="Method"></a>Method</h2><h3 id="DETR"><a href="#DETR" class="headerlink" title="DETR"></a>DETR</h3><p>方法基于DETR [2]，这是一种端到端检测模型，由主干网络（通常是卷积残差网络 [12]）和 Transformer Encoder-Decoder [59] 组成。</p>
<p>DETR  编码器对来自主干的 2D 展开的图像特征进行操作，并应用一系列transformer层。解码器将一组包含N个学习嵌入称为对象查询作为输入，可以将其视为模型需要用检测到的对象填充的槽。所有的对象查询并行传送到解码器，解码器使用交叉注意力层查看编码图像并预测每个查询的输出嵌入。每个对象查询的最终表示使用共享的前馈层独立解码为目标框坐标和类标签。对象查询的数量是模型可以同时检测到的对象数量的上限。必须将它设置为在给定图像中可能会遇到的对象数量的足够大的上限。由于特定图像中的实际对象数可能小于查询数  N，因此我们使用对应于“no object”的额外类标签，记为∅。模型经过训练，可以为每个与对象不对应的查询输出这个额外类。</p>
<p>DETR  使用<strong>匈牙利匹配损失</strong>进行训练，其中在 N  个提议的对象和真实对象之间计算二分匹配。每个匹配的对象都使用相应的目标作为ground truth进行监督，而未匹配的对象被监督以预测“no object”标签∅。分类头使用标准交叉熵进行监督，而边界框头使用绝对误差（L1  损失）和广义 IoU [48] 的组合进行监督。</p>
<h3 id="MDETR"><a href="#MDETR" class="headerlink" title="MDETR"></a>MDETR</h3><p>与 DETR 一样，图像由卷积主干编码并执行扁平化（flatten）。2-D 位置嵌入被添加到这个扁平化的向量中以保存空间信息。使用预训练的transformer语言模型对文本进行编码，以生成与输入大小相同的隐藏向量序列。将模态相关的线性投影应用于图像和文本特征，以将它们投影到共享的嵌入空间中。然后将这些特征向量在序列维度上连接起来，以产生单个图像和文本特征序列。该序列被馈送到称为交叉编码器的联合变压器编码器。在 DETR  之后，MDETR在对象查询上应用transformer解码器，同时交叉关注交叉编码器的最终隐藏状态。解码器的输出用于预测实际的检测框。</p>
<h3 id="损失函数"><a href="#损失函数" class="headerlink" title="损失函数"></a>损失函数</h3><p>MDETR 使用了两个附加的损失函数，用于鼓励图像和文本之间的对齐。它们都使用相同来源的注释：带有对齐边界框的自由格式文本。第一个损失函数是非参数对齐损失，称为soft token prediction loss。第二个损失函数是有参数损失函数，称为文本查询对比对齐，用于在对齐的对象查询和标记之间执行相似性。</p>
<h4 id="Soft-token-prediction"><a href="#Soft-token-prediction" class="headerlink" title="Soft token prediction"></a>Soft token prediction</h4><p>调制检测与标准检测设置不同，我们对预测每个检测到的对象的分类类别不感兴趣，而是从引用每个匹配对象的原始文本中预测标记的跨度。具体来说，我们首先将任何给定句子的最大标记数设置为 L = 256。对于使用二分匹配与ground truth边界框匹配的每个预测框，该模型被训练以预测与对象对应的所有标记位置的均匀分布。</p>
<p><strong>关于soft token prediction</strong></p>
<ul>
<li>‘soft’指不会直接预测每一个检测到的bbox的对应类别，而是预测image patch token对应的word token (来自原始输入的text)</li>
<li>每一个text(或sentence)的长度(最大word token数)会被限制在256<ul>
<li>256个token，对256个word token进行bbox的判断，是或不是（TODO</li>
</ul>
</li>
<li>MDERT会计算bbox与每一个word token的相关度(bi-partite)，以下图为例,猫的bbox和前两个work token “a cat”产生了最强关联(uniform distribution)</li>
<li>每个bbox和text关系的uniform distribution如下图所示</li>
<li>特别注意：1) text中一些无对应bbox的word，会分配给一个“no object”。2) 一个word可能会对应多个bbox，例如”夫妻”可能对应到图像的两个bbox</li>
</ul>
<p>下图展示了一个示例，其中cat的边界框被训练以预测前两个单词的均匀分布。MDETR 使用卷积主干提取视觉特征，使用 RoBERTa  等语言模型提取文本特征。两种模式的特征都被投影到一个共享的嵌入空间，连接起来并馈送到一个transformer编码器-解码器，该转换器预测对象的边界框及其在文本中的背景。</p>
<p><img src="/2022/04/20/mdetr/1650377504382.png" alt="1650377504382"></p>
<p>在图 9 中，作者展示了这个例子的损失的简化可视化，考虑每个框的单词分布，但在应用中作者使用 BPE 方案 [52]  进行标记化之后使用标记跨度。任何与目标不匹配的查询都经过训练以预测“no object”标签∅。</p>
<p><img src="/2022/04/20/mdetr/1650377968308.png" alt="1650377968308"></p>
<p>需要注意的是，文本中的几个单词可能对应于图像中的同一个对象，相反，几个对象可能对应于同一个文本。</p>
<p>通过以这种方式设计损失函数，我们的模型能够从相同的引用表达式中学习共同引用的对象。例如，图像中的两个框所指的“一对”可以在同一个caption中进一步单独引用。通过以这种方式设计损失函数，模型能够从相同的引用表达式中学习共同引用的对象。</p>
<h4 id="Contrastive-alignment"><a href="#Contrastive-alignment" class="headerlink" title="Contrastive alignment"></a>Contrastive alignment</h4><p>对比对齐（Contrastive alignment）虽然软标记预测（Soft token prediction）使用位置信息将对象与文本对齐，但【对比对齐损失】会强制【解码器输出处对象】的嵌入表示与【交叉编码器输出处】的文本表示之间对齐。与不相关标记的嵌入相比，这种额外的对比对齐损失确保（视觉）对象及其对应（文本）标记的嵌入在特征空间中更接近。这个约束比软标记预测损失更强，因为它直接对表示进行操作，而不是仅仅基于位置信息。</p>
<p>更具体地说，考虑最大令牌数为 $L$ ，最大对象数为 $N$ 。令 $T^+<em>i$ 是给定对象 $o_i$ 应对齐的令牌集，$O^+<em>i$ 是要与给定令牌 $t_i$ 对齐的对象集。受  InfoNCE [40] 启发，所有对象的对比损失通过每个对象的正标记数进行归一化：<br>$$<br>l</em>{o}=\sum</em>{i=0}^{N-1} \frac{1}{\left|T_{i}^{+}\right|} \sum_{j \in T_{i}^{+}}-\log \left(\frac{\exp \left(o_{i}^{\top} t_{j} / \tau\right)}{\sum_{k=0}^{L-1} \exp \left(o_{i}^{\top} t_{k} / \tau\right)}\right)<br>$$<br>其中$\tau$是温度参数，根据文献[64, 47]设置为0.07。</p>
<p>通过对称性，所有标记的对比损失，由每个标记的正对象数归一化，由下式给出：<br>$$<br>l_{t}=\sum_{i=0}^{L-1} \frac{1}{\left|O_{i}^{+}\right|} \sum_{j \in O_{i}^{+}}-\log \left(\frac{\exp \left(t_{i}^{\top} o_{j} / \tau\right)}{\sum_{k=0}^{N-1} \exp \left(t_{i}^{\top} o_{k} / \tau\right)}\right)<br>$$<br>我们将这两个损失函数的平均值作为对比对齐损失。</p>
<p><strong>关于contrastive alignment</strong> </p>
<ul>
<li>这一项损失用来确保image在投影后能够和text共享embedding space，即添加一个Loss，让image feature在通过Transformer后的embedding和uniform distribution中关联最强的text的embedding进行对比计算。这里的对比损失来自于InfoNCE的启发，如公式1和2所示，其中o是object(bbox)对应的embedding，t是word对应的embedding</li>
<li>公式（1）让 某一个bbox的embedding 和对应的 word token集合 的 embedding尽量趋近，而和其他的word token尽量不相似 </li>
<li>公式（2）让 某一个word的embedding 和 对应的bbox token集合  的embedding尽量趋近，而和其他的bbox token尽量不相似</li>
</ul>
<h2 id="Experiments"><a href="#Experiments" class="headerlink" title="Experiments"></a>Experiments</h2><p>本节中描述了用于预训练 MDETR 的数据和训练，并提供了用来评估我们的方法的任务的详细信息和结果。</p>
<p>CLEVR 数据集合成数据的实验和结果在4.1中描述，4.2中描述对真实世界图像的实验。</p>
<h3 id="Synthetic-Images"><a href="#Synthetic-Images" class="headerlink" title="Synthetic Images"></a>Synthetic Images</h3><p>CLEVR  [20] 是一个合成数据集，旨在评估受控环境中的多模态模型。虽然按照现在的标准，这项任务的视觉理解部分是微不足道的，但主要挑战是理解问题，其中可能包含多达 20  个推理步骤，并将其中的关键短语与图像中的正确对象对齐。与 CLEVR [21, 17, 37, 68] 的几种成功方法不同，MDETR  没有包含任何特殊的归纳偏差来应对如此复杂的推理任务。在本节中，作者展示了尽管MDETR的公式相对简单，但本文的方法在问答任务上与最先进的模型竞争。</p>
<p>在模型上，MDETR使用来自 Torchvision 的 ResNet-18 [12]，在 ImageNet [49] 上进行预训练作为卷积主干。对于文本编码器，使用来自  HuggingFace [62] 的预训练 DistilRoberta  [50]。最终的transformer和DETR一样，有6个encoder层和6个decoder层，注意力层中有  8 个注意力头。作者将对象查询的数量减少到 25 个，因为要检测的最大对象数量很少。</p>
<p>在监督学习上，训练  MDETR 所需的第一个要素是图像中对象的边界框注释。原始 CLEVR  数据集并没有提供，因此作者需要使用数据集中的场景图来重新创建它们。所需的第二个要素是问题中边界框和标记之间的对齐。 MDETR  被训练为仅预测问题中提到的对象。关于边界框和跨度提取的更多细节可见于附录 B.1。</p>
<p>在训练上，本文提取完整数据集的一个子集，其中仅包含对给定对象的单个引用的问题。作者称之为 CLEVR-Medium ，这个子集比完整集小约  15%。本文采用课程学习策略，首先在这个简化集上只训练 30 个 epoch 的调制检测任务，然后在带有调制检测和问答损失的完整 CLEVR 上训练 30 个  epoch。我们发现，如果没有这种课程策略，模型会收敛到局部最小值，并且在完整的 CLEVR 数据集上评估的性能低于标准。如图 3  所示，作者在转换器解码器中使用附加查询来回答 CLEVR 中的每种类型的问题：数字、二值和属性。作者使用标准的交叉熵损失来监督训练模型的每一个头。附录 B.2  和 B.3 中提供了超参数以及有关课程策略需求的更多详细信息以及对损失的一些消融。</p>
<p>如下图，在 MDETR 预训练期间，模型被训练以检测问题中提到的所有对象。为了将其扩展为问答，除了对象查询之外，本文还提供 QA  特定查询作为转换器解码器的输入。作者为不同的问题类型使用特定的头。</p>
<p><img src="/2022/04/20/mdetr/1650386142369.png" alt="1650386142369"></p>
<p>多个测试集及任务的结果如下：</p>
<ul>
<li>MDETR 在 CLEVR 上与 NS-VQA[68]（一种以 groundtruth 程序注释形式使用外部监督的方法）的性能非常匹配，同时远超其他未使用这个额外的监督信号的算法性能。</li>
<li>CLEVR-Humans  [21] 是关于 CLEVR 图像的人类生成问题的数据集。<strong>它测试了模型对新词汇和不同推理原语的鲁棒性。</strong>在zero-shot setting中，MDETR远超SOTA。作者将这种改进归功于其预先训练的语言模型。微调后，差距缩小，表明可能需要额外的工作来进一步提高性能。</li>
<li>CoGenT  是一种组合泛化测试。评估方式：在set A上进行训练，其中球体可以是任何颜色，但立方体是灰色、蓝色、棕色或黄色，圆柱体是红色、绿色、紫色或青色。然后，以零样本的方式对split B 进行评估，split B  具有立方体和圆柱体的相反颜色-形状对。与其他模型类似，作者观察到显著的泛化差距，最大的准确率下降出现在查询对象形状的问题（从 testA 的  99.98% 到 testB 的 34.68%），这表明该模型已经学习到了<strong>形状和颜色之间的强烈虚假偏差</strong>。（PS：这里我的理解是，模型学习到了形状和色彩之间的联系性，将特定颜色与特定形状关联了起来，即为论文中提到的虚假偏差）</li>
<li>CLEVR-REF+[31] 是一个基于 CLEVR 图像构建的引用表达理解数据集。对于每个对象查询，作者训练一个额外的二进制（binary）头来预测查询是否对应于被引用的对象（与检测到的句子中的辅助对象相反）。在 [31]  之后，作者评估引用唯一对象的表达式子集的准确性，衡量排名靠前的框与目标框的 IoU 是否至少为  0.5。使用上述二元预测对框进行排序，模型正确地将验证集的每个示例的有效框排在首位，从而达到 100% 的准确度，大大优于先前的工作。</li>
</ul>
<h3 id="Natural-Images"><a href="#Natural-Images" class="headerlink" title="Natural Images"></a>Natural Images</h3><p>对于预训练，本文专注于调制检测的任务，其目的是检测对齐的自由格式文本中引用的所有对象。作者使用来自  Flickr30k [46]、MS COCO [30] 和 Visual Genome (VG) [24]  数据集的图像创建了一个组合数据集。来自引用表达式数据集、VG 区域、Flickr 实体和 GQA 训练平衡集的注释用于训练。有关数据集的详细信息可参见附录  C。</p>
<p>在结合上，对于每张图像，作者从这些数据集中获取所有注释，并结合引用同一图像的文本，同时确保所有下游任务的验证或测试集中的所有图像都从训练集中删除。句子的组合是使用图着色算法完成的，该算法确保只组合具有  GIoU≤0.5 框的短语，并且组合句子的总长度小于 250 个字符。通过这种方式，作者得到了一个具有 130  万对齐的图像-文本对的数据集。这个组合步骤很重要，原因有两个：1）数据效率，通过将更多信息打包到单个训练示例中；2）它为我们的软标记预测损失提供了更好的学习信号，因为模型必须学会在多次出现的相同的对象类别，如图  4  所示。在单个句子的情况下，软标记预测任务变得微不足道，因为它总是可以在不查看图像的情况下预测句子的根（root）。通过实验，作者发现这种密集的注释可以转化为文本和图像之间更好的grounding，进而转化为更好的下游性能。</p>
<p>如下图，注释组合产生了如下示例：“穿着灰色衬衫的人手腕上戴着手表。另一个穿着蓝色毛衣的人。第三个穿着灰色外套和围巾的人。“图像展示了模型对此caption的预测，它能够关注图像中的所有对象，然后根据文本消除它们之间的歧义。模型经过训练，可以将短语的词根预测为正标记跨度，正如在图中看到的那样，它正确地指代了三个不同的人。</p>
<p><img src="/2022/04/20/mdetr/1650387355308.png" alt="1650387355308"></p>
<p>在模型上，作者使用预训练的 RoBERTa-base [32] 作为文本编码器，具有 12 个转换器编码器层，每个具有 768 的隐藏维度和多头注意力中的 12  个头。作者采用了 HuggingFace [62] 的实现和权重。对于视觉骨干，作者探索了两个选项。第一个是在 ImageNet 上预训练的 ResNet-101  [12]，具有冻结的 batchnorm 层，取自 Torchvision。这可以与多模态理解领域的最新研究相媲美，其中流行的方法是使用 BUTD 对象检测器和  [1] 中在 VG 数据集上训练的 Resnet-101 主干。在本文的工作中，作者不受预训练检测器存在的限制，并受到其在目标检测 [58]  中的成功的启发，选择探索 EfficientNet 系列 [57] 作为主干。除了 ImageNet 之外，作者还使用了一个模型，该模型使用一种称为  Noisy-Student [65] 的伪标记技术对大量未标记数据进行了训练。作者选择了 EfficientNetB3，它在 ImageNet 上仅使用12M 的参数量就实现了  84.1% 的 top 1 准确率。作者使用了 Timm 库 [61] 提供的实现，并冻结 batchnorm 层。训练超参数在附录 A  中有详细说明。</p>
<p>在 4 个下游任务上方法的性能：</p>
<ul>
<li>Phrase grounding：给定一个或多个可能相互关联的短语，任务是为每个短语提供一组边界框。作者为此任务使用  Flickr30k 实体数据集，并使用 [46] 提供的训练/验证/测试集拆分，并根据 Recall@k 评估性能。对于测试集中的每个句子，作者预测 100  个边界框，并使用软标记对齐预测根据给予与短语对应的标记位置的分数对框进行排序。作者根据两个协议进行评估，将其命名为 ANY-BOX [26, 22] 和  MERGEDBOXES [44]。有关这两种协议的讨论，请参见附录 D。作者将他们的方法与两种方法的现有最先进结果进行比较——文本条件检测模型 [45, 67]  和基于转换器的视觉语言预训练模型 [26]。在 ANY-BOX 设置中，根据验证集上的 Recall@1 衡量，我们在此任务上获得了 8.5  点的提升，而不使用任何预训练（没有额外数据）。通过预训练，我们在测试集中的性能相较于使用相同骨干的SOTA进一步提高了 12.1 个百分点。</li>
<li>Referring expression comprehension：给定一张图片和一个纯文本的引用表达式，任务是通过返回边界框来定位被引用的对象。大多数先前的工作  [70, 34, 6, 69]  在此任务上采用的方法是对一组与图像相关联的预提取边界框进行排序，这些边界框是使用预训练的对象检测器获得的。本文解决了一个更困难的任务—给定一个参考表达式和相关图像，模型通过训练直接进行边界框的预测。该任务有三个已建立的数据集，称为  RefCOCO、RefCOCO+ [71] 和 RefCOCOg  [36]。由于在预训练期间，作者对文本中引用的每个对象进行了注释，因此在此任务中使用模型的方式略有变化。例如，在预训练期间，给定标题“穿着蓝色连衣裙的女人站在玫瑰丛旁边。”，MDETR  将被训练来预测所有参考对象的框，例如女人、蓝色连衣裙和玫瑰丛。 但是，对于引用表达式，任务将只返回一个边界框，这表示整个表达式都引用了女性。出于这个原因，作者在任务特定数据集上对模型进行了  5 个 epoch 的微调。在推理时，使用∅标签对检测到的 100 个框进行排名。设 P (∅) 是分配给“no object”标签的概率，按 1− P (∅)  的降序排列。作者在表 3 中报告了结果，显示了模型在所有数据集相较于SOTA的巨大改进。</li>
<li>Referring expression segmentation：与  DETR 类似，作者展示了其方法可以扩展以通过评估最近的 PhraseCut [63] 数据集的引用表达式分割任务来执行分割，该数据集由来自 VG  的图像组成，每个引用表达式都用分割掩码进行注释。这些表达式包含对象、属性和关系的广泛词汇，使其成为具有挑战性的benchmark。与其他引用表达式分割数据集相反，在  PhraseCut 中，表达式可能引用多个对象。模型被期待找到所有相应的实例。训练分两个阶段进行。第一步，在 40 个 epoch  后采用预训练模型，并在该数据集上对其进行 5 个 epoch 的微调，监督训练模型对所引用的表达式输出正确的框。作者使用验证集上的边界框 AP 来进行early- stopping。在第二阶段，在 [2] 之后，冻结网络的权重，只训练一个分割头 15 个 epoch，使用 Dice/F1 loss [38] 和 Focal  loss [29] 的组合进行监督学习。在推理时，为每个预测框分配一个等于 1−P (∅) 的置信度，其中 P (∅) 是分配给“no object”标记的概率。然后过滤置信度低于 0.7 的边界框。最后，将与这些框中的每一个对应的掩码合并为一个与该引用表达式对应的二进制掩码。结果收集在表 4  中。模型能够为 PhraseCut 涵盖的各种长尾概念生成干净的掩码。附录 A 中给出了MDETR模型对该数据集的预测示例。</li>
<li>Visual Question Answering：作者通过在 GQA 数据集上微调我们的预训练模型来评估调制检测是多模态推理的有用组件的假设。模型架构如图 3 所示。除了用于检测的 100  个查询之外，作者还使用了专门针对问题类型的附加对象查询以及用于预测问题类型的对象查询，其中类型是在 GQA 注释中定义为 REL、OBJ、GLOBAL、CAT  和 ATTR。首先将预训练模型在组合数据集上训练了 40 个 epoch，随机初始化这些对象查询以及每个对象的 head，并且首先在不平衡的所有  GQA 拆分上微调 5 个 epoch，然后在平衡的拆分上训练10个epoch，类似于之前的工作 [28, 5]。在前 5 个 epoch  中，作者训练调制的检测损失和问答，但重视问答损失，鼓励模型更多地关注这项任务。对于平衡分割微调，只使用问答损失。在推理过程中，特定类型的head预测特定类型问题，得到的答案取自该head。使用MDETR模型和  Resnet-101 骨干网络，模型性能不仅优于使用同等水平数据量的 LXMERT [55] 和 VL-T5 [7]，而且优于在预训练中使用更多数据的 OSCAR  [28]。如表 5 所示，具有 EfficientNet-B5 主干的 MDETR 能够将性能提升到更高。NSM 模型利用外部场景图生成模型，而 MMN  模型在训练期间利用场景图和功能程序。为了训练 MDETR，作者使用场景图来获得问题词和框之间的对齐。</li>
</ul>
<h3 id="Few-shot-transfer-for-long-tailed-detection"><a href="#Few-shot-transfer-for-long-tailed-detection" class="headerlink" title="Few-shot transfer for long-tailed detection"></a>Few-shot transfer for long-tailed detection</h3><p>受  CLIP [47] 成功的启发，关于图像分类的零样本迁移，作者探索了在给定标签集上从预训练的 MDETR 模型构建有效检测器。与 CLIP  不同，作者不确保预训练数据集包含所有目标类的平衡表示。通过构建，我们的数据集没有与文本对齐的无边界框的训练实例，模型始终是预测给定文本的边界框。这会阻止在真正的零样本迁移设置中进行评估，因此作者转而使用少量样本设置，在该设置中，模型在可用标记数据的一小部分上进行训练。</p>
<p>作者在  LVIS 数据集 [11] 上进行了实验，这是一个具有 1.2k  类别的大量词汇的检测数据集，其长尾包含非常少的训练样本，这使其成为当前方法的一个具有挑战性的数据集。联合数据集通常会给标准检测器带来问题，并且需要开发特定的损失函数  [56]。然而，这个属性使它非常适合训练 MDETR：对于每个正类别，创建一个由图像和类名的文本版本组成的训练实例，并提供注释此类别的所有实例。对于每个负类别，提供类名和一组空注释。对于给定图像的推理，MDETR查询每个可能的类名，然后合并在每个文本提示上检测到的框集。这种推理方案在  GPU 上每个图像花费大约 10 秒。</p>
<p>作者在 LVIS 训练集的三个子集上微调 MDETR，每个子集分别包含 1%、10% 和 100% 的图像。作者确保对类别进行平衡抽样，以便 1%  集合包含来自每个类别的至少一个正例和一个负例。作者比较了两个baseline：第一个是在 LVIS 的完整训练集上专门训练的 Mask-RCNN。另一个是在 MSCOCO  上预训练的 DETR 模型，然后在 LVIS 训练集的各个子集上进行微调。结果如表 6 所示。根据最近关于大词汇量上下文中 AP 评估的建议  [8]，本文提供了边界框固定的 AP，它是通过限制每个类别而不是每个图像的检测数量获得的。即使每个类只有 1 个example，MDETR  也利用文本预训练，在稀有类别上优于完全微调的 DETR。然而，作者注意到，在对整个训练集进行完全微调的情况下，小对象的性能从 10% 数据的 20.9 AP  显着下降到 100% 的 7.5 AP，这可能是由于极端的类不平衡。作者预计重复因子采样等常见技术将在未来的工作中改善这种情况。</p>
<h2 id="论文中的表格"><a href="#论文中的表格" class="headerlink" title="论文中的表格"></a>论文中的表格</h2><p><img src="/2022/04/20/mdetr/1650389864482.png" alt="1650389864482"></p>
<p><img src="/2022/04/20/mdetr/1650389878579.png" alt="1650389878579"></p>
<p><img src="/2022/04/20/mdetr/1650389890063.png" alt="1650389890063"></p>
<p><img src="/2022/04/20/mdetr/1650389902875.png" alt="1650389902875"></p>
<p><img src="/2022/04/20/mdetr/1650389912182.png" alt="1650389912182"></p>
<p><img src="/2022/04/20/mdetr/1650389923875.png" alt="1650389923875"></p>
<p><img src="/2022/04/20/mdetr/1650389953648.png" alt="1650389953648"></p>
<p><img src="/2022/04/20/mdetr/1650389968938.png" alt="1650389968938"></p>
<p><img src="/2022/04/20/mdetr/1650389983585.png" alt="1650389983585"></p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>Reference Expression Comprehension</title>
    <url>/2022/03/05/rec/</url>
    <content><![CDATA[<h2 id="Abstract"><a href="#Abstract" class="headerlink" title="Abstract"></a>Abstract</h2><p>引用表达式理解 (Referring expression comprehension)：定位图像中的目标对象，该图像由用自然语言表达的引用表达式描述。</p>
<p>方法根据机制，分为编码视觉（visual）和文本（textual）模式。</p>
<p>本文特别研究了将图像和表达式联合嵌入到公共特征空间的常用方法。</p>
<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>引用表达式通常与三个任务相关联：生成、分割和理解</p>
<p>引用表达式生成（REG）：生成对图像中对象的判别描述。</p>
<ul>
<li>这有点像给图像添加字幕。但与添加字幕不同的是，REG更具体地针对图像中的对象或区域，且具有明确定义的评估指标：它需要明确地描述对象，以便系统轻松理解描述并推断定位图像中的正确区域。</li>
</ul>
<p>引用表达式分割（RES）：根据引用表达式对引用对象进行分割。</p>
<ul>
<li>传统的解决方案是将语言编码器嵌入到分割网络中，学习一个多模态张量以对分割掩码（segmentation mask）进行解码。</li>
</ul>
<p>引用表达式理解（REC）：REG的逆向任务，基于表达式定位图像中的对象，从一组区域建议中选择最佳区域。</p>
<p>REC困难的原因</p>
<ul>
<li>表达式长度可能会很长，需要处理更多信息，可能需要更多的推理步骤</li>
<li>图像是高维的，比纯文本更加嘈杂（noisier）</li>
<li>图像缺乏语言的结构和语法规则，没有像NLP任务中能够使用的如语法分析器等有效方便的工具</li>
</ul>
<p>类似REC的任务</p>
<ul>
<li>Visual Grounding：定位图像中的多个对象区域，这些区域对应于描述底层场景的句子中的多个名词短语。而引用表达式理解的目标是通过给定表达式找到最佳匹配区域。</li>
<li>Object Detection：目标检测使用预定义类别标签对固定对象进行分类，而REC使用自然语言表达式来引用对象。自然语言表达的方式更加实用，因为它会根据图像和文本的信息而变化，更适合实际应用场景。</li>
<li>REC的关键点：利用语言信息来区分目标对象和其他对象，尤其是同一类别的对象。</li>
</ul>
<p>REC要求模型充分理解文本信息中的自然语言表达，从而得到目标和关系。因此表达式和目标之间的匹配非常重要</p>
<p>本文结构</p>
<ol>
<li>概述</li>
<li>七类REC方法<ul>
<li>联合嵌入方法</li>
<li>基于模块的方法</li>
<li>基于图的方法</li>
<li>使用外部解析器的方法</li>
<li>弱监督方法</li>
<li>一阶段方法</li>
<li>视觉语言预训练方法</li>
</ul>
</li>
<li>当前可用的 REC 数据集和用于引用表达式的评估指标</li>
<li>REC 未来可能的方向</li>
</ol>
<h2 id="Methods"><a href="#Methods" class="headerlink" title="Methods"></a>Methods</h2><p>现有方法概述图</p>
<p><img src="/2022/03/05/rec/1647784718443.png" alt="1647784718443"></p>
<h3 id="Joint-embedding-approaches"><a href="#Joint-embedding-approaches" class="headerlink" title="Joint embedding approaches"></a>Joint embedding approaches</h3><p>动机：执行REC的第一步是将图像区域和引用表达式编码到相同的向量空间中，对于图像的表示，CNN可以通过将输入图像嵌入到固定长度的向量中来生成丰富的图像表示以用于视觉任务。对于文本表示，LSTM被广泛用于句子特征编码。</p>
<p>方法：</p>
<ul>
<li><p>生成任务中用预训练的CNN对图像中的目标对象以及上下文进行建模，将其作为输入提供给LSTM进行生成。</p>
</li>
<li><p>理解任务中LSTM将区域级的CNN特征图和一个词向量作为输入，在每个时间步的目标是最大化给定参考区域的表达可能性。</p>
</li>
<li><p>Mao et al. [18]利用 CNN 模型从边界框，利用LSTM 网络中提取视觉特征来生成表达式。利用最大似然训练，其中引用表达的概率仅在参考区域高，在所有其他区域低。提出了一种最大互信息（MMI）方法，如果生成的参考表达式也可以由图像中的其他对象生成，则模型将受到惩罚。这同时解决了逆问题：根据表达式定位图像中的目标（引用表达式理解）。</p>
<p><img src="/2022/03/05/rec/1647784848118.png" alt="1647784848118"></p>
</li>
<li><p>Yu et al. [19]引入了视觉比较方法（Visdif）来将目标物体与周围物体区分开来，而非提取整个图像的CNN特征。Visdif专注于目标类型和表达式之间的关系，利用对象之间的视觉差异来表示视觉上下文以提升性能。这是重要的，因为生成过程中必须考虑相似对象的视觉特征，以便选择最不同的方面进行描述。</p>
</li>
<li><p>Nagaraja et al. [20] 将【18】的工作进行了拓展，能够通过多实例学习（MIL）来学习上下文区域（ since annotations for context objects are generally not available for training.）他们使用 MIL 对象函数来训练 LSTM，整个图像也被视为潜在的上下文区域。输入词嵌入和区域特征，以及整个图像的 CNN 特征和边界框特征作为上下文。使用 LSTM 将引用表达式的概率建模为区域和上下文区域的函数，将引用表达式映射到区域及其支持的上下文区域。这两个训练目标函数的公式类似于 MISVM 和  mi-SVM [48]。</p>
</li>
<li><p>Hu et al. [21] 提出了一种新的空间上下文循环卷积网络（SCRC）模型。该模型以自然语言查询、局部图像描述符、空间配置和全局场景级上下文作为输入，对候选区域的输出进行评分。受长期递归卷积网络（LRCN）的启发，SCRC 采用双层  LSTM 网络结构，其中嵌入的文本序列和视觉特征作为第一层和第二层的输入。由于数据集中缺少带解释的对象边界框和描述对，SCRC  对图像字幕域进行预处理，然后将其适应自然语言对象检索域，从而可以将视觉语言知识从之前的任务中迁移出来可以在后面的任务中转移。通过这种预训练和自适应的过程可以改进性能，避免过拟合。</p>
</li>
<li><p>Luo et al. [22] 利用学习的理解模型来指导模型生成更好的引用表达。理解模块对人工生成的表达式，通过代理方法进行训练。代理方法的训练受到生成对抗网络 [49]、[50]  中的生成器-判别器结构的启发。然后他们使用生成和重新排序管道，其中理解模型充当“批评者”来判断表达式是否可以正确引用。具体而言，这一过程首先生成候选表达式，然后根据理解任务的表现选择最佳表达式。</p>
</li>
<li><p>Zhang et al. [23] 提出了一种称为变分上下文（VC）的变分贝叶斯方法，适用了所指对象和上下文之间的关系。无论是referent还是context都会影响对另一个的后验分布的估计，因此context的搜索空间会减少。对于每个区域，VC  估计一个粗略的上下文，这将有助于细化所指对象的真实定位。变分下界可以通过参考和上下文之间的交互来解释：给定它们中的任何一个都可以帮助定位另一个，因此它有望降低上下文的复杂性。与正式的基于 MIL 的方法相比，VC 使用该框架来近似具有弱监督的组合上下文配置。</p>
</li>
<li><p>总结：</p>
<ul>
<li>“VisDif”训练了一个 Fast-RCNN  检测器并在此基础上进行实验。结果表明，这种相关对象的组合比以前的工作产生了更好的结果，以减少生成过程中的歧义。一个弱点是该模型依赖于检测性能。</li>
<li>Nagaraja 的工作【20】表明对象之间的上下文建模提供了比仅建模对象属性更好的性能。他们的模型可以识别参考区域及其支持上下文区域。“SCRC”方法有效地利用了局部和全局信息，在  ReferItGame 数据集和 Flickr30K  实体数据集上比以前的基线方法显着提高了性能。结果表明，空间配置和全局上下文的结合可以提高性能。</li>
<li>罗的工作【22】改进了多个基准数据集上的引用表达式生成。“VaruContext”还将模型扩展到无监督设置，在  ReferItGame 上的性能有所提高。在各种基准上进行的大量实验表明，在有监督和无监督设置中，过去的方法都在不断改进。</li>
</ul>
</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：简洁有效</li>
<li>缺点：受到单一向量表示的限制，这些表示忽略了复合语言表达以及图像中的复杂结构。它们按顺序对表达式进行编码，而忽略表达式中的依赖关系。</li>
</ul>
<h3 id="Attention-mechanism"><a href="#Attention-mechanism" class="headerlink" title="Attention mechanism"></a>Attention mechanism</h3><p>动机：允许在处理大量信息时专注于输入的某一部分。注意力机制能够在视觉和文本信息之间建立元素连接，以便在对文本中的每个单词进行编码时可以利用来自某些特定图像区域（即感兴趣区域）的信息，反之亦然，从而在语义上得到丰富的视觉和文本表示。</p>
<p>方法：</p>
<ul>
<li>Zhuang et al. [30] 提出了一种并行注意 (PLAN)  方法，该方法在图像级和区域级包含两个并行注意过程推理。给定一个引用表达式，图像级注意力模块通过反复关注不同的图像块来编码整个全局信息和引用语言描述，这允许模型利用相关的上下文信息。而区域级注意模块根据语言信息反复关注对象建议，并获得每个建议的表示。然后，将全局图像和目标提议的表示放入匹配模块中，以计算每个目标提议的匹配概率。</li>
<li>Deng et al. [31] 将 REG 制定为三个连续的子任务，即 1）确定查询中的主要焦点； 2）理解图片中的概念；  3）定位最相关的对象，他们提出了一种累积注意力（AccumAtt）机制来共同解决这三个任务。具体来说，AccumAtt 采用了三个注意力模块来分别处理上述三个子任务中的注意力问题。为了捕捉这些注意力问题之间的相关性，AccumAtt  进一步采用累积过程将所有类型的注意力组合在一起并循环提炼它们，其中每种类型的注意力将在计算其他两种注意力时用作指导。最后，AccumAtt  计算每个对象建议的参与表示与来自查询和图像注意模块的细化表示之间的匹配分数，以定位目标对象。</li>
<li>注意力机制能够与其他模型相集合，如后面的介绍的模块化网络、图模型等。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：通过在CNN-LSTM框架上应用注意力机制，AccumAtt [31] 和 PLAN [30] 在主流数据集上都取得了显著进步。PLAN 中的实验验证了在多个级别（即区域级别和图像级别）执行注意力的重要性。此外，AccumAtt  的消融研究显示了使用多个注意力模块来处理不同模式并累积多轮注意力分数的有效性。此外，这两种方法的可视化结果也为理解注意力过程提供了有价值的解释。</li>
<li>缺点：这些基于注意力的方法不能保证正确的注意力分配，因为数据集通常没有提供相应的解释。</li>
</ul>
<h3 id="Modular-based-Models"><a href="#Modular-based-Models" class="headerlink" title="Modular-based Models"></a>Modular-based Models</h3><p>动机：模块化网络已经成功应用于许多任务。在引用表达式的任务中，以前的工作主要简单地将所有特征连接起来作为输入，并使用单个 LSTM  对整个表达式进行编码或解码，而忽略了表达式中提供的不同信息之间的差异。在早期的工作中，expression parsing with attention需要适用外部语言解析器 [56]  进行分解，但近期的研究建议学习端到端分解。通过将表达式分解为不同的组件，并通过模块化网络将每个组件与对应的视觉区域进行匹配，从而实现一步推理。框架图如下</p>
<img src="1647829954766.png" alt="1647829954766" style="zoom:67%;">

<p>方法：</p>
<ul>
<li>Hu et al. [27] 提出了组合模块化网络 (CMN)，它可以端到端地学习语言分析和视觉推理。  CMN由定位模块和关系模块组成。定位模块将主题或对象与每个图像区域匹配并返回一元分数。关系模块将关系与一对区域匹配并返回成对分数。给定一张图像和一个表情，CMNs首先将表情解析为具有软注意力的主题、关系和对象。然后将这两个模块结合起来考虑区域的局部特征和区域之间的相互作用。</li>
<li>Yu et al. [28] 提出了模块化注意力网络（MAttNet）。 MAttNet  将自然语言表达分解为三个模块化元素，这些组件与主题外观、位置和与其他对象的关系有关。主题模块处理类别、颜色和其他属性，位置模块处理绝对和相对位置，关系模块处理主题对象关系。每个模块都有不同的结构，在自己的模块空间中学习参数而不影响其他模块。  MAttNet 不依赖外部语言解析器，而是通过基于软注意力的机制学习自动解析表达式。然后计算三个视觉模块的匹配分数来衡量对象和表达式之间的兼容性。</li>
<li>Liu et al. [32] 提出了一种跨模态注意力引导擦除（CM-Att-Erase）策略，用于训练参考表达理解模型。 CM-Att-Erase 采用 MAttNet  作为其骨干模型。通过在训练期间从文本或视觉信息中删除参与度最高的部分，CM-Att-Erase  鼓励模型发现更多用于视觉-文本对齐的潜在线索，从而实现全面的跨模态对应。此外，CM-Att-Erase 对每个模块的设计进行了多次修改。具体来说，与  MAttNet 仅采用文本信息来学习单词级注意力和模块级注意力不同，CM-Att-Erase 进一步考虑了全局图像特征。此外，CM-Att-Erase  将位置和关系模块制定为具有句子感知注意力的统一结构，该结构考虑多个上下文对象，并关注最重要的对象。CM-Att-Erase  提出了一种基于注意力的擦除过程，通过丢弃来自文本或视觉域的最主要信息，以在线生成困难的训练样本，并驱动模型发现互补的文本-视觉对应关系。这个过程鼓励模型探索更难的例子，消除数据集偏差。</li>
</ul>
<p>优缺点：</p>
<ul>
<li>优点：在 RefCOCOg 数据集上的实验表明，虽然是弱监督，但 CMN 不仅可以正确定位主题区域，而且可以为对象实体找到合理的区域。MAttNet 具有显着的优越性能，在边界框的定位上实现了约 10% 的改进，像素分割精度几乎翻了一番。CM-Att-Erase 在 MAttNet 之上又在RefCOCO/+/g数据集的真实对象建议和检测对象建议上取得了显著进步。在没有跨模态擦除训练策略的情况下，它仍然明显优于 MAttNet。基于模块化的模型（例如  CM-Att-Erase）比更复杂的方法（例如基于图形的模型）取得了更好的结果。这表明该系统在现有数据集有限的情况下无需复杂的推理过程即可获得良好的性能。</li>
<li>缺点：CMN 依赖于有限的句法信息，并且由于缺乏对语言递归的建模，无法追踪多个支持的对象。模块化网络过度简化了语言结构并忽略了视觉对象之间的关系。换言之，语言和区域特征是在彼此不了解的情况下独立学习或设计的，这使得两种模式的特征难以相互适应，尤其是在复杂表达的情况下。因此，当表达式过于复杂时，此方法不适用。</li>
</ul>
<h3 id="Graph-based-models"><a href="#Graph-based-models" class="headerlink" title="Graph based models"></a>Graph based models</h3><p>动机：对于引用表达式任务，解决这个问题的关键是学习一个能够适应所用表达式的区分对象特征。为了避免歧义，这个表达式通常不仅描述对象本身的属性，还描述对象与其邻居之间的关系。以前的方法只处理对象，或者只研究对象之间的一阶关系，而不考虑表达式的潜在复杂性。因此，提出了基于图的方法，其中节点可以突出显示相关对象，而边缘则用于识别表达式中存在的对象关系。因为图注意力可以在图上建立参考和其他支持线索，所以参考表达决策可以是可视的和可解释的，如下图。</p>
<p><img src="/2022/03/05/rec/1647839167761.png" alt="1647839167761"></p>
<p>方法</p>
<ul>
<li>Wang et al. [33] 提出了一种语言引导的图注意力网络（LGRAN）。该网络由三个模块组成：语言自注意力模块、语言引导图注意力模块和匹配模块。语言自注意力模块采用自注意力方法分解描述主体、类内关系和类间关系三部分的表达，学习相应的表示。语言引导的图注意模块，构造一个有向图的候选对象，强调节点、类内边和类间边，最终得到每个对象的三种表达相关的表示。匹配模块计算表达式到对象的匹配分数。LGRAN可以在参与图的基础上动态丰富对象表示，以更好地适应引用表达式。</li>
<li>Yang et al. [34] 提出了一个动态图注意网络（DGA），它可以对图像和表情之间的交互进行多步推理。给定一个表达式和图像，DGA  构建图像中对象的图，其中节点和边对应于对象和关系，然后将表达式的语言表示合并到图中。之后，分析器通过探索表达式的语言结构来学习推理的语言指导。在预测的视觉推理过程的指导下，DGA  在图上逐步进行动态推理。视觉推理过程是由组成表达式组成的序列。最后，DGA 计算复合对象和引用表达式之间的匹配分数。</li>
<li>Liu et al. [35] 利用语言引导的图形表示来捕获全局上下文及其接地对象的相互关系。此外，他们还提出了一种名为 CMCC 的上下文感知跨模态图匹配策略。CMCC由四个主要模块组成：主干网络提取基本语言和视觉特征，短语图网络对句子描述中的短语进行编码，视觉对象图网络计算对象建议特征，图相似度网络预测短语和对象之间的全局匹配建议。</li>
<li>Yang et al. [36] 提出了一个跨模态关系推理网络（CMRIN），它包含一个跨模态关系提取器（CMRE）和一个门控图卷积网络（GGCN）。 CMRE  自适应地提取所有需要的信息，以构建具有跨模态注意力的语言引导视觉图。 GGCN 融合来自不同模式的信息并传播多模式信息以计算语义上下文。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：LGRANs 使参考表达决策既可视化又可解释。在RefCOCO/+/g上的结果具有一定的优势。DGA实验结果表明，该方法不仅可以显着超越现有的所有算法，而且为复杂语言描述中物体的逐步定位提供了视觉证据。 CMCC 采用两阶段策略训练整个图神经网络，并在  Flickr30K 实体基准上对其进行评估，达到了SOTA，并以较大优势超越了其他方法。</li>
<li>缺点：本文未提及</li>
</ul>
<h3 id="External-parser"><a href="#External-parser" class="headerlink" title="External parser"></a>External parser</h3><p>动机：引用表达式利用目标对象的辨别特征及其与其他对象的相对位置来消除目标对象的歧义。引用表达式的语言结构与引用表达式的解析过程之间存在对应关系。但现有方法过度简化了语言的复合性质，将其简化为单个句子，或主语、谓语和宾语短语的综合分数。尽管其中一些使用词汇级别的注意力机制来关注信息语言部分，但与人类级别的推理相比，它们的推理仍然很粗糙。更严重的是，这个粗略的基础分数很容易偏向于学习某种视觉语言模型而不是视觉推理。这个问题在许多端到端视觉语言嵌入框架中使用的其他任务中反复发现，例如  VQA 和图像字幕。因此，借助外部语言解析器，基于语法的方法可以帮助定位表达式中提到的目标对象和辅助支持对象。如下图</p>
<p><img src="/2022/03/05/rec/1647841596649.png" alt="1647841596649"></p>
<p>方法</p>
<ul>
<li>Cirik et al. [38] 提出了GroundNet，这是第一个使用外部句法分析来解析输入参考表达式的方法，它有助于目标对象和辅助支持对象的定位。当给定输入表达式的解析树时，它们将树中出现的句法组件和关系显式映射到由神经模块组成的组合图，该图定义了执行定位的架构。</li>
<li>Liu et al. [37] 开发了一个神经模块树网络（NMTree），它在自然语言句子的依赖解析树（DPT）[57]中积累了基础置信度来定位目标区域。给定一个自然语言表达式作为输入，他们首先通过  Dependency Parsing Tree、Bidirectional Tree LSTM 和 Module Assembler 将其转换为 NMTree。  NMTree由三个简单的神经模块组成：叶子和根的Single，内部节点是Sum和Comp。每个模块计算一个grounding score并以自下而上的方式累积。最终结果grounding score是根节点的输出分数。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：与之前的研究相比，GroundNet 具有更好的可解释性。首先，它可以确定引用表达点的哪个短语指向图像中的哪个对象，其次，它可以跟踪网络如何确定目标对象的位置。  GroundNet 在目标对象的定位方面保持了相当的性能。定性结果和人工评估表明，NMTree 理解更细粒度和可解释的语言复合推理，并具有更好的性能。</li>
<li>缺点：使用解析器处理和转换表达式时，可能会删除一些限定词，而这些词汇（例如名词的不定性）可能有助于确定对象。</li>
</ul>
<h3 id="Vision-Language-pre-training"><a href="#Vision-Language-pre-training" class="headerlink" title="Vision-Language pre-training"></a>Vision-Language pre-training</h3><p>动机：视觉语言推理需要理解【视觉内容】、【语言语义】以及【跨模态对齐和关系】。该领域的主要策略是使用【专门为视觉或语言任务设计】并在【大规模视觉或语言数据集】上进行【预训练】的单独的【视觉】和【语言】模型，但是缺乏统一的基础来学习视觉概念之间的联合表示和语言语义，当配对的视觉语言数据有限或有偏差时，通常会导致泛化能力差。因此，拥有一个联合预训练的视觉和语言模型来为下游视觉和语言任务提供统一的知识表示可能是有益的。如下图</p>
<p><img src="/2022/03/05/rec/1647849604588.png" alt="1647849604588"></p>
<p>方法</p>
<ul>
<li>Lu et al. [39] 提出 ViLBERT  框架在大规模视觉语言数据集上执行视觉和语言信息的联合建模，并利用预训练模型来解决包括引用表达理解在内的多种视觉语言任务。具体来说，ViLBERT  包含两个分支，一个用于语言信息，另一个用于视觉信息。每个流由两个不同的层组成：Transformer Layer (TRM) 和提议的  Co-Attentional Transformer Layer (Co-TRM)，后者用于连接两个分支。为了学习联合视觉语言表示，ViLBERT 首先通过预训练对象检测器获得输入图像中的一组对象建议。然后，Co-TRM  层计算每个对象提议和输入文本中每个标记之间的交叉模态注意力分数。预训练是在概念字幕上执行的，概念字幕是一个包含 300  万个图像字幕对的大规模图像字幕数据集。使用了两个预训练任务：1）Masked Multi-modal  Learning，其中模型必须在给定观察输入的情况下重建图像区域类别或掩码输入的单词；  2）多模态对齐预测，其中模型必须预测标题是否描述了图像内容。在对引用表达理解任务进行微调时，他们只需将每个图像区域的最终表示传递到一个学习的线性层以预测匹配分数。</li>
<li>与 ViLBERT 不同，ViLBERT 对不同的输入模态采用单独的分支，并交替执行跨模态注意和自注意以获得联合表示，Su et al. [40] 提出在一个名为  VL-BERT 的统一单分支框架中处理图像和文本输入。在 VLBERT 中，图像中的每个 object proposal 或文本中的每个单词都被视为输入  token 进入一堆 Transformer Layers，它学习在所有类型的 token 之间建立连接，没有任何限制，从而实现 co-attention 和  self -注意力是同时进行的。和 ViLBERT 一样，VL-BERT 的预训练也是在 Conceptual Captioning 上进行的，而  VL-BERT 仅采用 Masked Multimodal Learning 作为预训练任务。在对引用表达理解任务进行微调时，VL-BERT 遵循与  ViLBERT 相同的策略。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：通过视觉和语言预训练，ViLBERT 和 VL-BERT 在 RefCOCO+ 数据集上实现了最佳性能，并大大优于以前的最先进结果。</li>
<li>缺点：模型太大了，需要大量计算资源</li>
</ul>
<h3 id="One-stage-approaches"><a href="#One-stage-approaches" class="headerlink" title="One stage approaches"></a>One stage approaches</h3><p>动机：大多数引用表达理解方法都遵循一个常见的两阶段管道：i）使用像  Faster RCNN 这样的对象检测器来根据输入图像生成一组对象建议； ii)  计算每个建议和引用表达式之间的匹配分数，然后采用得分最高的建议作为模型预测结果。然而，这些两阶段解决方案有两个需要注意的问题。首先，对象检测阶段引入了额外的计算。此外，由于在阶段i中通常会生成数十甚至数百个proposal，需要大量计算来提取所有检测到的object proposal的特征，这使得实时引用表达理解不可行。其次，在第 i 阶段获得的对象建议的质量会严重影响最终的性能。生成的建议与真实对象之间通常存在很多错位，这可能会阻碍阶段 ii 的学习过程。更关键的是，目标检测器甚至可能在第 i 阶段错过目标（这很常见），在这种情况下，第  ii 阶段实际上是在浪费时间，因为 VG 任务永远不会成功。如下图</p>
<p><img src="/2022/03/05/rec/1647854685200.png" alt="1647854685200"></p>
<p>方法</p>
<ul>
<li>Yang et al. [14] 发了一种基于 YOLOv3 [58] 的单阶段引用表达理解方法（FAOA）。他们通过 Darknet [58] 从图像中获取特征金字塔，并通过  BERT  从引用表达式中提取语言特征。然后，对于图像特征金字塔中的每一层，将语言特征和图像特征连接在一起，并馈入预测头，以预测目标对象的边界框。为了使模型具有位置感，由像素的归一化坐标组成的空间特征也被输入到预测头中。</li>
<li>Sadhu et al. [15] 提出了一种将检测网络和接地系统结合在一起的零样本Grounding (ZSG)  方法。与[14]不同的是，他们采用带有FPN的ResNet来提取图像特征金字塔，并使用Bi-LSTM来提取语言表示。此外，他们没有采用二元交叉熵，而是采用  Focal Loss 对目标对象内部/外部的区域进行分类。</li>
<li>Luo et al. [16] 提出了一种多任务协作网络（MCN），它在一个阶段的管道中共同解决了引用表达理解和引用表达分割。具体来说，他们试图通过  1）一致性能量最大化来解决两个引用表达任务之间的预测冲突问题，这迫使两个任务在输入图像上具有相似的注意力； 2) Adaptive Soft  Non-Located Suppression，根据引用表达式理解分支中的预测，软抑制引用表达式分割分支中无关区域的响应。</li>
<li>Liao et al. [17] 提出了一种实时跨模态相关过滤方法 (RCCF)，将引用表达理解任务重新表述为相关过滤过程。 RCCF 计算参考对象和图像之间的相关图以预测对象中心。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：与两阶段参考表达理解方法相比，一阶段方法具有明显更快的推理速度。事实上，两阶段方法中最耗时的部分是目标提议的生成和它们的特征提取，而在单阶段方法中，这两个步骤不是必需的，从而导致了一个数量级的加速。此外，FAOA  和 MCN 通过几个强大的两阶段基线（包括先前最先进的模型 MAttNet）实现了具有竞争力甚至更好的性能。</li>
<li>缺点：本文未提及</li>
</ul>
<h3 id="Weakly-supervised"><a href="#Weakly-supervised" class="headerlink" title="Weakly supervised"></a>Weakly supervised</h3><p>动机：传统上，以监督方式训练引用表达模型需要大量手动解释来指示输入表达和对象之间的映射。此外，监督模型只能在有限的训练数据中处理某些类型的接地，不能满足实际应用的需要。因此，希望在没有或很少监督的情况下从数据中学习。弱监督参考表达是指在训练阶段参考对象与查询之间的映射未知时，根据语言查询在图像中定位对象，如下图。</p>
<p><img src="/2022/03/05/rec/1647855003528.png" alt="1647855003528"></p>
<p>方法：</p>
<ul>
<li>Rohrbach et al. [41] 提出了 GroundR，它通过使用注意力机制重建给定的短语来学习grounding。在训练期间，GroundR  包含两个阶段。第一阶段的目标是在根据短语选择最相关的区域或潜在的多个区域，然后第二阶段尝试从它在第一阶段关注的区域中重建相同的短语。在测试时，GroundR  移除了短语重建阶段，仅使用第一部分进行grounding。不足的是，这一方法忽略了位置和上下文与参照对象的区分信息，无法区分一个特定类别的多个对象并列在一起的特定对象。</li>
<li>在GroundR的基础上，Chen et al. [42] 设计了一个知识辅助一致性网络（KAC Net），它重建了输入表达式和区域的信息。 KAC Net  由两个分支组成：视觉一致性分支和语言一致性分支。视觉一致性分支旨在预测和对齐与表达相关的区域的位置参数，语言一致性分支旨在从区域重构输入表达。为了利用来自视觉特征提取器的互补知识，使用基于知识的池化门来专注于视觉和语言重建。</li>
<li>与从一组候选区域中选择最佳区域不同，Zhao et al. [43] 提出了一种多尺度锚定变压器网络（MATN），它在没有边界框监督信息的情况下定位图像中的自由格式文本短语。 MATN  由一个多尺度对应网络和一个从一组候选区域诱导的锚约束组成。该模型是通过同时最小化单个图像中不同短语之间的对比度重建误差和具有相似短语的多个图像之间的一组三重损失来训练的。</li>
<li>Liu et al. [44] 提出一个自适应重建网络（ARN），这一方法采用自适应接地和协同重建的方法建立图像建议和查询之间的对应关系。它由特征编码自适应接地和协同重建组成。具体来说，他们首先提取主题、位置和上下文特征来表示建议和查询。然后使用自适应grounding模块通过分层注意模型计算每个建议和查询之间的匹配分数。最后，基于注意力分数和提议特征，他们使用语言重建损失、自适应重建损失和属性分类损失的协同损失来重建输入查询。</li>
</ul>
<p>优缺点</p>
<ul>
<li>优点：GroundR 在 Flickr 30k Entities 和 ReferItGame 数据集比最先进的技术高 4.5% 和 10.6%。而 KAC Net 的准确率分别提高了 9.78% 和 5.13%。大量实验表明，MATN 在显着程度上优于最先进的弱监督短语定位方法。 ARN  的自适应机制有助于模型缓解不同引用表达之间的差异。量化分析的结果表明，该方法可以更好地处理同一类别的多个对象同时存在的问题。</li>
<li>缺点：本文未提及。</li>
</ul>
<h2 id="Datasets"><a href="#Datasets" class="headerlink" title="Datasets"></a>Datasets</h2><h3 id="ReferItGame"><a href="#ReferItGame" class="headerlink" title="ReferItGame"></a>ReferItGame</h3><p>ReferItGame 包含 130,525 个表达式，涉及 19,894 张图像中的 96,654 个不同对象。</p>
<p>数据集的构建基于 ImageCLEF IAPR [69] 图像检索数据集，以及包含分割的 SAIAPR TC-12 [70] 扩展，其中包括将每个图像分割为多个区域。</p>
<p>该数据集是在一个两人游戏中收集的，其中第一个玩家根据指定的目标对象编写一个引用表达式。第二个玩家只显示图像和引用的表达，并要求点击所描述对象的正确位置。如果点击在目标对象区域，则两个玩家都获得游戏点数并交换下一个图像。如果点击不正确，他们将保持当前角色。然而，这个数据集中的图像有时只包含给定类别的一个对象，因此说话者倾向于使用简短的描述并关注上下文而不是对象。</p>
<h3 id="RefCOCO-amp-RefCOCO"><a href="#RefCOCO-amp-RefCOCO" class="headerlink" title="RefCOCO &amp; RefCOCO+"></a>RefCOCO &amp; RefCOCO+</h3><p>这些数据集是使用  ReferIt Game [59] 收集的。作者进一步收集了 MSCOCO [71] 图像上的 RefCOCO 和 RefCOCO+ 数据集。 RefCOCO  和 RefCOCO+ 数据集各包含 50,000个平均有 3 个引用表达式的引用对象。RefCOCO 和 RefCOCO+ 的主要区别在于，在 RefCOCO+ 中，不允许玩家使用位置词，因此将引用表达集中在纯粹基于外观的描述上。</p>
<p>RefCOCO 包含 19,994 张图像中 50,000 个对象的 142,209 个引用表达式，RefCOCO+ 包含 19,992 张图像中 49,856  个对象的 141,564 个表达式 [19]。数据集分为训练集、验证集、测试集 A 和测试集 B。测试集 A 包含多人，测试集 B 包含所有其他对象的多个实例。</p>
<h3 id="RefCOCOg"><a href="#RefCOCOg" class="headerlink" title="RefCOCOg"></a>RefCOCOg</h3><p>数据集是在 Amazon Mechanical Turk 上收集的，与字幕相似，它们更长、更复杂，通常是整个句子而不是短语。</p>
<p>Mao et al. [18] 在非交互环境中构建 Mechanical Turk 任务，需要一组标注者为 MSCOCO  图像中的对象编写自然语言引用表达式，然后要求另一组标注者针对给定的引用表达式单击指定对象如果单击与正确的对象重叠，则将引用表达式视为有效并添加到数据集中。如果不是，则为该对象收集另一个引用表达式。此表达式生成和验证任务交互重复 3 次。</p>
<p>数据集包含 26,711 张图像中 54,822 个对象的 104,560 个表达式。平均每个物体有1.91个表情，每张图片有3.91个表情。 RefCOCO  表达式的平均长度为 3.61，而 RefCOCO+ 的平均长度为 3.53，RefCOCOg 的平均长度为 8.43 个单词。拆分为 85,474 和  9,536个用于训练和验证的expression-referent对。</p>
<h3 id="Flickr-30k-entities"><a href="#Flickr-30k-entities" class="headerlink" title="Flickr 30k entities"></a>Flickr 30k entities</h3><p>该数据集用于短语定位，即给定图像和准确描述它的标题，预测该句子中特定实体提及的边界框。在 158,915 个 Flickr30k 字幕中中共有 513,644 个实体或场景提及（平均每个字幕 3.2 个），他们被链接到 244,035 个共同指代链（平均每个图像 7.7 个）。在  31,783 张图像中共有 275,775 个边界框（平均每张图像 8.7 个）。</p>
<p>注释过程包括两个主要阶段：共同引用决议，或形成引用相同实体的共同引用链，以及生成链的边界框注释。这一工作流程有两个优点：首先，识别共同引用提及有助于减少冗余并节省绘图工作；其次，共同参考注释具有内在价值，例如，用于训练交叉字幕（cross-caption）共同参考模型（co-reference model）。</p>
<h3 id="GuessWhat"><a href="#GuessWhat" class="headerlink" title="GuessWhat?!"></a>GuessWhat?!</h3><p>GuessWhat 数据集 [61]  是一个合作的两人游戏，其中两个玩家都看到具有多个对象的丰富视觉场景的图片。一名玩家——神谕者——被随机分配到场景中的一个对象。另一个玩家——提问者——不知道这个对象，他们的目标是通过向神谕询问一系列是否的问题来定位该对象。</p>
<p>原始的数据集由 155,280 个对话组成，其中包含 66,537 个图像和 134,073 个对象的 821,889  个问题/答案对。答案分别为 52.2% 否、45.6% 是和 2.2% N/A。平均而言，每个对话有 5.2 个问题，每张图片有 2.3 个对话。对话总共包含  3,986,192 个单词的标记，由 11,465 个至少出现一次的不同单词和至少 3 次出现的 5,444 个单词组成。</p>
<h3 id="CLEVR-Ref"><a href="#CLEVR-Ref" class="headerlink" title="CLEVR-Ref+"></a>CLEVR-Ref+</h3><p>前面提到的数据集是针对复杂的真实图像中的 REC  问题收集的。然而，有证据表明这些数据集是有偏差的。简单地选择突出的前景对象将产生比随机猜测高得多的baseline。这让人对当前 REC  模型的真正能力产生怀疑。此外，这些数据集中没有中间推理过程的解释，因此它们的性能评估只能取决于最终预测（例如边界框），而不考虑 REC 模型的中间推理过程。</p>
<p>CLEVR-Ref+ 考虑了一种不同的情况，他们使用合成图像，其中对象被放置在 2D  平面上，并且在形状、颜色、大小和材料方面只有很少的选择。引用表达式也是使用精心设计的模板合成的，表达式可能非常复杂，需要很强的推理能力。与统一的采样策略一起，这种设计可以减轻数据集偏差并揭示模型的推理能力。CLEVR-Ref+ 在训练集中包含 70K 图像，在验证和测试集中包含 15K 图像，每张图像都与 10 个引用表达式相关联。</p>
<h3 id="Cops-Ref"><a href="#Cops-Ref" class="headerlink" title="Cops-Ref"></a>Cops-Ref</h3><p>在一些当前流行的引用表达式数据集中，数据通常只描述对象的一些简单的独特属性，图像包含有限的分散注意力的信息，无法为评估模型的推理能力提供理想的测试平台。为了缓解这些问题，Cops-Ref  [63] 采用了一种新颖的表达引擎来渲染各种推理逻辑，可以灵活地与丰富的视觉属性相结合，以生成具有不同成分的表情。此外，提出了一种新的测试设置，以更好地利用表达式中体现的完整推理链。  75299幅图像中共有148712个表情和1307885个区域。表达式的平均长度为 14.4，词汇量为 1,596。分别有  119,603/16,524/12,586 个表达式用于训练/验证/测试。</p>
<h3 id="Embodied-Referring-Expression-datasets"><a href="#Embodied-Referring-Expression-datasets" class="headerlink" title="Embodied Referring Expression datasets"></a>Embodied Referring Expression datasets</h3><p>除了静态图像上的引用表达式数据集外，还有一些数据集将任务转移到了实体环境中。</p>
<ul>
<li>Touchdown-SDR 是一个使用真实视觉观察进行空间推理的数据集。智能体必须首先在真实的视觉城市环境中遵循导航指令，然后识别用自然语言描述的位置，并在目标位置找到隐藏的对象。与正常的指称表达任务不同，Touchdown-SDR 的目标是描述特定的位置，而不是区分它们。</li>
<li>Refer360° 数据集中，指令是从场景的局部视场(FOV)的角度给出的，可以动态地改变视场以探索场景。该数据集的一个重要特征是，目标位置不是对象，而是场景中的任何点。数据集由17,137个指令序列组成(每个场景8.57个)，描述了来自SUN360数据集中2,000个全景场景的随机分布的目标位置。</li>
<li>REVERIE  将视觉和语言导航以及参考表达识别结合到一项任务中。在逼真的 3D 环境中，系统被要求导航到目标位置并指出真实室内环境中的远程对象。该数据集由来自 86  座建筑物的 10,318 张全景图像组成，包含 4,140 个目标对象和 21,702 条指令。</li>
</ul>
<h2 id="Evaluation-Measures"><a href="#Evaluation-Measures" class="headerlink" title="Evaluation Measures"></a>Evaluation Measures</h2><p>评估是通过计算引用表达式的真实边界框和顶部预测框之间的交并比  (IoU) 来执行的。如果 IoU 大于 0.5，则认为预测为真阳性。否则，我们将其视为fp。然后将所有引用表达式的分数取平均。</p>
<h2 id="Results-of-existing-methods"><a href="#Results-of-existing-methods" class="headerlink" title="Results of existing methods"></a>Results of existing methods</h2><p>监督方法在 ReferItGame 数据集上的表现明显优于弱监督方法。从 Flickr 30k 实体数据集（在表 VI 中）和 Cops-Ref 数据集（在表  VII 中）的结果也可以得出类似的结论。然而，弱监督方法可能更实用，因为大规模完全注释的引用表达数据集成本高昂。</p>
<p>对于有监督的方法，可以看到，在最流行的数据集  RefCOCO 和 RefCOCO+ 上，基于预训练的方法 [39]、[40]  始终产生最好的性能并远超过其他方法。这表明，在大规模的视觉和语言数据集上进行预训练确实可以提高学习到的联合视觉和语言表征的泛化能力。</p>
<p>其他一些方法，如基于注意力的方法 [30]、[31]、基于图的方法 [34]、[36] 或基于模块化的方法  [28]、[32]，其性能明显优于大多数遵循CNN/LSTM 或联合嵌入框架。这证明了注意力机制（通常是图注意力、模块化注意力）在挖掘多模态信息的特征关系方面的有效性。</p>
<p>这些方法在人工标记的对象建议上的性能明显高于检测到的对象建议的性能。这表明检测到的目标提议引入的噪声信息严重阻碍了性能。从其他数据集的结果中可以得出类似的结论。</p>
<p>另一方面，一些单阶段方法[16]、[17]虽然速度快得多，但由于使用了先进的骨干模型、精心设计的学习方案和标签分配策略，并且可能还使用了额外的注释[16]，因此仍然比许多复杂的基于注意力或基于模块的两阶段方法具有竞争力。</p>
<h2 id="DISCUSSION-AND-FUTURE-DIRECTIONS"><a href="#DISCUSSION-AND-FUTURE-DIRECTIONS" class="headerlink" title="DISCUSSION AND FUTURE DIRECTIONS"></a>DISCUSSION AND FUTURE DIRECTIONS</h2><p>REC仍然存在挑战，因为需要对视觉和文本信息进行联合推理。</p>
<p>当前存在如下缺陷</p>
<ul>
<li>缺乏可解释性。本文提及的方法大多数属于两阶段方法，很多研究专注于联合嵌入方法上。该联合嵌入模型隐式融合了表达式和候选对象，然后选择最匹配的对象。因此，文本和视觉内容的匹配过程更像是一个黑匣子，推理过程无法可视化。虽然使用了注意机制，但它只显示了对决策过程更重要的参与区域。如何形象化和解释REC过程中的推理步骤仍然是一个挑战。</li>
<li>难以评估中间结果。在评价过程中，系统只能对最终的预测进行评价，而不会对中间的分步推理过程进行评价，不利于可解释模型的开发。许多模型倾向于给出直接的答案，而不需要中间的推理过程。因此，人们很难对模型的推理能力进行评估，也很难分析模型的缺陷。</li>
<li>存在严重的数据集偏差问题。在数据选择和注解过程中，许多表达式直接用属性描述所指对象。这种不平衡使得模型只能学习浅层的关联，而不能实现图文的联合理解。<ul>
<li>Cirik et al. [73] 指出REC模型可以在没有推理的情况下在图像中定位目标，仅依赖于通过一些隐含的线索来记忆目标，例如由意外偏差引入的浅层相关性。此外，REC  模型可能会忽略表达式，也可以胜过仅使用图像信息的随机猜测。这些模型无法真正理解视觉和语言信息，不适用于实际场景。这种数据集偏差问题在视觉和语言任务中很常见。例如，在视觉问答任务中，模型严重依赖问答之间的表面相关性，仅根据文本统计相关性预测答案，而不了解视觉内容。</li>
</ul>
</li>
</ul>
<p>是否到达了性能瓶颈</p>
<ul>
<li>当前的REC方法更依赖于对对象的属性和关系的识别。而事实是现有的数据集包含有限数量的属性。因此，REC技术在这些有限的数据集上取得了良好的性能。也就是说，设计更复杂的模型并不能产生更多的性能增益，也不需要在有限的数据集上进行更复杂的推理过程。基于图的模型就是例子。尽管引入了更全面的关系编码，但他们没有观察到现有引用表达式数据集的显著改进。</li>
</ul>
<p>在这项任务中，下一个技术上的大飞跃可能是什么？</p>
<ul>
<li><p>基于图和基于模块的方法在一定程度上实现了推理的可解释性，语言预训练方法建立了视觉和文本概念的对应关系。未来的方向将集中在如何将语言训练前的训练与REC任务中的可解释性结合起来。</p>
</li>
<li><p>本文作者相信仍有潜力更好地利用推理链的概念来解决引用表达方面的挑战。</p>
<ul>
<li><p>推理链是指问题解决过程中的逻辑推理步骤。正如人们在推理过程中会遵循一定的规则顺序一样，推理链旨在从空间和语义两个角度考虑指称表达理解任务。例如，对于“绿色包右边的人”这个表达，一个合理的推理过程应该是找到图像中的所有包，识别出绿色的那个，然后定位到它右边的那个人。</p>
</li>
<li><p>当前已经提出了一些需要  REC 模型进行推理的数据集。Liu et al. 提出 CLEVR-Ref+ [62]，它包含渲染图像和自动生成的表情，图像中的对象是具有属性的简单 3D 形状。如图 10  所示，REC 系统只需两个步骤即可在 RefCOCO [59] 数据集中定位目标对象。在 CLEVR-Ref+ [62]  数据集上，系统需要多个推理步骤。</p>
<p><img src="/2022/03/05/rec/1647867470699.png" alt="1647867470699"></p>
</li>
<li><p>Chen et al. 提出Cops-Ref [63]，涵盖了多种可以灵活组合的推理逻辑并具有丰富的视觉属性。他们在这个新数据集上评估了最先进的  REC 模型，并观察到与在传统引用表达式数据集上取得的卓越性能相比，模型的性能在这个数据集上有显著的下降。这表明大多数模型可能只学习到了数据集的偏置，而不是推理。 REC  系统仍然要求一个长且语义丰富的表达式来帮助评估模型的推理能力。开发具有“真正”推理能力的 REC 模型将是下一个挑战。</p>
</li>
</ul>
</li>
</ul>
<h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>本文回顾了REC的最新技术，首先回顾了将参考描述和图像区域嵌入到公共特征空间的最流行的方法，然后描述了基于它的更先进的基于模块化和基于图形的模型，以及one-stage和预训练模型等方法。根据数据集、主干模型、设置对结果进行分组，进行公平的比较。最后建议在引用表达式中考虑复杂的组合推理让模型进行学习，以作用于更多的视觉语言任务。</p>
<h2 id="论文中的表格"><a href="#论文中的表格" class="headerlink" title="论文中的表格"></a>论文中的表格</h2><p><img src="/2022/03/05/rec/1647867783007.png" alt="1647867783007"></p>
<p><img src="/2022/03/05/rec/1647867815328.png" alt="1647867815328"></p>
<p>ReferitGame上的弱监督学习结果</p>
<p><img src="/2022/03/05/rec/1647867828706.png" alt="1647867828706"></p>
<p>RefCOCO/+/g上各个模型的准确率（top-1 acc）（TranVG论文中的数据用的是TABLE V里面的）</p>
<p>当使用了真实边界框（人工标注）的时候</p>
<p><img src="/2022/03/05/rec/1647867848245.png" alt="1647867848245"></p>
<p>当使用了检测到的物体（模型检测）的时候</p>
<p><img src="/2022/03/05/rec/1647867866973.png" alt="1647867866973"></p>
<p> FLICKR30K实体集上的结果</p>
<p><img src="/2022/03/05/rec/1647868665661.png" alt="1647868665661"></p>
<p>COPS-REF上的结果</p>
<p><img src="/2022/03/05/rec/1647868676707.png" alt="1647868676707"></p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>Revisiting VG</title>
    <url>/2022/03/04/revisit-vg/</url>
    <content><![CDATA[<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>VG：在图像中定位结构化描述的元素。</p>
<p>结构化描述：一个自然语言短语，被解析为一个场景图或主谓宾三元组。</p>
<h3 id="场景图"><a href="#场景图" class="headerlink" title="场景图"></a>场景图</h3><p><img src="/2022/03/04/revisit-vg/1646963318842.png" alt="1646963318842"></p>
<p>给定一个场景图和一个输入图像，任务是创建与指定对象对应的边界框，使定位的对象具有指定的属性和关系。</p>
<p>energy score：反映场景图和定位框之间的匹配质量（lower is better），可用于在检索任务中对图像进行排名。</p>
<h3 id="主谓宾三元组"><a href="#主谓宾三元组" class="headerlink" title="主谓宾三元组"></a>主谓宾三元组</h3><p><img src="/2022/03/04/revisit-vg/1646963932294.png" alt="1646963932294"></p>
<p>给定一个三元组和一个输入图像，任务是创建与命名的主题和对象相对应的边界框，以使得定位的框适合指定的谓词。</p>
<h2 id="Image-Retrieval-Using-Scene-Graphs"><a href="#Image-Retrieval-Using-Scene-Graphs" class="headerlink" title="Image Retrieval Using Scene Graphs"></a>Image Retrieval Using Scene Graphs</h2><p>这一方法不执行任何语言分析，假定自然语言描述已经转换为场景图。</p>
<p>训练方式：有监督训练，边框用对象类别和属性来标记，边框对用关系来标记。</p>
<h3 id="原实验成果"><a href="#原实验成果" class="headerlink" title="原实验成果"></a>原实验成果</h3><ul>
<li><p>采用Recall@k作为评价指标</p>
</li>
<li><p>优于一些baseline</p>
</li>
<li><p>相较于两种消融形式更好</p>
<ol>
<li>只使用对象概率（忽略属性和关系概率）</li>
<li>同时使用对象和属性概率，忽略关系概率</li>
</ol>
</li>
</ul>
<h3 id="Revisit-IRSG"><a href="#Revisit-IRSG" class="headerlink" title="Revisit IRSG"></a>Revisit IRSG</h3><p>关注问题：</p>
<ol>
<li>除了使用对象信息之外，使用关系信息是否有助于系统的性能提升</li>
<li>这一研究中使用的数据集中是否存在可利用偏差，类似于Cirik et al. (2018b)在Google-Ref 数据集上的发现<ul>
<li>此处偏差的定义是：数据集本身允许学习算法依赖于浅层相关性，而不是实际解决预期的任务</li>
</ul>
</li>
<li>如果数据集存在这种偏差，那么IRSG在不含这种偏差的其他数据集上表现如何</li>
</ol>
<p>使用一个baseline image-retrieval method，只使用对象概率信息。</p>
<p>对于给定的测试图像和一个场景图查询，在测试图像中运行IRSG的Geodesic Object Proposal method以获得边界框。然后在每个边界框上跑IRSG训练好的的CNN以获得每个对象类别的概率。</p>
<p>对于查询中命名的每个对象类别，baseline只为该查询选择具有最高概率的边界框，不使用属性或关系信息。</p>
<p>然后使用Recall@K来比较二者的性能。</p>
<p>Recall@k的计算方式：</p>
<ul>
<li>对于给定的场景图查询，记Sp为测试集中的positive image（真实对象、属性和关系符合查询），Sn为测试集中的negative image，运行IRSG，返回每个图像相对于场景图的Energy score。</li>
<li>此外，还计算第二个分数：最高对象类别概率的几何平均值（这一分数忽略了属性和关系信息）。</li>
<li>由于Sp因不同的查询而异，分别考虑Sp中的每一个正样本Ip，将Ip单独放到有所有负样本的池中，询问Ip是否在top k之中，记R@k为Sp在top k中的比例。</li>
</ul>
<p>经过实验，发现IRSG的表现并没有因为系统的关系模型而受益（似乎也没有从属性模型中受益）。</p>
<p>可能的原因：</p>
<ul>
<li>IRSG中的对象关系模型(高斯混合模型)没有捕获有用的信息</li>
<li>在数据集中存在偏差</li>
</ul>
<p>结论：</p>
<ul>
<li>由GMM计算的关系概率没有捕获有用的信息。</li>
<li>数据集中超过一半的positive test images存在偏差（例如，当给出诸如“穿着衬衫的站着的人”的查询时，不需要区分哪个是穿着“衬衫”的特定“站着的人”:每种只有一个。）</li>
</ul>
<p>在clean queries and data上结果相似。</p>
<h3 id="Revisit-“Referring-Relationship”-Grounding"><a href="#Revisit-“Referring-Relationship”-Grounding" class="headerlink" title="Revisit “Referring Relationship” Grounding"></a>Revisit “Referring Relationship” Grounding</h3><p>Krishna et al. (2018)使用迭代attention根据给定谓词在图像区域之间移动，以便定位主体和客体。</p>
<p>评价指标：主题（subject）和对象（object）检测与真实边界框的平均交集(IOU)</p>
<p>这一指标无法对检测率进行反馈。</p>
<p>对比实验：</p>
<ul>
<li><p>数据集：VRD</p>
</li>
<li><p>使用fast-RCNN进行目标检测，对每个测试图像运行fast-RCNN（微调预训练版本），并为每个查询选择主体和客体类别的最高可信度边界框。(如果查询主题和对象是相同的类别，我们将主题和对象随机分配到最高和第二高的置信度框中。)</p>
</li>
<li><p>对于每个查询，我们手动检查每个测试图像中预测的主题和对象框的可视化，以确定主题和对象框是否适合查询的主题、对象和谓词。对于56%的图像/查询对，fast-RCNN已经识别出正确的主题和对象框。这说明不使用任何关系信息，就能在56%的时间里识别出正确的主题和对象框。</p>
</li>
<li><p>这表明数据集中存在明显的偏差，这就对该数据集中任何与baseline不相比较的已发表的参考关系结果提出了质疑。</p>
</li>
</ul>
<h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>IRSG这一被大量引用的方法表现（Recall@k）未能超过baseline（object-only）（从图表上看几乎一致，IRSG略高一点），且似乎并没有使用object之间的信息，数据集（IRSG作者提供的）中存在可利用偏差。在干净的数据集上，IRSG模型和geometric mean model性能（Recall@k）基本一致（从图表上看IRSG略低一些）</p>
<p>Krishna et al. (2018).的工作使用的VRD数据集也存在着可利用偏差（前面没有研究指出），但目前只考察了这一篇，另外几篇使用了VRD的还没revisit。</p>
<p>今后的工作需要关注模型的学习内容和方式，数据集是否包含可利用偏见（exploitable bias）</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>t5model</title>
    <url>/2022/06/11/t5model/</url>
    <content><![CDATA[<h2 id="Abstract"><a href="#Abstract" class="headerlink" title="Abstract"></a>Abstract</h2><p>T5（Transfer Text-to-Text Transformer）模型：一个统一框架，靠着大力出奇迹，将所有 NLP 任务都转化成 Text-to-Text （文本到文本）任务。</p>
<p><img src="/2022/06/11/t5model/1653383618153.png" alt="1653383618153"></p>
<h2 id="数据预处理方法"><a href="#数据预处理方法" class="headerlink" title="数据预处理方法"></a>数据预处理方法</h2><ul>
<li>只保留结尾是正常符号（ 句号、感叹号、问号或结尾引号 ）的行</li>
<li>删除任何包含不好的词的页面，具体词表参考List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words库。还有一些没有见过的停用词</li>
<li>包含Javascript（是一种具有函数优先的轻量级，解释型或即时编译型的 编程语言 ）的词的行全部去掉。</li>
<li>包含编程语言中常用大括号‘{’的页面</li>
<li>任何包含lorem ipsum占位符（用于排版测试）的页面</li>
<li>连续三句话重复出现的情况，只保留一个</li>
</ul>
<h2 id="常用的三种模型"><a href="#常用的三种模型" class="headerlink" title="常用的三种模型"></a>常用的三种模型</h2><p><img src="/2022/06/11/t5model/1653383682900.png" alt="1653383682900"></p>
<ol>
<li>第一种，Encoder-Decoder 型 ，即 Seq2Seq 常用模型，分成 Encoder 和 Decoder 两部分，对于 Encoder 部分，输入可以看到全体，之后结果输给 Decoder，而 Decoder 因为输出方式只能看到之前的。此架构代表是 MASS、BART等；</li>
<li>第二种， 相当于上面的  Decoder 部分 ，当前时间步只能看到之前时间步信息。典型代表是 GPT2 、 CTRL ；</li>
<li>第三种， Prefix LM（Language Model） 型 ，可看作是上面 Encoder 和 Decoder 的融合体，一部分如 Encoder 一样能看到全体信息，一部分如 Decoder 一样只能看到过去信息， UniLM 便是此结构。</li>
</ol>
<h2 id="对预训练目标的范围探索"><a href="#对预训练目标的范围探索" class="headerlink" title="对预训练目标的范围探索"></a>对预训练目标的范围探索</h2><p><img src="/2022/06/11/t5model/1653383737637.png" alt="1653383737637"></p>
<p>第一个方面，高层次方法（自监督的预训练方法）对比，总共三种方式，其中发现 Bert-style 最好。 </p>
<ul>
<li>语言模型式，就是 GPT-2 那种方式，从左到右预测；</li>
<li>BERT-style 式，就是像 BERT 一样将一部分给破坏掉，然后还原出来；</li>
<li>Deshuffling （顺序还原）式，就是将文本打乱，然后还原出来。</li>
</ul>
<p>第二方面，对文本一部分进行破坏时的策略，也分三种方法，效果最好的是 Replace Span 法，类似做法如 SpanBERT 也证明了有效性。 </p>
<ul>
<li>Mask 法，如现在大多模型的做法，将被破坏 token 换成特殊符如 [M]；</li>
<li>replace span（小段替换）法，可以把它当作是把上面 Mask 法中相邻 [M] 都合成了一个特殊符，每一小段替换一个特殊符，提高计算效率；</li>
<li>Drop 法，没有替换操作，直接随机丢弃一些字符。</li>
</ul>
<p>第三方面，到底该对文本百分之多少进行破坏呢，挑了 4 个值，10%，15%，25%，50%，最后发现 BERT 的 15% 就很 ok了。</p>
<p>第四方面，因为 Replace Span 需要决定对大概多长的小段进行破坏，于是对不同长度进行探索，2，3，5，10 这四个值，最后发现 3 结果最好。</p>
<h2 id="T5-Model"><a href="#T5-Model" class="headerlink" title="T5 Model"></a>T5 Model</h2><ul>
<li>Transformer Encoder-Decoder 模型；</li>
<li>BERT-style 式的破坏方法；</li>
<li>Replace Span 的破坏策略；</li>
<li>15 %的破坏比；</li>
<li>3 的破坏小段长度。</li>
</ul>
<h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><ul>
<li>Small：encoder,decoder都有6层，hidden dim为512，8个head。</li>
<li>Base ：encoder,decoder用BERT-base设置。</li>
<li>Large：encoder,decoder用BERT-large设置，除了层数只用12层。</li>
<li>3B、11B：层数都用 24 层，不同的是其中头数量和前向层的维度。</li>
</ul>
<h3 id="效果"><a href="#效果" class="headerlink" title="效果"></a>效果</h3><img src="1653384299639.png" alt="1653384299639" style="zoom: 80%;">

<h3 id="多语言模型mT5"><a href="#多语言模型mT5" class="headerlink" title="多语言模型mT5"></a>多语言模型mT5</h3><p>和T5不同之处</p>
<ul>
<li>BPE 多语言预训练编码器 </li>
<li>Wordpiece 词嵌入</li>
<li>Sentencepiece 意群（句子嵌入，通用的各种语言都能切分）</li>
<li>激活函数变更：Gated-GELU activation替代ReLU； </li>
<li>无标签数据不做dropout； </li>
<li>embedding和分类层不做参数共享； </li>
<li>更大的d_model，更小的num_heads和d_ff；</li>
</ul>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>线段树模板及常见应用</title>
    <url>/2022/03/06/test/</url>
    <content><![CDATA[<h2 id="线段树"><a href="#线段树" class="headerlink" title="线段树"></a>线段树</h2><h3 id="区间修改，区间查询"><a href="#区间修改，区间查询" class="headerlink" title="区间修改，区间查询"></a>区间修改，区间查询</h3><pre class="line-numbers language-c++" data-language="c++"><code class="language-c++">#include &lt;bits/stdc++.h&gt;
#define ll(x) (x &lt;&lt; 1)
#define rr(x) (x &lt;&lt; 1 | 1)
using namespace std;
const int N = 400000 + 5;
typedef long long lol;

lol n, m;
lol w[N];

struct seg_tree {
    lol sum, l, r, lazy;
} t[N];

lol gi() {
    lol ans = 0, f = 1;
    char i = getchar();
    while (i &lt; '0' || i &gt; '9') {
        if (i == '-') f = -1;
        i = getchar();
    }
    while (i &gt;= '0' &amp;&amp; i &lt;= '9') {
        ans = ans * 10 + i - '0';
        i = getchar();
    }
    return ans * f;
}

void up(lol root) {
    t[root].sum = t[ll(root)].sum + t[rr(root)].sum;
}

void build(lol root, lol l, lol r) {
    int mid = l + r &gt;&gt; 1;
    t[root].l = l, t[root].r = r;
    if (l == r) {
        t[root].sum = w[l];
        return;
    }
    build(ll(root), l, mid);
    build(rr(root), mid + 1, r);
    up(root);
}

void pushdown(lol root) {
    lol mid = t[root].l + t[root].r &gt;&gt; 1;
    t[ll(root)].lazy += t[root].lazy;
    t[rr(root)].lazy += t[root].lazy;
    t[ll(root)].sum += t[root].lazy * (mid - t[root].l + 1);
    t[rr(root)].sum += t[root].lazy * (t[root].r - mid);
    t[root].lazy = 0;
}



void updata(lol root, lol l, lol r, lol val) {
    lol mid = t[root].l + t[root].r &gt;&gt; 1;
    if (l &lt;= t[root].l &amp;&amp; t[root].r &lt;= r) {
        t[root].sum += val * (t[root].r - t[root].l + 1);
        t[root].lazy += val;
        return;
    }
    if (t[root].lazy) pushdown(root);
    if (l &lt;= mid) updata(ll(root), l, r, val);
    if (mid &lt; r) updata(rr(root), l, r, val);
    up(root);
}

lol query(lol root, lol l, lol r) {
    if (l &lt;= t[root].l &amp;&amp; t[root].r &lt;= r) return t[root].sum;
    if (r &lt; t[root].l || t[root].r &lt; l) return 0;
    if (t[root].lazy) pushdown(root);
    return query(ll(root), l, r) + query(rr(root), l, r);
}

int main() {
    lol f, x, y, val;
    n = gi();
    m = gi();
    for (lol i = 1; i &lt;= n; i++)
        w[i] = gi();
    build(1, 1, n);
    for (lol i = 1; i &lt;= m; i++) {
        f = gi();
        x = gi();
        y = gi();
        if (f == 1)
            val = gi(), updata(1, x, y, val);
        else
            printf("%lld\n", query(1, x, y));
    }
    return 0;
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Transformer</title>
    <url>/2022/02/27/transformer/</url>
    <content><![CDATA[<h2 id="注意力机制"><a href="#注意力机制" class="headerlink" title="注意力机制"></a>注意力机制</h2><h3 id="Memory-Based"><a href="#Memory-Based" class="headerlink" title="Memory-Based"></a>Memory-Based</h3><p>QKV模型，Transformer中采用的是这种。 假设输入为 q，Memory 中以（k，v）形式存储需要的上下文。k 是 question，v 是 answer，q 是新来的 question，看看历史 memory 中 q 和哪个 k 更相似，根据相似度决定权重，使用k 对应的 v，合成当前 question 的 answer。</p>
<p>注意力函数是对一个query向量，让它和一系列的key向量-value向量对进行映射并输出结果的函数，输出是value的加权和，权重是key和query的相似度计算来的；即使key-value对不改变，随着query的改变，输出向量会不一样,但输出的维度是和value一样的。</p>
<p>从本质上理解，Attention是从大量信息中有选择地筛选出少量重要信息并聚焦到这些重要信息上，忽略大多不重要的信息。聚焦的过程体现在权重系数的计算上，权重越大越聚焦于其对应的Value值上，即权重代表了信息的重要性，而Value是其对应的信息。</p>
<img src="1645790269336.png" alt="1645790269336" style="zoom: 50%;">

<p>工作流程：</p>
<p>attention函数共有三步完成得到attention value。</p>
<ul>
<li>Q与K进行相似度计算得到权值</li>
<li>对上部权值归一化</li>
<li>用归一化的权值与V加权求和</li>
</ul>
<p> 将Source中的构成元素想象成是由一系列的(Key,Value)数据对构成，此时给定Target中的某个元素Query，通过计算Query和各个Key的相似性或者相关性，得到每个Key对应Value的权重系数，然后对Value进行加权求和，即得到了最终的Attention数值。所以本质上Attention机制是对Source中元素的Value值进行加权求和，而Query和Key用来计算对应Value的权重系数。即可以将其本质思想改写为如下公式： </p>
<p>$$<br>\text { Attention }\left(\text { Query }<em>{j}, \text { Source }\right)=\sum</em>{i=1}^{N} \text { Similarity }\left(\text { Query }<em>{j}, \text { Key }</em>{i}\right) * \text { Value }_{i}<br>$$</p>
<p>在自然语言任务中，往往K和V是相同的。这时计算出的attention value是一个向量，代表序列元素$xj$的编码向量。此向量中包含了元素$xj$的上下文关系，即包含全局联系也拥有局部联系。全局联系是因为在求相似度的时候，序列中元素与其他所有元素的相似度计算，然后加权得到了编码向量。局部联系可以这么解释，因为它所计算出的attention value是属于当前输入的$x_j$的。这也就是attention的强大优势之一，它可以灵活的捕捉长期和local依赖，而且是一步到位的。</p>
<img src="1645791150589.png" alt="1645791150589" style="zoom: 50%;">

<h3 id="Transformer中的使用"><a href="#Transformer中的使用" class="headerlink" title="Transformer中的使用"></a>Transformer中的使用</h3><ul>
<li><p>对依赖项进行建模，无需考虑它们在输入/输出序列中的距离。</p>
</li>
<li><p>完全抛弃了卷积和递归，只使用注意力机制来构建输入和输出之间的全局依赖关系。</p>
</li>
<li><p>由于传统的卷积神经网络中，关联来自于任意两个输入/输出位置的信号所需的操作随着二者的位置之间的距离而增长，这使得网络更加难以学习远距离位置之间的依赖关系。Transformer能够将这样的操作减少到一个恒定的数目（任意两个单位之间的距离相同）。同时，考虑到平均注意力加权降低了有效分辨率的缺陷，Transformer采用了多头注意力机制。</p>
</li>
<li><p>Self-Attention：一种将单个序列的不同位置关联起来以计算序列表示的注意力机制。</p>
</li>
</ul>
<p><strong>优点</strong>：</p>
<ul>
<li>能够捕捉全局和局部的联系，从attention函数就可以看出来，它先是进行序列的每一个元素与其他元素的对比，在这个过程中每一个元素间的距离都是一，因此它比时间序列RNNs的一步步递推得到长期依赖关系好的多，越长的序列RNNs捕捉长期依赖关系就越弱。</li>
<li>提供并行性，能通过并行计算减少模型训练时间</li>
<li>模型复杂度小，参数少</li>
</ul>
<p><strong>缺点</strong>：</p>
<ul>
<li>Attention机制不是”distance-aware”的，它不能捕捉语序顺序，而在NLP中语序顺序是很重要的（对此，Transformer采用的处理方式是Position Encoding，在计算机视觉领域是如何解决的？查阅资料发现同样是采用Position Encoding进行解决的。（待读论文：Image Transformer、Vision Transformer）可否通过预训练使得模型具有识别图像序列顺序的能力？）</li>
</ul>
<h2 id="Encoder-Decoder"><a href="#Encoder-Decoder" class="headerlink" title="Encoder-Decoder"></a>Encoder-Decoder</h2><h3 id="结构图"><a href="#结构图" class="headerlink" title="结构图"></a>结构图</h3><img src="1645587474058.png" alt="1645587474058" style="zoom: 50%;">

<p><strong>Example</strong>【 <a href="https://zhuanlan.zhihu.com/p/364659780">Bert前篇：手把手带你详解Transformer原理 - 知乎 (zhihu.com)</a> 】：</p>
<img src="v2-31a51936a0af60dd63e68c98e3a7ab0f_r.jpg" alt="preview" style="zoom: 50%;">

<h3 id="Encoder"><a href="#Encoder" class="headerlink" title="Encoder"></a>Encoder</h3><p>由6个相同的层组成，每个层包括两个子层，第一个是多头自注意力机制，第二个是简单的position-wise全连接神经网络。在每个子层周围使用残差连接，然后进行层 normalization。每个sub-层的输出为$LayerNorm(x+Sublayer(x))$。</p>
<p><strong>Q：position-wise指的是什么？</strong></p>
<p>解释一：区别于普通的全连接网络，这里FFN的输入是序列中每个位置上的元素，而不是整个序列，所以每个元素完全可以独立计算，最极端节省内存的做法是遍历序列，每次只取一个元素得到FFN的结果，但是这样做时间消耗太大，“分段”的含义就是做下折中，将序列分成子段，也就是多个子序列，每次读取一个子序列进行FFN计算，最后将多份的结果拼接。 </p>
<p>解释二： 从卷积的角度解释，这里的FFN等价于kernel_size=1的卷积，这样每个position都是独立运算的。如果kernel_size=2，或者其他，position之间就具有依赖性了，不能叫做position-wise了。</p>
<h3 id="Decoder"><a href="#Decoder" class="headerlink" title="Decoder"></a>Decoder</h3><p>同样是6个相同的层组成，但是每个层包括三个子层，第三个sub-层用于对Encoder堆栈的输出执行多头注意力机制。残差连接与Encoder相同。修改了注意力层以避免当前位置继续参与（attend）到后面的位置中。</p>
<p><strong>Mask</strong>：Mask操作是为了避免解码器在训练的时候看到当前时刻看到以后时间的信息。</p>
<p>对于t时刻的query，应该只看到key(1), key(2),…key(t-1)的数据，因为key(t)在当前时刻还不存在。</p>
<p>Masked的作用是使对应value的权值为0，对于query(t)和未来时刻的key的内积，赋值为负无穷大（-inf），这样使用softmax计算权值为0，所以计算output的时候只用到了t-1以前时刻的key-value pair的信息。保证了训练时和预测时的情况是一样的。</p>
<img src="1645862082581.png" alt="1645862082581" style="zoom: 33%;">

<p>mask的具体做法：（ <a href="https://zhuanlan.zhihu.com/p/364659780">Bert前篇：手把手带你详解Transformer原理 - 知乎 (zhihu.com)</a> ）</p>
<p><img src="/2022/02/27/transformer/1645865219155.png" alt="1645865219155"></p>
<h2 id="Attention"><a href="#Attention" class="headerlink" title="Attention"></a>Attention</h2><img src="1645778258219.png" alt="1645778258219" style="zoom: 67%;">

<h3 id="Scaled-Dot-Product-Attention"><a href="#Scaled-Dot-Product-Attention" class="headerlink" title="Scaled Dot-Product Attention"></a>Scaled Dot-Product Attention</h3><p>输入由维度为$d_k$的queries和key以及维度为$d_v$的values组成。使用所有keys计算queries的点积，并除以$\sqrt{d_k}$，然后用softmax函数获得与values相乘的权重。公式如下：</p>
<p>$$<br>\text { Attention }(Q, K, V)=\operatorname{softmax}\left(\frac{Q K^{T}}{\sqrt{d_{k}}}\right) V<br>$$<br><strong>做法含义</strong>： 向量内积是一个向量在另一个向量上的投影长度乘以另一个向量的长度。内积的几何意义是一个向量在另一个向量上的投影，进而内积越大说明相似度越高。</p>
<p>对于attention中计算两个向量之间的相关度，两个常用函数为Additive attention和dot-product（multiple） attention。点积从公式中可以体现出来，Additive attention是使用一个前馈神经网络（只含一个隐藏层）来计算兼容性函数。</p>
<p>二者优缺点分析：dot-product attention，可以利用矩阵运算，计算速度更快，并且更节省空间，但是要求 Q 和 K向量维度相同，如果不同需要进行一定的映射变换。在dk较小时二者表现相似，但是dk较大时，dot-product attention表现不如additive attention，因为dk较大时点积之后的结果较大，求softmax之后，梯度会很小，不利于计算，需要做一定的缩放。</p>
<p>知乎：较长的序列（512）会导致QK的内积的方差很大，相对差距变大，极值做出来softmax会倾向于1， 计算的梯度接近0，导致难以学习，除以sqrt(dk)可以避免这种情况。</p>
<p>参考：</p>
<ul>
<li><a href="https://www.jianshu.com/p/70905e28d662">Additive Attention 和 Dot-product Attention</a> </li>
<li><a href="https://zhuanlan.zhihu.com/p/434357918">Transformer论文详解——想不懂都难 - 知乎 (zhihu.com)</a></li>
</ul>
<p><strong>作者采用的方案以及性能上的讨论</strong>：由于dot-product可以用高性能的矩阵乘法进行优化，计算速度快，空间上也更优，作者采用了dot-product。当$d_k$较小的时候，两种机制表现相近，Additive在$d_k$值较大的时候，无需对其缩放，因此性能优于dot-product。作者的观点是对于较大的$d_k$，dot-product在规模（magnitude）上会更大，这将softmax函数推入了极小梯度的区域。因此他们在公式中进行了除以$\sqrt{d_k}$这样一个缩放。</p>
<h3 id="Multi-Head-Attention"><a href="#Multi-Head-Attention" class="headerlink" title="Multi-Head Attention"></a>Multi-Head Attention</h3><p><strong>原理</strong>：与对keys、values、queries使用一个注意力机制不同，作者提出了一种更有效的方法：使用不同的线性投影，将queries、keys、values分别投影到$d_k$、$d_k$、$d_v$维（进行h次），然后在投影之后的queries、keys、values上并行执行attention function，得到维度为$d_v$的输出，然后将他们连接（concat）起来后再次投影产生最终值。	</p>
<p> <img src="/2022/02/27/transformer/v2-70a3f25fa1b618ecd9811567daca4ec9_r.jpg" alt="img"> </p>
<p><strong>优点</strong>：允许模型共同关注来自不同位置的不同表示子空间的信息，对于单个注意力头，平均化会抑制这一点。</p>
<p><img src="/2022/02/27/transformer/1645781140551.png" alt="1645781140551"></p>
<p><strong>实验参数</strong>：$h=8,d_k=d_v=d_{model}/h=64$</p>
<p><strong>性能讨论</strong>：由于每个头维度减少，总体计算量与单头注意力机制近似。</p>
<h3 id="Position-wise-Feed-Forward-Network"><a href="#Position-wise-Feed-Forward-Network" class="headerlink" title="Position-wise Feed-Forward Network"></a>Position-wise Feed-Forward Network</h3><p>除了注意力子层外，编码器与编码器中的每一层都包含一个全连接的前馈神经网络，分别相同应用于每个位置。包含两个线性变化，二者之间有一个ReLU激活函数。<br>$$<br>\operatorname{FFN}(x)=\max \left(0, x W_{1}+b_{1}\right) W_{2}+b_{2}<br>$$<br>transformer抽取序列信息：attention把序列里的信息抓取出来，在每个点做了一次全局的汇聚，里面包含了感兴趣的序列信息，以至于后面经过mlp做投影，把向量映射到更想要的语义空间，因为每个点都包含了序列相关信息，所以对每个点独立做MLP就可以了，这就是transformer如何抽取“序列信息”并加工的方法。</p>
<p>RNN抽取序列信息：MLP权重W是共用的，时序信息是通过把上一个时刻的输出接到当前时刻，与当前时刻的输入合并，再经过MLP,实现了序列信息的汇聚和提取。</p>
<p>区别：都是用MLP实现语义空间的转换，不一样的是如何传递“序列信息”。</p>
<img src="1645863103955.png" alt="1645863103955" style="zoom: 33%;">

<p><strong>实验参数</strong>：</p>
<ul>
<li>输入/输出维度$d_{model}=512$</li>
<li>inner-层维度$d_{ff}=2048$</li>
</ul>
<h3 id="Embedding-amp-Softmax"><a href="#Embedding-amp-Softmax" class="headerlink" title="Embedding &amp; Softmax"></a>Embedding &amp; Softmax</h3><p>通过学习好的Embedding将输入/输出token转换为维度为$d_{model}$的向量。在嵌入层中，作者对权重乘以$\sqrt{d_{model}}$</p>
<p>原因：Embedding的分布往往是$N(1, \frac{1}{\sqrt{d_{model}}})$</p>
<h3 id="Position-Encoding"><a href="#Position-Encoding" class="headerlink" title="Position Encoding"></a>Position Encoding</h3><p>为了让模型利用序列的顺序，需要在序列中注入关于序列的相对或绝对位置的信息。</p>
<p><strong>做法</strong>：在解码器/编码器的堆栈底部加入位置编码（Position Encoding）。位置编码与Embedding具有相同的维度$d_{model}$，二者可以直接相加。位置编码可以有多种选择，通过学习得到的（learned）或是固定的（fixed）。Transformer采用绝对位置编码。</p>
<p>在这里论文中使用了$sin$和$cos$函数的线性变换来提供给模型位置信息:<br>$$<br>\begin{aligned}<br>P E_{(p o s, 2 i)} &amp;=\sin \left(p o s / 10000^{2 i / d_{\text {model }}}\right) \<br>P E_{(p o s, 2 i+1)} &amp;=\cos \left(p o s / 10000^{2 i / d_{\text {model }}}\right)<br>\end{aligned}<br>$$</p>
<h3 id="为何使用self-attention"><a href="#为何使用self-attention" class="headerlink" title="为何使用self-attention"></a>为何使用self-attention</h3><ul>
<li>每层的总计算复杂度</li>
<li>可并行性</li>
<li>网络中远程依赖项之间的路径长度</li>
</ul>
<p> <img src="/2022/02/27/transformer/v2-197559ccb4049ff985e6f095a68a5c37_r.jpg" alt="img"> </p>
<p> <img src="/2022/02/27/transformer/v2-3ce7a8eea027f468ea0d8926b8f149b5_r.jpg" alt="img"> </p>
<h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ol>
<li>并行效率高：相较于RNN 第t时间步的隐藏状态ht要等前一个时间步ht-1的隐藏状态输出之后才能处理； Transformer同时提取所有上下文的信息。</li>
<li>信息传递损失小： RNN在序列过长会导致梯度爆炸或梯度消失或遗忘，RNN改进模型LSTM和GRU能够部分解决梯度消失的问题。transformer获取所有位置的信息的距离都是1。</li>
<li>信息融合效率高：卷积每次看比较小的窗口，对于距离较远的像素，需要很多层卷积一层一层扩大感受野才能把两个像素融合起来。transformer获取所有位置的信息的距离都是1。</li>
</ol>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>Vision Transformer</title>
    <url>/2022/03/02/vit/</url>
    <content><![CDATA[<h2 id="已有研究"><a href="#已有研究" class="headerlink" title="已有研究"></a>已有研究</h2><p>Transformer在NLP上的成功</p>
<p>Attention机制在CV领域的应用，能够完全替代卷积且在理论上验证有效。</p>
<p>缺陷：</p>
<ul>
<li>由于使用了专门的注意力机制，但未在现代硬件加速器上有效拓展，计算效率低</li>
<li>在大规模数据集上，Resnet-like的架构依然是SOTA</li>
</ul>
<p>作者的工作：尽可能少的修改标准的Transformer，将其应用于图像领域。</p>
<h2 id="自注意力机制的应用"><a href="#自注意力机制的应用" class="headerlink" title="自注意力机制的应用"></a>自注意力机制的应用</h2><p>核心思想：以每个像素作为序列中的元素会导致很高的计算复杂度，因此通过一些方法缩短序列的长度。</p>
<ul>
<li>Parmar et al. (2018）：仅在每个查询像素的局部邻域中应用自注意力机制，而非全局应用。stand alone attention</li>
<li>Hu et al., 2019; Ramachandran et al., 2019; Zhao et al., 2020：使用上述方法，完全替代了卷积。</li>
<li>Sparse Transformers (Child et al., 2019) ：应用了全局注意力的可拓展近似（scalable approximation）</li>
<li>Weissenborn et al., 2019：将自注意力机制用于大小不同的块中，以扩展（scale）注意力。</li>
<li>Hoet al., 2019; Wang et al., 2020a：尝试了一种极端情况，只沿单个轴。轴注意力axial attention，先在横轴上做注意力，再在纵轴上做注意力。</li>
</ul>
<p>上述方法的缺陷：需要复杂的工程才能高效应用于硬件加速器。</p>
<p>相似研究：</p>
<ul>
<li>Cordonnier et al. (2020)：将输入图像分割成多个大小为2x2的块，在顶部（top）应用完整的自注意力机制。</li>
<li>缺陷：仅适用于小分辨率图像，且效果不如SOTA的CNN。</li>
</ul>
<h3 id="其他研究（TODO）"><a href="#其他研究（TODO）" class="headerlink" title="其他研究（TODO）"></a>其他研究（TODO）</h3><p>结合self-attention的CNN</p>
<p>image GPT（iGPT）</p>
<p>使用额外数据集以取得SOTA结果</p>
<p>当数据集大小变化时，CNN的性能变化</p>
<p>CNN在大规模数据集上进行迁移学习的研究</p>
<h2 id="模型架构"><a href="#模型架构" class="headerlink" title="模型架构"></a>模型架构</h2><p><img src="/2022/03/02/vit/1646548420444.png" alt="1646548420444"></p>
<p>首先将图像分为多个固定大小的patches，通过线性映射层（linear embedding）获得输入。由于图片块本身是有相对位置的，而transformer中不考虑位置信息，因此加入position embedding，然后将生成的向量序列送到标准的Transformer编码器中。为了实现分类，向序列中添加额外的可学习的”classification token”（模仿BERT），CLS token。</p>
<h2 id="输入的转换"><a href="#输入的转换" class="headerlink" title="输入的转换"></a>输入的转换</h2><p>将图像拆分成块，通过线性Embedding对这些patch进行转换，作为Transformer的输入。</p>
<p>Image patch相当于NLP中的tokens（words），即题目所表达的，一张图片相当于很多个16x16的单词。</p>
<p>效果：在Imagenet-1k这样的较小的数据集上比Resnet要低几个百分点，但是在大数据集上达到了SOTA。</p>
<p>原因：Transformer缺少CNN固有的inductive biaes，如平移等变性（translation equivariance）和局部性，因此在数据量不足的时候并不能很好的泛化。</p>
<p>改进方法：在大规模数据集（14M-300M images）上进行训练。</p>
<p>ViT存在的缺陷：需要大数据集上进行训练才能够取得优秀的效果。</p>
<p>输入维度的转换：</p>
<p>$(H,W,C)\rightarrow(N,P^2*C)$，其中（H，W）是图像的分辨率，C是channels的数目。（P，P）是image patch的分辨率，$N=HW/P^2$是patch的数量，作为Transformer的有效输入长度。</p>
<p>Transformer在所有层中使用恒定的潜在向量大小D，作者将patch展平，并通过可训练的线性投射层将其映射到D维。</p>
<h2 id="Position-Embedding"><a href="#Position-Embedding" class="headerlink" title="Position Embedding"></a>Position Embedding</h2><p>并非直接传具体的数字1,2,3,….而是有一个表，表的每一行代表了序号，每一行是一个向量，维度是768（$16<em>16</em>3$），这个表通过学习得到。然后将这个向量信息加到前面生成的向量里面（197x768：其中196是patch的个数，注：224x224的图片，16x16的patch size。然后还有个cls token（197x1）），这里的加并非concatenation，是直接sum。</p>
<p>使用标准的1D可学习的Position embedding，没有用2D的是因为没有显著提升。</p>
<p>从附录中的figure 10也可以看到，Position Embedding是能够通过学习得到相对位置信息的，且能够学习到2D的邻域相似性。这也验证了上面的结论。</p>
<img src="1646569234586.png" alt="1646569234586" style="zoom:67%;">

<h2 id="归纳偏置"><a href="#归纳偏置" class="headerlink" title="归纳偏置"></a>归纳偏置</h2><p>归纳偏置：一种先验知识（提前做好的假设）</p>
<ul>
<li>locality：卷积神经网络以滑动窗口的形式，一点一点在图片上进行卷积，因此假设在图片上相邻的区域会有相邻的特征。</li>
<li>平移等变形translation equivariance：$f(g(x))=g(f(x))$，无论先做f函数还是g函数，结果是不变的。其中f可以理解为卷积操作，g可以理解为平移操作。</li>
</ul>
<p>相较于CNN，ViT的image-specific归纳偏差要少得多。</p>
<p>在CNN中，整个模型的每一层有局部性，平移等效性，二维邻域结构。</p>
<p>在ViT中，只有MLP具有平移等效性和局部性，自注意力是全局的。</p>
<p>二维邻域结构的时候非常谨慎：在模型开始时，通过将图像切割成块并在fine-tuning time来调整针对不同分辨率图像的Position Embedding。除此之外，初始化时的position embedding不携带有关patch的2D位置信息，必须从头学习patch之间的所有空间关系。</p>
<h2 id="混合模型"><a href="#混合模型" class="headerlink" title="混合模型"></a>混合模型</h2><p>输入序列由CNN的特征图组成，而非原始的图像块。</p>
<p>然后通过patch embedding projection对从CNN特征图中提取得到的序列进行处理。</p>
<p>在特殊情况下，patch的空间大小可以为1x1，这意味着输入序列是通过简单地将特征图的空间维度展平并投影到Transformer的维度上获得的。</p>
<h2 id="微调以及更高的分辨率"><a href="#微调以及更高的分辨率" class="headerlink" title="微调以及更高的分辨率"></a>微调以及更高的分辨率</h2><p>将预训练的ViT模型的预测头移除，加入一个零初始化的DxK的前馈层，其中K是下游任务中类别的数量。</p>
<p>对于更高的分辨率，保持patch size不变，这样会产生更大的有效序列长度。</p>
<p>ViT可以在内存限制范围内处理任意长度的序列，但预训练的position embedding可能就不再适用了。</p>
<p>因此作者根据它们在原始图像中的位置，对预训练的position embedding进行2D插值。这种分辨率上的调整和patch extraction是将有关图像2D结构的归纳偏置注入到ViT的唯一的点。</p>
<h2 id="实验"><a href="#实验" class="headerlink" title="实验"></a>实验</h2><h3 id="数据集"><a href="#数据集" class="headerlink" title="数据集"></a>数据集</h3><p>预训练</p>
<ul>
<li>ILSVRC-2012（1.3M images、1k classes）文中称之为Imagenet。</li>
<li>ImageNet-21k （14M images、21k classes）</li>
<li>JFT（303M 高分辨率images、18k classes）</li>
</ul>
<p>下游任务</p>
<ul>
<li>ImageNet on the original validation labels and the cleaned-up<br>ReaL labels </li>
<li>CIFAR-10/100 </li>
<li>Oxford-IIIT Pets</li>
<li>Oxford Flowers-102</li>
<li>19-task VTAB classification suite</li>
</ul>
<p>预处理方法见（ Kolesnikov et al. (2020））</p>
<h3 id="模型变体"><a href="#模型变体" class="headerlink" title="模型变体"></a>模型变体</h3><img src="1646554416365.png" alt="1646554416365" style="zoom:67%;">

<p>ViT-L/16 means the“Large” variant with 16 × 16 input patch size.</p>
<p>Transformer的序列长度与块大小的平方成反比，因此块大小较小的模型计算成本更高。</p>
<h3 id="Baseline"><a href="#Baseline" class="headerlink" title="Baseline"></a>Baseline</h3><p>Resnet，但是batch normalization换成了group normalization，并采用了标准化的卷积。</p>
<p>对于混合体，将中间特征图输入到ViT中，patch大小为一个像素。</p>
<h3 id="训练参数-TODO"><a href="#训练参数-TODO" class="headerlink" title="训练参数(TODO)"></a>训练参数(TODO)</h3><h3 id="评价指标"><a href="#评价指标" class="headerlink" title="评价指标"></a>评价指标</h3><p>通过linear few-shot或者fine-tuning accuracy来进行评价。</p>
<p>fine-tuning accuracy在模型微调后进行评估。</p>
<p>linear few-shot是通过解决正则化最小二乘回归问题得到的，该问题将训练图像子集的（冻结）表示映射到${-1,1}^k$目标向量。即不去finetune，直接拿来做特征提取器，做一个regression。</p>
<p>在评价中主要用fine-tuning accuracy，在微调成本过高时使用few-shot进行快速动态评估。</p>
<h3 id="与SOTA模型的比较"><a href="#与SOTA模型的比较" class="headerlink" title="与SOTA模型的比较"></a>与SOTA模型的比较</h3><p>在性能指标有微小的提升，但是预训练使用的资源大大减少。</p>
<p>对于数据集大小较为敏感，需要大量数据集进行训练。在Imagenet上不如Resnet，但是在更大的数据集上超国了Resnet。</p>
<p>在迁移学习的准确率上，ViT的效果要好过Resnet。在预训练计算量较小时，hybrid能够改进纯transformer。但随着模型的增大，这种差异消失了。这一现象是惊人的，因为人们在直觉上会觉得，卷积局部特征处理能够帮助任何大小的ViT。此外，ViT在作者尝试过的预训练计算量范围内并没有达到性能饱和，因此可以做进一步的拓展，有进一步提升的空间。</p>
<p>作者的结论：卷积归纳偏差对较小的数据集很有用，但对于较大的数据集，直接从数据中学习相关模式就足够了，甚至是beneficial的。</p>
<h3 id="自监督训练"><a href="#自监督训练" class="headerlink" title="自监督训练"></a>自监督训练</h3><p>采用自监督方法（masked patch prediction，模仿BERT），使用ViT-B/16在Imagenet上实现了79.9%的准确率，比从头开始训练高了2%，但是依然比有监督训练低了4%。</p>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>与先前的使用自注意力机制的CV工作不同（采用image-specific的归纳偏置、ViT仅在对初始patch进行提取的时候使用了），ViT将图像表达为一系列的patch，并使用NLP中的Transformer对序列进行处理。这在大规模的数据集上取得了SOTA的效果，同时能够保持一个相对低的预训练代价。</p>
<p><strong>Further Work</strong></p>
<ul>
<li>将ViT应用于检测和分割任务</li>
<li>进行基于自监督学习的预训练方法的探索</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>优点</p>
<ul>
<li>使用相对少的计算资源</li>
<li>在大数据集上取得了SOTA</li>
<li>可以通过预训练进一步提升性能，在已观测范围内未达到性能饱和</li>
</ul>
<p>缺点</p>
<ul>
<li>在中小数据集上效果不佳（可以考虑采用混合模型，加入CNN的归纳偏置以提升效果）</li>
<li>在图片分辨率变化较大的时候，仅仅通过简单的2D插值难以保持Position Embedding的有效性</li>
</ul>
<p>进一步的工作</p>
<ul>
<li>论文仅在分类任务上做了验证，还可以尝试用于检测和分割任务</li>
<li>改进自监督学习的方法</li>
</ul>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
</search>
